# TASK

## Active
- None

## Backlog
- [ ] Investigate or replace single-node Whisper provider concurrency - Current `pluja/whishper` runtime does not appear to provide true intra-node parallel transcription on the CPU profile. Direct internal probes to `http://whishper:8000/transcribe/` returned two simultaneous identical requests in about `3009ms` and `5826ms`, showing the second request effectively waits behind the first inside the provider. Container inspection showed `/bin/whishper` exposes no worker or prefork flags, Fiber reports `Processes = 1` with `Prefork = Disabled`, `transcription/main.py` starts `uvicorn.run(...)` with the default single worker, and `transcription/transcribe.py` loads a fresh `FasterWhisperBackend` model on every request before transcribing. Decide whether to cap each physical Whisper node to `maxConcurrentRequests = 1`, scale out with more node containers, or build a custom Whisper image that reuses loaded models and runs multiple ASR workers.

## Completed
- [x] Redesign workspace map system around `map_md/` - Replaced the old root-level per-surface maps with a new `MAP.md` index plus domain child maps under `map_md/`, migrated desktop/admin/backend/shared routing responsibilities into the new files, updated `.github/agents/coder.agent.md` to read `MAP.md` first and then the relevant child maps, and removed the obsolete root `MAP.admin.md` and `MAP.backend.md` files. Validation passed with direct file existence checks for every child map referenced by `MAP.md`, a search confirming no lingering references to the removed map files in the tracked guidance files, and root checks confirming `MAP.admin.md` and `MAP.backend.md` no longer exist.
- [x] Fix admin IAP bank-hook history list 500 - Corrected `server/services/iap_bank_hook_history_store.py` to call the imported `normalize_pagination` and `build_pagination` helpers instead of undefined underscored names, which had been throwing a `NameError` and surfacing the admin bank-hook history table as a Flask `500 Internal Server Error`. Validation passed with `npm.cmd run build` in `admin-frontend/`, `docker compose up -d --build web`, `docker compose exec -T web python -m compileall server`, container health `{"status":"ok"}` at `http://127.0.0.1:5000/api/health`, and an unauthenticated probe of `/api/admin/iap/bank-hook-history?page=1&pageSize=20` now returning `401 {"error":"Login is required to use this feature"}` instead of a server error.
- [x] Show Create Sub credit estimate on button - Added authenticated Create Sub credit estimation, exposed `originalSubtitles` and `videoDuration` into `panel.subtitle.list`, debounced the estimate in `frontend/src/components/SubtitlePanel/useCreateSubCreditEstimate.js`, rendered the estimated credit cost directly on the Create Sub button, and showed a partial-estimate hint when only detect cost is known before origin subtitles exist. Validation passed with `python -m compileall server`, desktop `npm run lint` and `npm run build`, VS Code Problems clean, `docker compose up -d --build web`, direct service-layer estimate probes for cached-origin and detect-first cases, and `npm run desktop:launch`; the known Windows disk-cache warning still appeared during launch without blocking Electron.
- [x] Build server-backed Create Sub flow with configurable subtitle credits - Replaced the standalone origin-subtitle button in `panel.subtitle.list` with a unified Create Sub selector/action, reused existing origin subtitles to skip detect work, added cached-origin user copy, calculated OpenAI translation credits by subtitle text word count, calculated Whisper detect credits by configured per-minute rate with floored duration minutes, added admin OpenAI and Whisper credit inputs, and split Whisper config persistence into `server/repositories/whisper_config_repository.py` to keep repository files under the 400-line guardrail. Validation passed with `python -m compileall server`, desktop `npm run lint` and `npm run build`, admin `npm run lint` and `npm run build`, VS Code Problems clean, `docker compose up -d --build web`, in-container backend health `{"status":"ok"}`, runtime credit probes, and `npm run desktop:launch`; host `127.0.0.1:5000` health was unavailable because the current compose state does not publish the web port to the host.
- [x] Remove voiceover process modal polling hint - Dropped the voiceover processing-modal hint text from `frontend/src/App.jsx` so the full-screen blocking overlay no longer shows the “polling every 0.5 seconds” copy while narration is running. Validation passed with `npm run lint`, `npm run build`, and a clean VS Code Problems check for the touched file.
- [x] Align Vbee credit estimate with cross-project cache reuse - Fixed a backend mismatch where Vbee credit estimation only counted active asset rows, while real voiceover creation could still reuse cache by backfilling from recent completed segments. Added a reusable-cache predicate in `server/services/vbee_asset_service.py`, pointed `server/services/vbee_credit_service.py` at that shared rule, and kept the create path on the same backfill eligibility logic so cross-project reused segments can reduce quoted credits before generation starts. Validation passed with `python -m compileall server`, backend health `{"status":"ok"}` after `docker compose up -d --build web`, VS Code Problems clean for the touched files, a live runtime probe showing `REUSABLE True`, and a live estimate sample returning `cachedSegments=1` plus `uncachedSegments=0` for a known completed cached segment.
- [x] Lock the editor during voiceover generation and base progress on downloaded files - Extracted the full-screen processing-lock portal into `frontend/src/components/AppShell/ProcessingLockModal.jsx`, reused it from `frontend/src/App.jsx` so the editor now blocks interaction while Vbee narration generation is running, and switched the displayed narration progress in `frontend/src/utils/voiceoverUtils.js` to update from downloaded completed audio files instead of the backend status percentage bands. Validation passed with `npm run lint`, `npm run build`, VS Code Problems clean for the touched frontend files, and a desktop relaunch through `npm run desktop:launch`; Electron stayed up, and one earlier relaunch showed the existing Windows disk-cache access warning without stopping the app.
- [x] Add dynamic Vbee voiceover credit estimates - Added server-side Vbee credit estimation that normalizes subtitles, compares cached versus uncached Vbee segment characters, applies configurable float per-character rates, rounds the final charge up to an integer, exposes authenticated `/api/voiceover/estimate`, and charges `/api/voiceover/start` with the same estimate. Added admin Vbee config inputs for `creditPerCharacter` and `cachedCreditPerCharacter`, wired the desktop subtitle panel to debounce server estimates after creating or editing subtitles, and updated the voiceover button to show the live estimated credit cost. Validation passed with `python -m compileall server`, `npm run lint` and `npm run build` in `frontend/`, `npm run lint` and `npm run build` in `admin-frontend/`, VS Code Problems clean for touched files, `docker compose up -d --build web`, backend health `{"status":"ok"}`, direct in-container config and estimate probes, public config route verification, estimate route registration check, and relaunching the desktop app with `npm run desktop:launch`.
- [x] Fix backend Vbee voiceover request creation against live segment-id schema - Diagnosed `/api/voiceover/start` returning `503 {"error":"Vbee storage is unavailable"}` to a real backend mismatch: the live `vbee_voice_segments` table uses string IDs (`VARCHAR(36)`) and requires callers to provide `id`, while the current Python path still coerced segment IDs to integers and inserted new rows without an `id`. Updated the Vbee request/schema helpers to generate UUID segment IDs for new inserts, treat persisted segment IDs as strings when reading or updating queued and processing rows, and keep Vbee summary/detail payloads aligned with the live schema. Validation passed by rebuilding the `web` container with `docker compose up -d --build web`, rerunning the previously failing in-container checks so queued UUID-backed segments loaded successfully and a temporary Vbee request insert or cleanup completed successfully, running `python -m compileall server`, and confirming `audioedit-web-1` remained healthy with `{"status":"ok"}` from `http://127.0.0.1:5000/api/health`.
- [x] Add bash desktop build script in `build/` - Created `build/build-desktop.sh`, a Git Bash-compatible script that builds the Vite renderer, stops any running `build/win-unpacked/Audio Edit.exe` instance that would lock the executable, generates a temporary Electron Builder config, packages the Windows x64 NSIS desktop app back into the root `build/` folder, prints the generated installer and unpacked executable sizes, and removes the temporary config on exit. Updated `.gitignore` so build artifacts stay ignored while `build/build-desktop.sh` remains visible. Validation passed by running `C:\Program Files\Git\bin\bash.exe build/build-desktop.sh`, which regenerated `build/Audio Edit-Setup-0.0.0.exe` and `build/win-unpacked/Audio Edit.exe` successfully.
- [x] Audit packaged project storage paths to use the sibling `projects/` folder only - Reviewed the Electron desktop project-store stack and tightened `frontend/electron/projectStoreShared.mjs` so packaged builds now both write and resolve project data strictly from a `projects/` folder beside `Audio Edit.exe`, instead of falling back to `%APPDATA%/frontend/projects`. This keeps project metadata, videos, subtitles, scene grids, voiceovers, and cached voiceover segments bound to the installed app folder the user is actually running. Validation passed with `npm run build`, `npm run lint`, a rebuilt Windows package via `electron-builder`, and a packaged benchmark run from `build/win-unpacked/Audio Edit.exe` succeeding in `21598ms` after staging project `7672282a-6975-4fda-a884-57360cd09808` only under `build/win-unpacked/projects/` while `%APPDATA%/frontend/projects/7672282a-6975-4fda-a884-57360cd09808` was absent.
- [x] Fix packaged desktop export failing with `Failed during loadedmetadata` - Updated `frontend/electron/export/nativeFfmpeg.mjs` so packaged Electron builds prefer the bundled FFmpeg executable from `resources/app.asar.unpacked/node_modules/ffmpeg-static/ffmpeg.exe` instead of the non-runnable `app.asar` path returned by `ffmpeg-static`. This keeps packaged `.exe` exports on the native backend instead of falling back into the renderer-side FFmpeg.wasm path that was producing a broken `cut.mp4` and then failing at `loadedmetadata`. Validation passed with VS Code file errors clean for the touched module, `npm run build`, `npm run lint`, a rebuilt Windows package via `electron-builder`, and the packaged benchmark project `7672282a-6975-4fda-a884-57360cd09808` passing from `build/win-unpacked/Audio Edit.exe` in `22804ms` with native diagnostics `encoder: nvidia-nvenc`, `frameRate: 60`, and `sourceFrameRate: 24`.
- [x] Build desktop EXE into root build folder - Built the Vite renderer and packaged the Electron desktop app with a temporary `electron-builder` config targeting Windows x64 NSIS output under the root `build/` folder. The first packaging attempt hit a local Windows symlink privilege error while extracting `winCodeSign`; retrying with executable signing/editing disabled and code-signing auto-discovery off completed successfully. Verified `build/Audio Edit-Setup-0.0.0.exe` (`110282814` bytes) and `build/win-unpacked/Audio Edit.exe` (`202690560` bytes) exist. Notes: the installer is unsigned and uses Electron's default icon because no app icon/signing certificate is configured.
- [x] Add selectable export FPS - Added 30/60fps selectors to `panel.export.content` and `dashboard.series.<series>.export.modal`, routed the selected FPS through editor export, series export, native Electron export, and fallback canvas recording, and split reusable helpers from `frontend/src/hooks/useFrameExport.js` into `frontend/src/hooks/frameExportModel.js` so the touched hook is back under the 400-line guardrail. Validation passed with `npm run lint`, `npm run build`, native FFmpeg filter probes for both 30fps and 60fps, and the benchmark project `7672282a-6975-4fda-a884-57360cd09808` passing default 60fps export at `23390ms`; FFmpeg metadata confirmed the benchmark MP4 stream as `60 fps, 60 tbr`. Backend restarted with `docker compose up -d`, and the desktop app was launched with `npm run desktop:launch`.
- [x] Export videos at smooth 60fps - Forced the native export cadence to 60fps instead of inheriting the lower source-video FPS, added source-FPS diagnostics, normalized the fast NVENC filter path to `fps=60` before timeline `setpts` so source duration remains stable, and made the hidden benchmark assert 60fps diagnostics. Validation passed with a one-second FFmpeg filter probe reporting `60 fps`, `npm run lint`, `npm run build`, the full benchmark project `7672282a-6975-4fda-a884-57360cd09808` passing at `24317ms` with diagnostics `frameRate: 60` and `sourceFrameRate: 24`, and FFmpeg metadata confirming the MP4 stream as `60 fps, 60 tbr`. Backend restarted with `docker compose up -d`, and the desktop app was launched with `npm run desktop:launch`.
- [x] Hide watermark for premium users in dashboard series preview and export - Propagated the premium watermark-hide flag from `frontend/src/App.jsx` into `frontend/src/components/ProjectDashboard/ProjectDashboard.jsx`, `frontend/src/components/ProjectDashboard/SeriesDetailView.jsx`, `frontend/src/hooks/useSeriesExport.js`, and `frontend/src/utils/seriesExportPipeline.js` so the dashboard series player and joined series export now reuse the same premium watermark behavior as the editor path. Validation passed with `npm run lint`, `npm run build`, and VS Code Problems showing no errors in the touched files; backend restarted with `docker compose up -d`, and the desktop app was launched with `npm run desktop:launch`.
- [x] Split oversized SeriesDetailView - Extracted the embedded series watch-player into `frontend/src/components/ProjectDashboard/SeriesVideoPlayer.jsx` so `frontend/src/components/ProjectDashboard/SeriesDetailView.jsx` dropped back under the 400-line guardrail (`142` lines) in the same session. Validation passed with `npm run lint`, `npm run build`, and VS Code Problems showing no errors in the split files.
- [x] Smooth native export scene zoom further - Increased the native fast `zoompan` foreground supersample from 2x to fast-bilinear 3x for small fitted videos in `frontend/electron/export/frameMotionFilter.mjs`, reducing remaining crop-rounding jitter while preserving the full native export path. Validation passed with FFmpeg filter probe, `npm run lint`, `npm run build`, and `npm run benchmark:export -- --project-id=7672282a-6975-4fda-a884-57360cd09808 --max-ms=15000` at `14222ms`; VS Code Problems reported no errors in the touched export file. Backend restarted with `docker compose up -d`, and the desktop app was launched with `npm run desktop:launch`.
- [x] Stabilize native export scene zoom - Updated `frontend/electron/export/frameMotionFilter.mjs` so the native fast `zoompan` foreground path supersamples smaller fitted foreground videos before animated zoom/crop, reducing visible pixel-rounding jitter in exported zoom scenes while preserving the full native fast path. Validation passed with FFmpeg filter probe, `npm run lint`, `npm run build`, and `npm run benchmark:export -- --project-id=7672282a-6975-4fda-a884-57360cd09808 --max-ms=15000` at `12311ms`; VS Code Problems reported no errors in the touched export files. Backend restarted with `docker compose up -d`, and the desktop app was launched with `npm run desktop:launch`.
- [x] Restore full-effects native export under 15s for benchmark project - Added a hidden Electron export benchmark runner for project `7672282a-6975-4fda-a884-57360cd09808`, surfaced native export diagnostics, restored scene-motion zoom parity through the native fast path, and optimized the full-effect NVENC filter path by using fixed-size zoompan foreground motion, low-resolution video-fade background work before upscale, and 10-second bounded scene chunks. Validation passed with `npm run lint`, `npm run build`, and `npm run benchmark:export -- --project-id=7672282a-6975-4fda-a884-57360cd09808 --max-ms=15000` at `12448ms`; backend restarted with `docker compose up -d`, and the desktop app was launched with `npm run desktop:launch`.
- [x] Lock the editor with a loading modal during scene detection - Updated `frontend/src/App.jsx` so `editor.isDetecting` now opens a full-screen blocking modal above the editor with progress feedback and a developer locator marker, plus a capture-phase keydown guard so keyboard actions are also blocked while scene splitting is running and an app-level latch so the modal stays visible briefly even when detection completes quickly. Switched that detecting overlay onto the same full-window topmost layer pattern already used by export modal so it behaves like a real modal instead of rendering inside page flow. Updated `frontend/src/App.css` with the modal backdrop and dialog styling so users cannot interact with any editor controls until detection finishes, then updated `frontend/src/utils/sceneDetection.js` to yield across two animation frames before analysis starts so the modal reliably appears before video processing begins.
- [x] Add series export feature to dashboard.series.detail - Added Export Series button in the series detail header, created `frontend/src/utils/seriesExportPipeline.js` to load each episode's stored data and export sequentially then concat, created `frontend/src/hooks/useSeriesExport.js` for config/progress/result state, created `frontend/src/components/ProjectDashboard/SeriesExportModal.jsx` for the modal UI with quality/filename/folder config, progress tracking, and result display. Updated `frontend/src/components/ProjectDashboard/SeriesDetailView.jsx` to wire the button and modal, added CSS to `ProjectDashboard.css`, and added i18n keys in both EN and VI.
- [x] Keep dashboard series detail context after opening and backing from editor - Updated `frontend/src/App.jsx` to store selected dashboard series/episode context and pass it back into the dashboard view, then updated `frontend/src/components/ProjectDashboard/ProjectDashboard.jsx` to sync that context while opening series, selecting episodes, entering editor, and returning, so back from project now restores the same `dashboard.series.<name>.detail` state instead of jumping to standalone project list.

## Notes
- [x] Added `server/build_and_push.sh` — script to build the server Docker image and push to a registry. See [server/build_and_push.sh](server/build_and_push.sh)
