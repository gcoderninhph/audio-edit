# TASK

## Active
- None.
## Backlog
- None.
## Completed
- [x] Group Whisper node row actions into a button group - Updated `admin-frontend/src/components/WhisperNodesPanel.jsx` so the `Edit` and `Delete` controls in the Actions column now sit inside the shared `.button-group` pattern instead of rendering as separate loose buttons. Validation passed with no diagnostics from `get_errors` on the touched panel, clean `npm run lint` and `npm run build` in `admin-frontend/`, and `docker compose up -d --build web` to publish the updated admin UI.
- [x] Add Whisper node delete flow in admin - Extended the Whisper admin backend with processing-node delete SQL, store validation that blocks deletion while a node still has in-flight requests, and a new `DELETE /api/admin/services/whisper/nodes/<id>` route. Updated the admin Whisper nodes panel with a `Delete` action, client-side confirmation, disabled state while a node is processing requests, and list refresh after deletion. Validation passed with `python -m py_compile server/repositories/whisper_admin_repository.py server/services/whisper_admin_store.py server/controllers/whisper_routes.py`, an in-container probe returning `{"deletedFree":{"deleted":true,"id":14},"busyDeleteError":"Cannot delete the node while it is processing requests."}`, clean `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, and `docker compose up -d --build web` with `docker compose ps web` showing `healthy`.
- [x] Fix Whisper node assignment when a preferred request hits a full node - Updated `server/repositories/whisper_admin_repository.py` so queued-request reads can prioritize a specific `preferred_request_id` inside the SQL `ORDER BY`, and updated `server/services/whisper_runtime.py` to pass that preferred request directly into the queue query instead of only sorting the limited in-memory batch. This fixes the case where `dispatch_queued_whisper_requests(limit=1, preferred_request_id=...)` could ignore the just-created request when an older queued row already existed, leaving the new request in `queued` even though another Whisper node still had free capacity. Validation passed with `python -m py_compile server/repositories/whisper_admin_repository.py server/services/whisper_runtime.py`, `python -m compileall server`, `docker compose up -d --build web`, `docker compose ps web` showing `healthy`, and an in-container probe returning `{"claimed":[{"requestId":"probe-queue-...-2","node":"http://idle-node"}],"final":{"probe-queue-...-0":"queued","probe-queue-...-1":"queued","probe-queue-...-2":"processing"}}` for the previously failing scenario.
- [x] Fix Whisper queue-file cleanup leaks and add a safe cleanup command - Updated `server/services/whisper_runtime.py` so the sync-success path now deletes the original queued temp file before the stored `tempFilePath` is cleared, added `server/scripts/cleanup_whisper_queue.py` to classify `server/uploads/whisper-queue` entries against live Whisper request records, and added the root wrapper command `cleanup_whisper_queue.ps1` so Windows operators can run a dry-run preview or an applied cleanup through the `web` container. Validation passed with `python -m py_compile server/services/whisper_runtime.py`, `python -m py_compile server/scripts/cleanup_whisper_queue.py`, `docker compose up -d --build web`, an in-container probe returning `{"syncSubmitStatus":"success","syncQueueDirExistsAfterSubmit":false}` while keeping a live queued request but deleting completed or orphaned queue dirs, `./cleanup_whisper_queue.ps1 -Json` returning an empty dry-run after cleanup, `docker compose ps web` showing `healthy`, and the host `backend_uploads/whisper-queue` directory reporting `count = 0` after the safe cleanup removed 28 stale queue directories.
- [x] Add a second Whisper runtime service to Docker Compose - Extended `docker-compose.yml` with a second `pluja/whishper` service named `whishper-2`, mapped it to host port `8001`, and gave it separate upload and log bind mounts under `whishper_data/uploads-node-2` and `whishper_data/logs-node-2` so it can run alongside the original node. Added an internal `translate` service because the Whisper image hardcodes `translate:5000` in nginx, which both Whisper nodes need in order to serve HTTP traffic cleanly. Validation passed with `docker compose config --quiet`, `docker compose up -d --build`, `docker compose ps translate whishper whishper-2` showing all three services up, `Invoke-WebRequest http://127.0.0.1:8001` returning `STATUS=200`, and `docker compose exec -T whishper-2 sh -lc "nginx -t 2>&1"` reporting the nginx config is successful.
- [x] Add Whisper node names and inline edit flow - Extended the Service/Whisper node model with a persisted `name`, backfilled existing unnamed rows from `node_url`, added backend node-update support, and updated the admin Whisper nodes panel so the modal now supports both add and edit flows with `name`, `url`, and `maxConcurrentRequests` fields plus an `Edit` action per row. Validation passed with `python -m py_compile server/repositories/whisper_admin_repository.py server/services/whisper_admin_store.py server/controllers/whisper_routes.py`, clean `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build web`, `docker compose ps web` showing `healthy`, and an in-container create or update probe returning `{"created":{"name":"Probe Whisper Node","maxConcurrentRequests":2},"updated":{"name":"Updated Probe Whisper Node","maxConcurrentRequests":4},"listedAfterUpdate":{"name":"Updated Probe Whisper Node","maxConcurrentRequests":4}}` for a temporary node that was cleaned up afterward.
- [x] Split Whisper runtime status helpers under the 400-line guardrail - Extracted Whisper runtime status constants and normalization helpers into the new `server/services/whisper_runtime_status.py` module so `server/services/whisper_runtime.py` stays focused on queue dispatch orchestration and remains under the workspace 400-line limit after the dispatch fixes. Validation passed with focused `python -m py_compile server/services/whisper_runtime.py server/services/whisper_runtime_status.py` plus the live concurrency probe recorded below.
- [x] Stop false Whisper refresh during in-flight dispatch - Updated `server/services/whisper_runtime.py` so processing rows without a real `providerRequestId` no longer poll `/api/transcriptions/<local-request-id>` while the outbound sync dispatch is still in flight. This prevents a bogus `Whisper provider job was not found.` failure on the first request and removes the blocking refresh that was keeping the next request queued even though the node still had free concurrency slots. Validation passed with `python -m py_compile server/services/whisper_runtime.py`, no diagnostics from `get_errors`, `docker compose up -d --build web`, and an in-container probe returning `{"providerGetCallsDuringDispatch":0,"nodeProcessingCountDuringDispatch":2,"nodeAvailableCapacityDuringDispatch":1,"requestStatusesDuringDispatch":{"probe-dispatching-a-1779534351109":"processing","probe-dispatching-b-1779534351110":"processing"},"requestNodeUrlsDuringDispatch":{"probe-dispatching-a-1779534351109":"http://probe-whisper-dispatching.local","probe-dispatching-b-1779534351110":"http://probe-whisper-dispatching.local"},"finalStatuses":{"probe-dispatching-a-1779534351109":"success","probe-dispatching-b-1779534351110":"success"}}`.
- [x] Allow concurrent Whisper dispatches to fill node capacity - Updated `server/services/whisper_runtime.py` so the named dispatch lock now only protects the queue-claim step that marks queued rows as `processing` and assigns `nodeBaseUrl`, while the outbound Whisper POST runs after the lock is released. This prevents simultaneous transcription requests from serializing behind one in-flight network call and lets one node use its full `maxConcurrentRequests` capacity. Validation passed with `python -m py_compile server/services/whisper_runtime.py`, `docker compose up -d --build web`, an in-container concurrent probe against the real configured node returning `{"processingCountDuringDispatch":2,"deltaProcessingCount":2,"requestNodeUrls":{"probe-real-a-1779533986085":"http://whishper:8000","probe-real-b-1779533986086":"http://whishper:8000"},"requestStatuses":{"probe-real-a-1779533986085":"processing","probe-real-b-1779533986086":"processing"}}`, and no diagnostics from `get_errors` on the touched runtime file.
- [x] Count in-flight Whisper node dispatches as processing - Fixed `server/services/whisper_runtime.py` so a queued Whisper request now flips to `processing` before the outbound POST to the selected node begins, instead of only after an async provider job id is returned. This makes `admin.react.service.whisper.nodes` show `processingCount = 1` while a sync Whisper dispatch is still in flight, rather than jumping directly from `queued` to `success`. Validation passed with focused `python -m py_compile server/services/whisper_runtime.py`, `docker compose up -d --build web`, a live container-side inspection showing recent Whisper rows previously went `queued -> success` with no persisted processing window, and an in-container concurrent probe returning `{"processingCountDuringDispatch":1,"availableCapacityDuringDispatch":2,"finalStatus":"success","finalNodeBaseUrl":"http://probe-whisper-processing-count.local"}` while the fake outbound Whisper POST was held open.
- [x] Add Whisper queue capacity and admin concurrency controls - Restyled the Service/Whisper request table to match the compact Vbee token layout, moved the Whisper node-create flow into a modal with `maxConcurrentRequests`, extended Whisper node storage with per-node concurrency limits, and added a queue-backed Whisper runtime that stores uploads temporarily on disk, keeps requests queued while nodes are full, and dispatches the saved file automatically when capacity becomes available. Validation passed with focused `python -m py_compile server/app.py server/controllers/proxy_transcription_routes.py server/services/whisper_runtime.py server/services/whisper_admin_store.py server/repositories/whisper_admin_repository.py`, clean `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, `docker compose up -d --build`, an in-container admin API probe returning `401` from `/api/admin/services/whisper/nodes`, an in-container SPA probe returning `200` for `/admin/service/whisper/nodes`, and an in-container queue probe returning `{'firstState': 'queued', 'firstQueuePosition': 1, 'secondState': 'success', 'secondStatusCode': 2, 'secondProviderRequestId': '', 'secondNodeBaseUrl': 'http://whishper:8000', 'tempFileStillTracked': False}` after forcing zero capacity and then reopening capacity.