# TASK

## Active
- None.
## Backlog
- Investigate or replace single-node Whisper provider concurrency - Current `pluja/whishper` runtime does not appear to provide true intra-node parallel transcription on the CPU profile. Direct internal probes to `http://whishper:8000/transcribe/` returned two simultaneous identical requests in about `3009ms` and `5826ms`, showing the second request effectively waits behind the first inside the provider. Container inspection showed `/bin/whishper` exposes no worker or prefork flags, Fiber reports `Processes = 1` with `Prefork = Disabled`, `transcription/main.py` starts `uvicorn.run(...)` with the default single worker, and `transcription/transcribe.py` loads a fresh `FasterWhisperBackend` model on every request before transcribing. Decide whether to cap each physical Whisper node to `maxConcurrentRequests = 1`, scale out with more node containers, or build a custom Whisper image that reuses loaded models and runs multiple ASR workers.
## Completed
- [x] Keep dashboard series detail context after opening and backing from editor - Updated `frontend/src/App.jsx` to store selected dashboard series/episode context and pass it back into the dashboard view, then updated `frontend/src/components/ProjectDashboard/ProjectDashboard.jsx` to sync that context while opening series, selecting episodes, entering editor, and returning, so back from project now restores the same `dashboard.series.<name>.detail` state instead of jumping to standalone project list.
- [x] Add narration volume range in panel video-player transport row - Updated `frontend/src/components/VideoPlayer/VideoPlayerTransportControls.jsx` and `frontend/src/components/VideoPlayer/VideoPlayer.jsx` so `panel.video-player` now shows an additional `VO` volume range in the bottom transport controls whenever a voiceover track exists, using the same `voiceoverVolume` state and handlers as the player audio mix logic. Updated `frontend/src/components/VideoPlayer/VideoPlayer.css` for grouped source/voiceover slider layout.
- [x] Apply shared video/voiceover volume adjustment logic to dashboard player parity - Extended `frontend/src/utils/videoDisplayLogic.js` with shared preview audio-mix helpers (`resolvePreviewAudioMix`, volume clamp/apply, and mute-toggle), updated `frontend/src/components/VideoPlayer/VideoPlayer.jsx` and `frontend/src/components/VideoPlayer/useVideoPlayerVoiceover.js` to use the shared volume application path, and updated `frontend/src/components/ProjectDashboard/SeriesDetailView.jsx` to restore persisted `export_audio_mix`, apply video plus narration volumes to preview playback, and expose dashboard-side volume controls so `dashboard.series.<name>.player` matches `panel.video-player` for video/narration volume behavior.
- [x] Align dashboard series player preview effects with panel video player - Updated `frontend/src/components/ProjectDashboard/SeriesDetailView.jsx` so `dashboard.series.<name>.player` no longer renders a raw `<video>` preview and now reuses `VideoPlayerPreviewStage` with restored per-project preview settings from local storage (`frame_preset_id`, `frame_background`, `subtitle_settings`, active subtitle track, scene/deleted-scene context, and saved voiceover audio). This brings frame/background/subtitle/narration display effects in the dashboard player in line with `panel.video-player` while keeping shared transport logic from `frontend/src/utils/videoDisplayLogic.js`.
- [x] Extract shared playback display logic for panel.video-player and dashboard series player - Added `frontend/src/utils/videoDisplayLogic.js` to centralize transport display logic (`formatPlaybackTimestamp`, `getPlaybackProgress`, pointer seek mapping, and media toggle behavior). Updated `frontend/src/components/VideoPlayer/VideoPlayer.jsx` and `frontend/src/components/ProjectDashboard/SeriesDetailView.jsx` to reuse this shared logic so playback display behavior stays consistent between `panel.video-player` and `dashboard.series.<name>.player`.
- [x] Save scene-grid image per project and use it for scene card thumbnails - Added project-scoped `scene-grid.png` persistence in `frontend/electron/projectStore.mjs` with IPC bridge exposure through `frontend/electron/preload.mjs` and renderer wrappers in `frontend/src/utils/projectStorage.js`. Extended desktop media protocol in `frontend/electron/projectMediaProtocol.mjs` to serve `desktop://app/project-media/<projectId>/scene-grid.png`. Updated `frontend/src/hooks/useVideoEditor.js` to build/save scene-grid after scene detection and hydrate `scene.card.xx` thumbnails by `thumbnailIndex` from the saved grid via new helper `frontend/src/utils/sceneGridThumbnails.js`; updated `frontend/src/components/SceneList/SceneList.css` and `frontend/src/components/SceneList/SceneList.jsx` behavior to keep the list filling remaining panel space and resolve thumbs by index first.
- [x] Make scene list fill remaining panel space and map thumbnails by cut-scene index - Updated `frontend/src/components/SceneList/SceneList.css` so `panel.scene-list` uses full-height flex layout and the scroll list consumes remaining vertical space instead of a fixed max height. Updated `frontend/src/hooks/useVideoEditor.js` to add `thumbnailIndex` metadata to detected scenes and store generated thumbnails by index (with id fallback), then updated `frontend/src/components/SceneList/SceneList.jsx` to resolve each card thumbnail by `thumbnailIndex` first so scene thumbs align with cut-scene order (matching `scene-grid` style indexing).
- [x] Simplify panel.scene-list controls and card actions - Updated `frontend/src/components/SceneList/SceneList.jsx` to remove the `Restore all`, `Delete all`, and `Re-detect` buttons from the panel UI, removed the per-card play button, and grouped only `config` plus `delete` actions into one card action group. Updated thumbnail click behavior to seek and auto-play the selected scene via `frontend/src/hooks/useEditorSceneListActions.js` optional play mode, and removed now-unused restore/delete prop wiring from `frontend/src/components/AppShell/AppEditorWorkspace.jsx`. Adjusted grouped action styling in `frontend/src/components/SceneList/SceneList.css`. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and desktop relaunch via `npm run desktop:launch`.
- [x] Remove scene-list sensitivity controls and hard-fix detection at 0.5 - Updated `frontend/src/components/SceneList/SceneList.jsx` to remove the sensitivity slider from `panel.scene-list.setup` and from the detected-scenes header area while keeping detect/re-detect actions. Updated `frontend/src/hooks/useVideoEditor.js` so `detectScenes(...)` always runs with fixed `sensitivity: 0.5` regardless of persisted state. Updated `frontend/src/components/AppShell/AppEditorWorkspace.jsx` to stop wiring sensitivity props into `SceneList`. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and desktop relaunch via `npm run desktop:launch`.
- [x] Correct panel height matching to use exact video-player height - Refined `frontend/src/components/AppShell/AppEditorWorkspace.jsx` so right-side panel height now matches `panel.video-player` directly (no subtraction for right tabs), ensuring `panel.subtitle.list` reaches the same panel height target requested in the editor layout. Validation passed with `npm run lint` and `npm run build`.
- [x] Match panel.video-player and panel.subtitle.list heights - Updated `frontend/src/components/AppShell/AppEditorWorkspace.jsx` to observe the rendered `panel.video-player` and right-tab header heights, then apply a synchronized desktop height to the right content panel so `panel.subtitle.list` (and scenes tab panel) stays equal in height with the video player panel. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and desktop relaunch via `npm run desktop:launch`.
- [x] Lock all close paths while export is running - Updated `frontend/src/components/AppShell/AppEditorWorkspace.jsx` to disable export-modal close interactions (close button, backdrop click, Escape) whenever `editor.isExporting` is true and to sync export-running state to Electron through `windowGuard.setExportRunning`. Updated `frontend/src/components/AppShell/AppEditorHeaderStatus.jsx` to disable the back-to-project button during export. Added window-guard bridge support in `frontend/electron/preload.mjs` and enforced BrowserWindow close blocking in `frontend/electron/main.mjs` so users cannot close the app window until export finishes. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and desktop relaunch via `npm run desktop:launch`.
- [x] Move panel.export into modal and add launcher in current-source area - Updated `frontend/src/components/AppShell/AppEditorWorkspace.jsx` to remove the inline `panel.export` grid row, add an export-launch button inside `editor.video.current-source`, and render `ExportPanel` inside a portal modal with backdrop click and Escape-to-close behavior. Added `frontend/src/components/AppShell/AppEditorWorkspace.css` for modal and launcher styling and updated `MAP.md` to reflect the new responsibilities. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and desktop relaunch via `npm run desktop:launch`.
- [x] Fix subtitle panel auto-scroll affecting main editor scroll while playback - Updated `frontend/src/components/SubtitlePanel/SubtitlePanel.jsx` to stop using `scrollIntoView` for active subtitle tracking and instead scroll only within the local subtitle list container via `scrollTop` adjustments, then added `overscroll-behavior: contain` to `frontend/src/components/SubtitlePanel/SubtitlePanel.css` so wheel scrolling in `panel.subtitle.list` no longer chains into outer editor scrolling. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and desktop relaunch via `npm run desktop:launch`.
- [x] Localize export-quality profile labels in the sidebar config panel - Updated `frontend/src/utils/exportQualityProfile.js` to include i18n key metadata (`labelKey`, `helperKey`) per quality profile, added Vietnamese and English quality-profile translations in `frontend/src/i18n/i18nConfig.js`, and switched `frontend/src/components/VideoPlayer/VideoPlayerExportControls.jsx` plus `frontend/src/components/ExportPanel/ExportPanel.jsx` to render localized profile names and helper text instead of hardcoded English labels. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and desktop relaunch via `npm run desktop:launch`.
- [x] Extend desktop panel i18n for player, subtitles, timeline, and export - Continued desktop localization by adding translation keys in `frontend/src/i18n/i18nConfig.js` for `panel.video-player`, `panel.subtitle.list`, `panel.timeline`, `panel.export`, `panel.video-player.sidebar`, and `panel.video-player.frame-controls`, then wiring those keys through `useI18n` in the related components under `frontend/src/components/VideoPlayer/`, `frontend/src/components/SubtitlePanel/`, `frontend/src/components/Timeline/`, `frontend/src/components/ExportPanel/`, and `frontend/src/components/SceneList/SceneBulkMotionConfig.jsx`. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and a desktop relaunch via `npm run desktop:launch`.
- [x] Normalize Vietnamese diacritics in desktop i18n strings - Updated the Vietnamese dictionary in `frontend/src/i18n/i18nConfig.js` so user-facing labels, alerts, auth text, dashboard text, and editor tab/status strings use proper Vietnamese diacritics (for example `Dự án`, `Đăng nhập`, `Phụ đề`, `Hủy`) while preserving all translation keys and placeholders. Validation passed with `npm run lint`, `npm run build`, `docker compose up -d`, and a desktop relaunch via `npm run desktop:launch`.
- [x] Add desktop language switcher and shell i18n - Added a lightweight renderer i18n stack (`frontend/src/i18n/i18nConfig.js`, `frontend/src/i18n/i18nContext.js`, `frontend/src/i18n/I18nProvider.jsx`, `frontend/src/i18n/useI18n.js`) with localStorage locale persistence, integrated a globe language switcher into `frontend/src/components/AppShell/AppHeader.jsx` via `frontend/src/components/AppShell/HeaderLanguageSwitcher.jsx`, and localized the visible desktop shell text in app header/status, auth header/dialog, and dashboard/series surfaces. Also extracted `frontend/src/components/AppShell/AppEditorHeaderStatus.jsx` so `frontend/src/App.jsx` stays under the 400-line guardrail while wiring localized editor status controls. Validation passed with `npm run lint`, `npm run build`, and clean diagnostics from `get_errors` on touched files.
- [x] Add per-subtitle delete action in the desktop editor - Added an icon-only delete control to each subtitle card so users can remove individual subtitle lines such as `subtitle.card.trans_27`, routed the action through the existing subtitle-track editor state for undo and autosave compatibility, and split the subtitle card list into the new `frontend/src/components/SubtitlePanel/SubtitleCardList.jsx` module so `frontend/src/components/SubtitlePanel/SubtitlePanel.jsx` now stays under the workspace 400-line guardrail. Validation passed with clean targeted editor diagnostics, `npm run lint`, `npm run build`, `docker compose up -d`, and a desktop relaunch through `npm run desktop:start`; Electron still logged a Windows disk-cache access warning during launch, but the renderer build completed successfully and the app process remained running.
- [x] Add 18 more Whisper runtime containers - Refactored the shared `pluja/whishper` service settings in `docker-compose.yml` into reusable YAML anchors, kept the existing `whishper` and `whishper-2` services on that shared base, and added `whishper-3` through `whishper-20` with separate upload and log bind mounts under `whishper_data/uploads-node-N` and `whishper_data/logs-node-N` while reusing the shared model directory. Validation passed with `docker compose config --quiet`, `docker compose up -d --build`, `docker compose ps` showing `whishper` through `whishper-20` up, `docker compose ps web whishper-20` showing `web` healthy and `whishper-20` up, and `docker compose exec -T whishper-20 sh -lc "nginx -t 2>&1"` reporting the nginx config is successful.
- [x] Diagnose single-node Whisper pseudo-concurrency - Verified that the current `pluja/whishper` CPU node does not provide true intra-node parallel transcription even when the backend dispatches requests concurrently. Direct internal probes to `http://whishper:8000/transcribe/` returned two simultaneous identical requests in about `3009ms` and `5826ms`, confirming the second request waits inside the provider. Container inspection also showed `/bin/whishper` exposes no worker or prefork flags, Fiber reports `Processes = 1` with `Prefork = Disabled`, `transcription/main.py` starts `uvicorn.run(...)` with the default single worker, and `transcription/transcribe.py` loads a fresh `FasterWhisperBackend` model on every request before transcribing.
- [x] Add Whisper node delete flow in admin - Extended the Whisper admin backend with processing-node delete SQL, store validation that blocks deletion while a node still has in-flight requests, and a new `DELETE /api/admin/services/whisper/nodes/<id>` route. Updated the admin Whisper nodes panel with a `Delete` action, client-side confirmation, disabled state while a node is processing requests, and list refresh after deletion. Validation passed with `python -m py_compile server/repositories/whisper_admin_repository.py server/services/whisper_admin_store.py server/controllers/whisper_routes.py`, an in-container probe returning `{"deletedFree":{"deleted":true,"id":14},"busyDeleteError":"Cannot delete the node while it is processing requests."}`, clean `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, and `docker compose up -d --build web` with `docker compose ps web` showing `healthy`.
- [x] Fix Whisper node assignment when a preferred request hits a full node - Updated `server/repositories/whisper_admin_repository.py` so queued-request reads can prioritize a specific `preferred_request_id` inside the SQL `ORDER BY`, and updated `server/services/whisper_runtime.py` to pass that preferred request directly into the queue query instead of only sorting the limited in-memory batch. This fixes the case where `dispatch_queued_whisper_requests(limit=1, preferred_request_id=...)` could ignore the just-created request when an older queued row already existed, leaving the new request in `queued` even though another Whisper node still had free capacity. Validation passed with `python -m py_compile server/repositories/whisper_admin_repository.py server/services/whisper_runtime.py`, `python -m compileall server`, `docker compose up -d --build web`, `docker compose ps web` showing `healthy`, and an in-container probe returning `{"claimed":[{"requestId":"probe-queue-...-2","node":"http://idle-node"}],"final":{"probe-queue-...-0":"queued","probe-queue-...-1":"queued","probe-queue-...-2":"processing"}}` for the previously failing scenario.
- [x] Fix Whisper queue-file cleanup leaks and add a safe cleanup command - Updated `server/services/whisper_runtime.py` so the sync-success path now deletes the original queued temp file before the stored `tempFilePath` is cleared, added `server/scripts/cleanup_whisper_queue.py` to classify `server/uploads/whisper-queue` entries against live Whisper request records, and added the root wrapper command `cleanup_whisper_queue.ps1` so Windows operators can run a dry-run preview or an applied cleanup through the `web` container. Validation passed with `python -m py_compile server/services/whisper_runtime.py`, `python -m py_compile server/scripts/cleanup_whisper_queue.py`, `docker compose up -d --build web`, an in-container probe returning `{"syncSubmitStatus":"success","syncQueueDirExistsAfterSubmit":false}` while keeping a live queued request but deleting completed or orphaned queue dirs, `./cleanup_whisper_queue.ps1 -Json` returning an empty dry-run after cleanup, `docker compose ps web` showing `healthy`, and the host `backend_uploads/whisper-queue` directory reporting `count = 0` after the safe cleanup removed 28 stale queue directories.
- [x] Add a second Whisper runtime service to Docker Compose - Extended `docker-compose.yml` with a second `pluja/whishper` service named `whishper-2`, mapped it to host port `8001`, and gave it separate upload and log bind mounts under `whishper_data/uploads-node-2` and `whishper_data/logs-node-2` so it can run alongside the original node. Added an internal `translate` service because the Whisper image hardcodes `translate:5000` in nginx, which both Whisper nodes need in order to serve HTTP traffic cleanly. Validation passed with `docker compose config --quiet`, `docker compose up -d --build`, `docker compose ps translate whishper whishper-2` showing all three services up, `Invoke-WebRequest http://127.0.0.1:8001` returning `STATUS=200`, and `docker compose exec -T whishper-2 sh -lc "nginx -t 2>&1"` reporting the nginx config is successful.
- [x] Add Whisper node names and inline edit flow - Extended the Service/Whisper node model with a persisted `name`, backfilled existing unnamed rows from `node_url`, added backend node-update support, and updated the admin Whisper nodes panel so the modal now supports both add and edit flows with `name`, `url`, and `maxConcurrentRequests` fields plus an `Edit` action per row. Validation passed with `python -m py_compile server/repositories/whisper_admin_repository.py server/services/whisper_admin_store.py server/controllers/whisper_routes.py`, clean `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build web`, `docker compose ps web` showing `healthy`, and an in-container create or update probe returning `{"created":{"name":"Probe Whisper Node","maxConcurrentRequests":2},"updated":{"name":"Updated Probe Whisper Node","maxConcurrentRequests":4},"listedAfterUpdate":{"name":"Updated Probe Whisper Node","maxConcurrentRequests":4}}` for a temporary node that was cleaned up afterward.
- [x] Split Whisper runtime status helpers under the 400-line guardrail - Extracted Whisper runtime status constants and normalization helpers into the new `server/services/whisper_runtime_status.py` module so `server/services/whisper_runtime.py` stays focused on queue dispatch orchestration and remains under the workspace 400-line limit after the dispatch fixes. Validation passed with focused `python -m py_compile server/services/whisper_runtime.py server/services/whisper_runtime_status.py` plus the live concurrency probe recorded below.
- [x] Stop false Whisper refresh during in-flight dispatch - Updated `server/services/whisper_runtime.py` so processing rows without a real `providerRequestId` no longer poll `/api/transcriptions/<local-request-id>` while the outbound sync dispatch is still in flight. This prevents a bogus `Whisper provider job was not found.` failure on the first request and removes the blocking refresh that was keeping the next request queued even though the node still had free concurrency slots. Validation passed with `python -m py_compile server/services/whisper_runtime.py`, no diagnostics from `get_errors`, `docker compose up -d --build web`, and an in-container probe returning `{"providerGetCallsDuringDispatch":0,"nodeProcessingCountDuringDispatch":2,"nodeAvailableCapacityDuringDispatch":1,"requestStatusesDuringDispatch":{"probe-dispatching-a-1779534351109":"processing","probe-dispatching-b-1779534351110":"processing"},"requestNodeUrlsDuringDispatch":{"probe-dispatching-a-1779534351109":"http://probe-whisper-dispatching.local","probe-dispatching-b-1779534351110":"http://probe-whisper-dispatching.local"},"finalStatuses":{"probe-dispatching-a-1779534351109":"success","probe-dispatching-b-1779534351110":"success"}}`.
- [x] Allow concurrent Whisper dispatches to fill node capacity - Updated `server/services/whisper_runtime.py` so the named dispatch lock now only protects the queue-claim step that marks queued rows as `processing` and assigns `nodeBaseUrl`, while the outbound Whisper POST runs after the lock is released. This prevents simultaneous transcription requests from serializing behind one in-flight network call and lets one node use its full `maxConcurrentRequests` capacity. Validation passed with `python -m py_compile server/services/whisper_runtime.py`, `docker compose up -d --build web`, an in-container concurrent probe against the real configured node returning `{"processingCountDuringDispatch":2,"deltaProcessingCount":2,"requestNodeUrls":{"probe-real-a-1779533986085":"http://whishper:8000","probe-real-b-1779533986086":"http://whishper:8000"},"requestStatuses":{"probe-real-a-1779533986085":"processing","probe-real-b-1779533986086":"processing"}}`, and no diagnostics from `get_errors` on the touched runtime file.
- [x] Count in-flight Whisper node dispatches as processing - Fixed `server/services/whisper_runtime.py` so a queued Whisper request now flips to `processing` before the outbound POST to the selected node begins, instead of only after an async provider job id is returned. This makes `admin.react.service.whisper.nodes` show `processingCount = 1` while a sync Whisper dispatch is still in flight, rather than jumping directly from `queued` to `success`. Validation passed with focused `python -m py_compile server/services/whisper_runtime.py`, `docker compose up -d --build web`, a live container-side inspection showing recent Whisper rows previously went `queued -> success` with no persisted processing window, and an in-container concurrent probe returning `{"processingCountDuringDispatch":1,"availableCapacityDuringDispatch":2,"finalStatus":"success","finalNodeBaseUrl":"http://probe-whisper-processing-count.local"}` while the fake outbound Whisper POST was held open.
- [x] Add Whisper queue capacity and admin concurrency controls - Restyled the Service/Whisper request table to match the compact Vbee token layout, moved the Whisper node-create flow into a modal with `maxConcurrentRequests`, extended Whisper node storage with per-node concurrency limits, and added a queue-backed Whisper runtime that stores uploads temporarily on disk, keeps requests queued while nodes are full, and dispatches the saved file automatically when capacity becomes available. Validation passed with focused `python -m py_compile server/app.py server/controllers/proxy_transcription_routes.py server/services/whisper_runtime.py server/services/whisper_admin_store.py server/repositories/whisper_admin_repository.py`, clean `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, `docker compose up -d --build`, an in-container admin API probe returning `401` from `/api/admin/services/whisper/nodes`, an in-container SPA probe returning `200` for `/admin/service/whisper/nodes`, and an in-container queue probe returning `{'firstState': 'queued', 'firstQueuePosition': 1, 'secondState': 'success', 'secondStatusCode': 2, 'secondProviderRequestId': '', 'secondNodeBaseUrl': 'http://whishper:8000', 'tempFileStillTracked': False}` after forcing zero capacity and then reopening capacity.