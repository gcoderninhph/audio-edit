# TASK

## Active
## Backlog
## Completed
- [x] Remove SOURCE_FILE_NAME from OpenAI prompt flow - Removed the `<SOURCE_FILE_NAME>` placeholder and its related prompt-building logic from the Service/OpenAI config note, the OpenAI test prompt preview, and the backend OpenAI prompt construction so the prompt no longer mentions source-file text. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Compact OpenAI prompt preview box heights - Reduced the preview dialog box heights and capped the modal so the cURL, system prompt, and user prompt sections fit more comfortably within the viewport while staying clearly separated. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Split OpenAI prompt preview into three boxes - Reworked the OpenAI test prompt preview dialog so it now shows three separate sections for the cURL request, the system prompt, and the user prompt, matching the requested review format. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Move OpenAI prompt preview icon to the right - Moved the eye icon button in the OpenAI test result panel from the top-left corner to the top-right corner while keeping the prompt preview dialog behavior unchanged. Validation pending `npm run lint` and `npm run build` in `admin-frontend/` plus `docker compose up -d --build`.
- [x] Add OpenAI prompt preview corner icon - Added a single eye icon button in the top-left corner of the OpenAI test result panel that opens a prompt-preview dialog before sending the selected `.srt` file to OpenAI. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Center OpenAI test upload control - Reworked the OpenAI test tab file picker into a centered custom upload card with a balanced filename display and a tighter target-language column so the upload area aligns more cleanly with the rest of the form. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Add OpenAI admin test tab for `.srt` uploads - Added a Service/OpenAI `test` tab with `.srt` upload, target-language select, inline translated-result panel, and full developer markers on the OpenAI nav tabs; also added an admin-only backend endpoint for direct one-off OpenAI test translations without creating a long-lived job. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Add GPT-5 family options to OpenAI model picker - Extended the Service/OpenAI config model dropdown with `GPT-5`, `GPT-5 mini`, and `GPT-5 nano` so the admin panel can switch to the newer model family without manual typing. Validation passed with `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Add spacing around OpenAI config template note - Added a dedicated class for the OpenAI config prompt-template notice so that panel gets 10px of vertical spacing above and below without changing the spacing of other notices across the admin UI. Validation pending `npm run build` in `admin-frontend/` plus `docker compose up -d --build`.
- [x] Darken admin select controls for OpenAI config readability - Increased the admin `select` styling contrast so the OpenAI model picker and other shared selects render with a darker field surface and dark option list instead of the washed-out Windows dropdown look. Validation passed with `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Add OpenAI model selection dropdown in admin config - Replaced the free-text `Model` input in the Service/OpenAI config panel with a dropdown of common OpenAI model choices while still preserving any already-saved custom model as the selected current option. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Remove standalone OpenAI translation notice from admin panel - Deleted the OpenAI service sidebar notice that said subtitle translation was configured independently from Vbee voiceover, keeping the admin UI focused on the actual OpenAI management sections. Validation passed with `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`.
- [x] Replace subtitle translation with OpenAI-managed service - Added a new Service/OpenAI admin surface with token CRUD, request monitoring, and prompt or model config, then switched `/api/translation/*` to create OpenAI-backed subtitle jobs while keeping legacy `llm-subtrans` status or download compatibility for older request ids. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, `docker compose up -d --build`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, and the admin route `https://audio-test.accstore.pro.vn/admin/service/openai/tokens` responding successfully after deploy.
- [x] Split repository maps by surface and route agent lookups - Split the repository map into a desktop app map at `MAP.md`, a web admin map at `MAP.admin.md`, and a backend map at `MAP.backend.md`, then updated `.github/agents/coder.agent.md` so it chooses the matching map file before broad search based on whether the user asks for desktop, admin, backend, or cross-surface work. Updated `.github/roles/map.role.md` so future map maintenance follows the same split-map contract. Validation passed with markdown diagnostics showing no errors in the touched `.github` and map files, `npm run build` in both `frontend/` and `admin-frontend/`, `docker compose up -d`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, and a fresh Electron relaunch with the `VideoForge — Smart Video Editor` process active.
- [x] Remove project voiceover segment cache after narration - Updated the desktop Vbee narration flow so once narrator compose finishes, it deletes the entire `projects/<id>/subtitles/voiceover-segments/` directory instead of leaving cached `vbee-audio-*.audio` segment files behind. Also removed the existing cache folder from the sample project, leaving only the subtitle track JSON files under `subtitles/`. Validation passed with `node --check frontend/electron/projectVoiceoverSegmentStore.mjs frontend/electron/narratorCompose.mjs`, `npm run lint` and `npm run build` in `frontend/`, a cleanup run deleting the existing `projects/9f2a2ecc-a1cc-4b28-95c2-2aaffdc44bd9/subtitles/voiceover-segments` directory, and a direct directory read of `projects/9f2a2ecc-a1cc-4b28-95c2-2aaffdc44bd9/subtitles` showing only `track-original.json` and `track-vietnamese.json` remain.
- [x] Make narrator compose requests fully ephemeral - Updated the desktop narrator compose flow so every request now runs in a temp-only workspace and deletes the entire request directory after reading the final WAV bytes, which means no `voiceovers/narrator-compose/` folder, no raw `segment-*` files, no `voiceover.wav`, and no `manifest.json` are persisted under any project. Also deleted the previously retained narrator-compose request folders from the existing projects so the sample project no longer has a `voiceovers/narrator-compose/` directory at all. Validation passed with `node --check frontend/electron/narratorCompose.mjs`, `npm run lint` and `npm run build` in `frontend/`, a cleanup run deleting all persisted narrator-compose request directories, a direct existence check confirming `projects/9f2a2ecc-a1cc-4b28-95c2-2aaffdc44bd9/voiceovers/narrator-compose` no longer exists, `docker compose up -d`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, and a fresh Electron relaunch with the `VideoForge — Smart Video Editor` process active.
- [x] Remove retained narrator raw files while keeping manifests - Updated the desktop narrator compose flow so project-local `voiceovers/narrator-compose/` workspaces now delete the raw `segment-*.audio`, normalized `segment-*.wav`, and temporary `voiceover.wav` artifacts after each compose, leaving only `manifest.json` when a project id is available. Also cleaned the currently retained narrator raw artifacts from the existing project workspaces so the attached sample folder now resolves to just `manifest.json` on direct disk reads. Validation passed with `node --check frontend/electron/narratorCompose.mjs`, `npm run lint` and `npm run build` in `frontend/`, an absolute-path PowerShell directory read of `projects/9f2a2ecc-a1cc-4b28-95c2-2aaffdc44bd9/voiceovers/narrator-compose/vbee-1779365195-bb1c8c56f0-1779365196943` returning only `manifest.json`, an absolute-path repo-wide scan returning `MATCH_COUNT=0` non-manifest files under `projects/*/voiceovers/narrator-compose/*`, `docker compose up -d`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, and a fresh `npm run desktop:launch` relaunch with the `VideoForge — Smart Video Editor` Electron process active.
- [x] Mask admin Vbee cache keys and keep project narrator manifests - Updated the Service/Vbee `segments` admin React flow so cache keys never reveal the full `vbee-audio-*` hash in the list or detail heading, always stripping the prefix and showing only the last 5 characters. Updated the desktop narrator compose flow so the renderer passes the active `projectId` into Electron and `frontend/electron/narratorCompose.mjs` now keeps the exact `manifest.json` plus normalized compose workspace under each project's `voiceovers/narrator-compose/` directory instead of deleting everything after compose. Validation passed with `npm run lint` and `npm run build` in both `admin-frontend/` and `frontend/`, `node --check frontend/electron/narratorCompose.mjs`, `docker compose up -d --build`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, and a fresh `npm run desktop:dev` relaunch with the `VideoForge — Smart Video Editor` Electron process active.
- [x] Add password-confirmed single Vbee segment deletion - Updated the Service/Vbee `segments` admin detail flow so each grouped segment now has a `Delete segment` action that opens a password-confirmed dialog similar to `Clear cache`, then calls a backend `DELETE /api/admin/services/vbee/segments/:cacheKey` route that removes the selected segment group from MySQL, deletes the owned R2 asset when present, clears the related Redis audio/request cache entries, deletes now-empty requests, and refreshes surviving request summaries so `totalSegments`, `completedSegments`, and `characterCount` stay correct after partial history removal. Validation passed with `python -m py_compile server/vbee_request_store.py server/vbee_store.py server/vbee_asset_service.py server/vbee_routes.py`, `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, line-count checks keeping `server/vbee_request_store.py` at 354 lines, `server/vbee_asset_service.py` at 378 lines, `server/vbee_routes.py` at 376 lines, and `admin-frontend/src/components/VbeeSegmentsPanel.jsx` at 398 lines, plus a non-destructive container probe proving the helper deletes the targeted segment rows and asset row, removes a now-empty request, refreshes the remaining request totals, records the R2 object deletion, and returns HTTP `200` for correct admin password versus HTTP `401` for an incorrect password on the new delete route.
- [x] Fix Vbee segment hash preview, grouped toolbar actions, and inline admin audio playback - Updated the Service/Vbee `segments` admin flow so the list now strips the `vbee-audio-` prefix before showing only the last 5 hash characters, truncates the visible voice column to the first 10 characters, and groups the status selector with the Refresh and Clear cache controls in a single segmented toolbar. Reworked the admin segment audio preview to stop binding the R2 download-style URL directly into the web audio player: the backend now exposes an authenticated `/audio-stream` proxy that returns inline audio with forwarded `Range` headers, and the admin frontend now fetches that stream as a blob with access-token/refresh-token handling before attaching a local `blob:` URL to the player. Validation passed with `python -m py_compile server/vbee_routes.py`, `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, and a non-destructive container probe proving `/api/admin/services/vbee/segments/<hash>/audio-stream` returns HTTP `206` with `Content-Type: audio/mpeg`, `Content-Disposition: inline`, `Accept-Ranges: bytes`, the expected streamed body, and correct forwarding of `Range: bytes=0-3` to the upstream asset request.
- [x] Add Vbee segment pagination, admin password cache clear, and detail expiry visibility - Updated the Service/Vbee `segments` admin flow so the grouped segment list now paginates at 10 rows per page, truncates the visible text column to 15 characters, removes the `vbee-audio-` prefix from the displayed hash id before truncating it, and opens a password-confirmed clear-cache dialog that calls a new admin-only backend route to wipe all Vbee request/segment/cache records plus Redis Vbee keys and `vbee-audio-*` Cloudflare R2 objects. Updated Vbee segment detail to show `expiresAt` and grouped the Back/Refresh actions into a shared button group. Validation passed with repeated `python -m py_compile` checks for the touched Vbee backend files, `npm run lint` and `npm run build` in `admin-frontend/`, line-count checks keeping `admin-frontend/src/components/VbeeSegmentsPanel.jsx` at 307 lines, `server/vbee_routes.py` at 311 lines, and `server/vbee_asset_service.py` at 340 lines, `docker compose up -d --build`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, a non-destructive container probe confirming the clear-cache route is registered and the default segment page size is `10`, and a safe runtime probe with temporary DB rows plus mocked admin auth showing `get_vbee_segment_detail()` returns `expiresAt`, the clear-cache route returns HTTP `200` with a result payload when password verification passes, and returns HTTP `401` with `Admin password is incorrect.` when verification fails.
- [x] Move Vbee segment assets to Cloudflare R2 with 3-day expiry - Changed the backend Vbee completion flow so provider audio is downloaded server-side, uploaded to Cloudflare R2, and stored as a public `<CLOUDFLARE_R2_DOMAIN>/<fileName>` asset tracked in MySQL plus Redis with a 3-day expiry that is refreshed on identical-request reuse. Split the new asset/cache lifecycle into `server/vbee_asset_service.py`, `server/vbee_asset_expiry.py`, and `server/vbee_audio_cache_store.py` so `server/vbee_service.py` and `server/vbee_request_store.py` both returned under the 400-line guardrail, updated admin segment audio reads to trust only active R2 assets instead of re-fetching provider links, and passed the Cloudflare R2 env vars into the Docker `web` runtime. Validation passed with repeated `python -m py_compile` checks across the touched Vbee files, line-count checks showing `server/vbee_service.py` at 345 lines and `server/vbee_request_store.py` at 271 lines, `docker compose config --quiet`, `docker compose up -d --build`, `docker compose ps`, live `https://audio-test.accstore.pro.vn/api/health` returning HTTP `200`, an in-container smoke request storing a public R2 URL plus `fileName`/`expiresAt`, an immediate identical-request reuse probe returning `complete` and extending expiry, a forced-expiry cleanup probe clearing the DB asset row plus matching segment `audio_url` values, and a fresh public-R2 download probe returning HTTP `200` before cleanup.
- [x] Fix Vbee segment reuse skipping historical completed requests - Confirmed the backend reuse path only trusted short-lived `audioUrl` cache entries younger than 150 seconds, so identical requests older than that window always fell back to `queued` and resubmitted new TTS work to Vbee even though completed segment history still existed. Updated the Vbee request store to expose the latest reusable completed segment by `cacheKey`, and updated `create_voiceover_request()` to refresh a fresh `audioUrl` from the historical `providerRequestId` plus token before deciding to queue a new segment. Validation passed with `python -m py_compile server/vbee_request_store.py server/vbee_store.py server/vbee_service.py`, a direct expired-segment probe showing 148 completed segments older than the freshness window, a single-segment reuse probe returning `complete` immediately with the same historical `providerRequestId`, and a 3-segment historical-request reuse probe returning `complete` immediately with all three provider request ids reused.
- [x] Fix admin Vbee segment groups stuck in processing - Confirmed the admin segment tab was reading stale grouped segment rows without triggering provider refresh, so hashes with provider-completed work could remain shown as `processing` forever until some unrelated client status poll happened. Updated the admin Vbee segment list/detail/audio routes to refresh processing segments before loading grouped summaries, keeping the admin segment view aligned with the latest provider state. Validation passed with `python -m py_compile server/vbee_routes.py`, a focused container probe importing `_refresh_admin_vbee_segments()` plus `list_vbee_segment_summaries_page(status='processing')`, and a refresh recheck returning `0` remaining processing groups for the previously stuck sample set.
- [x] Add Vbee segment library admin view and project-local segment storage - Added grouped Vbee segment summary/detail APIs plus a fresh segment-audio URL refresh endpoint for admin playback, added the Service/Vbee `segments` route and detail UI, persisted downloaded Vbee segment files under each desktop project's `subtitles/voiceover-segments/` directory with local reuse before re-downloading, and taught the renderer voiceover flow to retry one failed segment download after refreshing request status so stale URLs stop surfacing as generic fetch failures. Validation passed with `python -m compileall server`, focused `python -m py_compile server/vbee_segment_store.py server/vbee_store.py server/vbee_routes.py`, `node --check frontend/electron/projectStoreShared.mjs frontend/electron/projectVoiceoverSegmentStore.mjs frontend/electron/narratorCompose.mjs`, `npm run lint` and `npm run build` in both `admin-frontend/` and `frontend/`, `docker compose up -d --build`, live `https://audio-test.accstore.pro.vn/api/health` returning `200`, a live three-subtitle Vbee smoke request reaching `complete` with the first segment audio URL returning HTTP `200`, and a fresh `npm run desktop:start` relaunch.