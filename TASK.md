# TASK

## Active
## Backlog
- [ ] Add client remember login
## Completed
- [x] Split frame chunk runner to keep native export files under the 400-line guardrail - Extracted frame chunk execution, per-chunk FFmpeg argument building, progress aggregation, and hardware retry logic from `frontend/electron/export/framePipeline.mjs` into the new `frontend/electron/export/frameChunkRunner.mjs`, leaving `framePipeline.mjs` focused on planning, start logging, and final concat orchestration. Validation passed with `node --check` on the split files, synthetic native export smoke tests, `npm run lint`, `npm run build` in `frontend/`, and line-count checks confirming `framePipeline.mjs` is 143 lines and `frameChunkRunner.mjs` is 323 lines.
- [x] Fix GPU export slowdown from NVENC oversubscription - Updated `frontend/electron/export/nativeFfmpeg.mjs` so hardware-backed native export no longer reuses the aggressive CPU worker plan that spawned 14 concurrent NVENC frame workers and 84 target chunks on this 28-logical-core machine. The hardware path is now session-aware with larger chunk durations, a queue depth of 2, and a default worker cap of 4 on this machine, while `frontend/electron/export/frameChunkRunner.mjs` falls back stepwise from 4 to 2 to 1 worker if a real project cannot sustain higher multi-session hardware encode. Validation passed with `node --check`, a worker-plan probe confirming the hardware path now plans 4 workers instead of 14 for 22/60/120/240-second timelines, a timed synthetic `nvidia-nvenc` export smoke test producing a final MP4 with audio in about 1.6 seconds, `npm run lint`, `npm run build` in `frontend/`, and line-count checks keeping `nativeFfmpeg.mjs` and `frameChunkRunner.mjs` under 400 lines.
- [x] Fix false GPU fallback and native export audio drift - Updated `frontend/electron/export/nativeFfmpeg.mjs` so the hardware-encoder verification probe uses a valid `256x144` test frame instead of an undersized `64x64` frame that incorrectly made NVENC look unusable on this machine, allowing `getNativeEncodePlan('balanced')` to select verified `h264_nvenc` again. Updated `frontend/electron/export/exportAudioStage.mjs` and `frontend/electron/export/timelineInputPlan.mjs` so native export audio is rebuilt from one source input with per-scene `atrim` plus `asetpts` filters instead of stacking multiple fast `-ss/-t -i` seeks that can accumulate sync error across many kept scenes, and updated `frontend/src/utils/exportAudioMix.js` to stop using `aresample=async=1` time-stretch filters in the shared export audio helpers. Validation passed with `node --check` on the touched modules, a real local GPU probe now selecting verified `nvidia-nvenc`, a synthetic native export smoke test producing a final MP4 with audio through `nvidia-nvenc`, `npm run lint`, `npm run build` in `frontend/`, and line-count checks keeping all touched files under 400 lines.
- [x] Implement hybrid native export scheduling for CPU + GPU - Updated `frontend/electron/export/exportCoordinator.mjs` so native export now probes the verified encoder plan up front and, when a usable hardware encoder is available, runs frame rendering and audio rendering in parallel instead of serializing them. The frame path continues to use the verified GPU encoder through `framePipeline.mjs` while `exportAudioStage.mjs` mixes or trims audio on CPU in the same wall-clock window, with CPU-only machines keeping the older sequential path to avoid unnecessary CPU contention. Validation passed with `node --check` on the touched export modules, a step-by-step isolation probe confirming the input generation, native audio stage, and native frame stage each succeed with a simulated hybrid encoder plan, a full synthetic hybrid-parallel smoke test producing a final MP4 with audio, `npm run lint`, `npm run build` in `frontend/`, and line-count checks keeping `exportCoordinator.mjs`, `exportAudioStage.mjs`, and `framePipeline.mjs` under 400 lines.
- [x] Maximize native export resource usage - Tuned `frontend/electron/export/nativeFfmpeg.mjs` so native frame export uses shorter chunks, deeper queues, and more concurrent FFmpeg workers when machine resources are available, and added explicit per-worker `encodeThreads` consumed by `frontend/electron/export/framePipeline.mjs`. On this 28-logical-core machine the balanced CPU path now plans 12 frame workers for a 120-second timeline, 2.5-second target chunks, 60 queued chunks, and separate decode/encode/filter threads per worker, while retaining verified GPU encoder probing when a usable GPU encoder is available. Validation passed with `node --check` on the changed modules, a worker-plan probe, an aggressive synthetic native export smoke test that produced a final MP4 with audio, `npm run lint`, `npm run build` in `frontend/`, and line-count checks keeping touched export files below 400 lines.
- [x] Speed up native export pipeline - Removed the native export dependency on intermediate `segment-*.mp4` and `merged.mp4` scene files by adding `frontend/electron/export/timelineInputPlan.mjs`, changing `framePipeline.mjs` to seek directly into kept source scene ranges per chunk, and changing `exportAudioStage.mjs` to render video audio directly from the same kept scene ranges before optional voiceover mixing. Updated `exportCoordinator.mjs` to skip `extractSceneSegments -> mergeSceneSegments`, kept final remux behavior for audio output, and updated `nativeFfmpeg.mjs` so GPU encoders are selected only after a real one-frame encode probe succeeds. Validation passed with `node --check` on changed export modules, a native encoder probe showing this machine falls back to verified `cpu-libx264` because no GPU encoder is usable, a synthetic native export smoke test that produced a final MP4 with audio through the direct timeline path, `npm run lint`, `npm run build` in `frontend/`, and line-count checks keeping all touched export files below 400 lines.
- [x] Remove LibreTranslate from the Whishper stack - Updated `docker-compose.yml` to remove the `translate` service, removed `whishper`'s `depends_on` link to LibreTranslate, and cleared `PUBLIC_TRANSLATION_API_HOST` so the runtime matches the no-LibreTranslate Whishper sample while preserving the existing internal transcription target `http://whishper:8000`. Validation passed with `docker compose config --quiet`, `docker compose up -d --build`, `docker compose ps`, a web-container env check confirming `WHISPER_API_URL=http://whishper:8000`, and a direct POST probe to `http://whishper:8000/transcribe/` returning HTTP `200`.
- [x] Add LLM-Subtrans Docker service and internal proxy target - Added `llm-subtrans-web` plus optional `llm-subtrans` CLI services to `docker-compose.yml`, pointed the backend `web` service at `LLM_SUBTRANS_API_URL=http://llm-subtrans-web:8080/api`, reused the compose `mysql` service for the LLM-Subtrans runtime, published Redis on host port `6379`, switched the LLM-Subtrans runtime `REDIS_URL` to `redis://host.docker.internal:6379/0` so workers created outside the compose network can reach Redis, exposed the LLM-Subtrans web admin on `${LLM_SUBTRANS_PORT:-8091}` so managed workers can be created without colliding with an occupied `8090`, and ignored the new `llm_subtrans_data/` bind-mount directory. Updated `server/proxy_routes.py` so translation requests no longer target hard-coded `localhost:8090` from inside the container and instead resolve the internal Docker service through environment configuration. Validation is tracked in this session after the compose restart and Redis reachability checks.
- [x] Fix subtitle generation against sync Whishper API - Updated `server/proxy_transcription_routes.py` to normalize `WHISPER_API_URL`, call the internal `http://whishper:8000/transcribe/` API with FastAPI-compatible form fields, accept immediate `segments` responses from Whishper, and mark sync request records as completed without polling. Updated `frontend/src/utils/audioExtractor.js` so the desktop subtitle flow accepts immediate segment results while keeping legacy async job-id handling as a fallback, corrected `server/auth_store.py` so the credit wrappers forward `change_type`, `note`, and `details` metadata instead of crashing before transcription starts, and corrected `docker-compose.yml` so `web` now points `WHISPER_API_URL` at the working internal API port while keeping `whishper` internal-only. Validation passed with `python -m py_compile server/auth_store.py server/proxy_transcription_routes.py server/proxy_credit_helpers.py`, `python -m compileall server`, `docker compose config --quiet`, `docker compose up -d --build`, a web-container env check confirming `WHISPER_API_URL=http://whishper:8000`, a direct POST probe to `http://whishper:8000/transcribe/` returning HTTP `200` with transcription `segments`, and a live authenticated `POST https://audio-test.accstore.pro.vn/api/transcription/start` probe returning HTTP `200` with `creditBalance`, `creditCost`, `segments`, and `text`.
- [x] Add internal Whishper Docker service and backend proxy target - Added `mongo`, `translate`, and `whishper` services to `docker-compose.yml` with only internal Docker exposure, bind-mounted Whishper data under `whishper_data/`, wired `web` to call Whishper through the internal `WHISPER_API_URL` setting, ignored the new worker data directory in `.gitignore`, and split transcription plus shared credit logic into `server/proxy_transcription_routes.py` and `server/proxy_credit_helpers.py` so `server/proxy_routes.py` returned under the 400-line guardrail. Validation passed with `docker compose config --quiet`, `python -m py_compile server/proxy_routes.py server/proxy_transcription_routes.py server/proxy_credit_helpers.py`, and line counts of 354/122/36 for the split proxy files; runtime rebuild and relaunch are tracked below in this same session.
- [x] Restore missing scene-list actions hook for startup - Recreated `frontend/src/hooks/useEditorSceneListActions.js` after `npm run desktop:start` failed because `useVideoEditor.js` imported the split scene-list hook but the file was missing; the restored hook owns delete/restore all, toggle scene deletion, seek-to-scene, export-result clearing, and undo snapshot wiring. Validation passed with diagnostics on the touched hook files, a 37-line guardrail check, `npm run lint`, `npm run build` in `frontend/`, `docker compose up -d` with web healthy, and a fresh `npm run desktop:start` relaunch whose logs show backend ready, renderer loaded, and the `My Projects` page rendered.
- [x] Fix series detail menu and player display - Updated the dashboard series detail so per-episode option dropdowns render through a fixed portal above the dashboard instead of being clipped inside episode rows, kept the project edit dialog mounted above the series detail view, and replaced the raw series `<video controls>` player with an editor-player-style preview stage plus custom play, seek, and time controls. Validation passed with diagnostics on touched dashboard files, line-count checks keeping `ProjectDashboard.css` at 357 lines and other touched files under 400, `npm run lint` and `npm run build` in `frontend/`, `docker compose up -d` with backend services healthy, and a fresh `npm run desktop:start` Electron relaunch against `https://audio-test.accstore.pro.vn`.
- [x] Add dashboard series grouping - Extended local desktop project metadata with series and episode fields, added `lucide-react` for icon buttons, split the dashboard into reusable cards, dialogs, series detail, and pure model helpers, changed `dashboard.root` into a top series list plus lower standalone-video list, added per-video option dropdowns for edit/delete, added video info editing with series assignment or None, added series creation from selected standalone videos, and added a YouTube-like series detail view with a left video player plus right sorted episode list where double-clicking the episode label enables inline episode-number editing. Validation passed with `npm run lint` and `npm run build` in `frontend/`, line-count checks confirming touched dashboard/Electron files are under 400 lines including `frontend/electron/projectStore.mjs` at 399 lines, `docker compose up -d` with backend services running, and a fresh `npm run desktop:start` Electron relaunch against `https://audio-test.accstore.pro.vn`.
- [x] Cancel pending QR payments on close and split PaymentQrDialog - Updated `header.dashboard.premium-popup.payment` so closing the popup while a desktop QR payment is still pending now calls a server-side cancel route before dismissing the UI, added authenticated ticket cancel support in `server/iap_payment_routes.py` plus `server/iap_payment_store.py`, extended `frontend/src/utils/iapClient.js` with the cancel call, styled admin `cancelled` status pills in `admin-frontend/src/styles/tables.css`, and split the oversized popup into `frontend/src/components/AppShell/PaymentQrDialog.jsx`, `frontend/src/components/AppShell/PaymentQrDialogShared.jsx`, and `frontend/src/components/AppShell/paymentQrDialogModel.js` so the main dialog returned under the 400-line guardrail. Validation passed with `npm run build` and `npm run lint` in `frontend/`, `python -m py_compile server/iap_payment_store.py server/iap_payment_routes.py`, `npm run build` and `npm run lint` in `admin-frontend/`, line-count checks confirming `PaymentQrDialog.jsx` is 303 lines, `PaymentQrDialogShared.jsx` is 37 lines, `paymentQrDialogModel.js` is 82 lines, `server/iap_payment_store.py` is 355 lines, and `server/iap_payment_routes.py` is 220 lines, followed by `docker compose up -d --build`, live `200` checks for `https://audio-test.accstore.pro.vn/api/health` plus `https://audio-test.accstore.pro.vn/admin/iap/payment-tools/transactions/1`, and a fresh `npm run desktop:start` relaunch against `https://audio-test.accstore.pro.vn`. End-to-end cancel API probing was not run because it requires an authenticated pending ticket.
- [x] Add desktop status-check time to admin payment transaction detail - Updated `admin.react.manage.iap.api-key.payment-transactions.detail` so the admin detail table now shows the last time the desktop client polled payment status, persisted as `last_client_check_at` in `server/iap_payment_store.py`, written by the authenticated `/api/iap/payments/:id` status route in `server/iap_payment_routes.py`, and rendered in `admin-frontend/src/components/IapPaymentTransactionDetailPanel.jsx`. Split the ticket/refund row mappers and pagination helpers into `server/iap_payment_records.py` so `server/iap_payment_store.py` stayed under the 400-line guardrail. Validation passed with a focused fallback `python -m py_compile server/iap_payment_store.py server/iap_payment_routes.py server/iap_payment_records.py` check when no sample payment ticket existed, `npm run build` and `npm run lint` in `admin-frontend/`, post-split line-count checks showing `server/iap_payment_store.py` back under 400 lines, `docker compose up -d --build`, and a live `200` check for `https://audio-test.accstore.pro.vn/admin/iap/payment-tools/transactions/1`.
- [x] Add premium expiry countdown bar to the desktop premium popup - Updated `header.editor.premium-popup` in `frontend/src/components/AppShell/PremiumPackagesDialog.jsx` to show the current premium expiry in the header note and a single HP-style countdown bar with the remaining time centered inside it, using the new `frontend/src/utils/premiumWindow.js` helper plus matching popup styles in `frontend/src/components/AppShell/PremiumPackagesDialog.css`. Validation passed with `npm run build` and `npm run lint` in `frontend/`, line-count checks confirming `PremiumPackagesDialog.jsx` is 388 lines, `PremiumPackagesDialog.css` is 396 lines, and `premiumWindow.js` is 45 lines, followed by `docker compose up -d` and a fresh `npm run desktop:start` relaunch against `https://audio-test.accstore.pro.vn`.
- [x] Move pack-function editing into package actions and remove the tab - Removed the standalone `admin.react.manage.iap.pack-function` tab, mapped the old `/admin/iap/pack-function` route back to packages, placed the function/edit/delete flows into one compact icon-only action group in the package Actions column, and replaced the deleted pack-function panel with a per-package dialog that now shows Package/Package id/Pack type/Current behavior as a key-value table before the editable function fields. Validation passed with `npm run build` and `npm run lint` in `admin-frontend/`, `docker compose up -d --build`, and live `200` checks for `https://audio-test.accstore.pro.vn/admin/iap/packages` plus `https://audio-test.accstore.pro.vn/admin/iap/pack-function`.
- [x] Move bank-hook-history detail into the payment-tools content area - Switched bank-hook-history row clicks inside payment tools to a dedicated `/admin/iap/payment-tools/history/:id` route, rendered the bank-hook detail page in embedded mode so it replaces the right-side history table like other payment-tool details, and kept `/admin/iap/bank-hook-history/:id` as a frontend alias for the same embedded payment-tools detail state. Validation passed with `npm run build` and `npm run lint` in `admin-frontend/`, `docker compose up -d --build`, and live `200` checks for `https://audio-test.accstore.pro.vn/admin/iap/payment-tools/history/1` plus `https://audio-test.accstore.pro.vn/admin/iap/bank-hook-history/1`.
- [x] Align bank-hook detail summary with payment transaction detail layout - Reworked `admin.react.manage.iap.bank-hook-history.detail.summary` so the summary now renders as the same two-column detail table used by payment transaction detail, while keeping the raw JSON payload in its own panel below. Validation passed with `npm run build` in `admin-frontend/`.
- [x] Add beneficiary and refund detail views in payment tools - Re-enabled embedded bank-hook history row navigation so payment-tools history opens the existing detail page again, simplified the refund-pending list down to transaction or amount or status, and added local replacement detail views for both refund-pending and beneficiary rows with back or refresh actions plus key-value tables. Validation passed with `npm run build` in `admin-frontend/`, `npm run lint` in `admin-frontend/`, `docker compose up -d --build`, and live `200` checks for `https://audio-test.accstore.pro.vn/admin/iap/payment-tools/refunds`, `/admin/iap/payment-tools/beneficiaries`, `/admin/iap/payment-tools/history`, and `/admin/iap/bank-hook-history/1`.
- [x] Polish IAP payment detail tables and popup layout - Tightened the embedded bank-hook history panel inside payment tools so it drops duplicate full-page chrome and uses a denser embedded table layout, converted `admin.react.manage.iap.api-key.payment-transactions.detail` into a two-column key-value table, and redesigned `header.dashboard.credit-popup.payment` into a two-column package-card plus information-table layout with synchronized secondary or primary buttons. Validation passed with `npm run build` in `admin-frontend/`, `npm run build` in `frontend/`, `npm run lint` in `admin-frontend/`, `npm run lint` in `frontend/`, `docker compose up -d --build`, live `200` for `https://audio-test.accstore.pro.vn/admin/iap/payment-tools/history`, and a fresh `npm run desktop:start` relaunch after rebuilding the desktop frontend.
- [x] Make IAP tabs route-driven and add transaction detail panel - Switched `admin.react.manage.iap` tabs and payment-tools subviews to URL-driven navigation, removed the extra payment-tools heading copy, kept the left and right payment-tools panels stretched to equal height, added a route-addressable transaction detail replacement view with Back and Refresh actions, added the matching admin payment-transaction detail API, and extended Flask admin SPA serving so refreshed `/admin/iap/...` deep links resolve correctly. Validation passed with targeted diagnostics, `npm run lint` and `npm run build` in `admin-frontend/`, `python -m py_compile server/iap_payment_routes.py server/admin_web_routes.py`, `docker compose up -d --build`, and live `200` checks for `https://audio-test.accstore.pro.vn/admin/iap/api-key`, `/admin/iap/payment-tools/transactions`, `/admin/iap/payment-tools/history`, and `/admin/iap/payment-tools/transactions/1`.
- [x] Move payment utility buttons into a dedicated IAP tab - Added a separate `Payment tools` tab under `admin.react.manage.iap`, moved beneficiaries, refund pending, transactions, and history out of the API-key tab, and rendered them in a 30/70 split layout with the four buttons on the left and the selected content on the right, defaulting to transactions. Reused the bank-hook history list in embedded mode so the tab keeps the same filters and pagination without route-level header actions. Validation passed with admin-file diagnostics, `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and live `200` verification for `https://audio-test.accstore.pro.vn/admin/iap`.
- [x] Refine QR payment overlay and inline admin IAP views - Moved admin API-key beneficiary, refund-pending, and payment-transaction views from modal dialogs into inline body sections, kept beneficiary creation in a dedicated popup form, repaired the purchase-popup CSS, split QR overlay styles into `frontend/src/components/AppShell/PaymentQrDialog.css` so the touched popup stylesheet returned below the 400-line guardrail, rendered the QR checkout as a portal overlay above the package popup with fuller confirmation details, and added an authenticated backend QR-image proxy consumed by the desktop client as a blob so the QR image displays under the desktop runtime policy. Validation passed with targeted diagnostics, `npm run lint` and `npm run build` in `frontend/`, `npm run lint` and `npm run build` in `admin-frontend/`, `python -m py_compile server/iap_payment_routes.py`, line-count checks confirming `PremiumPackagesDialog.css` is 293 lines and `PaymentQrDialog.css` is 150 lines, `docker compose up -d --build` with the web container healthy, `https://audio-test.accstore.pro.vn/api/health` returning `200`, the new QR route returning authenticated `401` when unauthenticated, and a fresh `npm run desktop:start` relaunch with workspace Electron processes running.
- [x] Add Sepay QR IAP payment flow - Added beneficiary account management with one current account in `admin.react.manage.iap.api-key`, loading bank ids from `https://qr.sepay.vn/banks.json`, plus admin dialogs for refund pending records and payment transactions. Added `server/iap_beneficiary_store.py`, `server/iap_payment_store.py`, `server/iap_payment_routes.py`, and `server/iap_payment_expiry.py` for current beneficiary accounts, 3-minute payment tickets, once-per-minute expiry, bank-hook validation against transaction content/account/amount, entitlement application through pack functions, and refund-pending records with detailed failure reasons. Updated desktop premium and credit package dialogs to open a QR payment overlay above the package popup, create server payment tickets, build Sepay QR image URLs with bank/account/amount/content, poll once per second, show countdown, update the local auth session on success, and show failure reasons on failed or expired tickets. Guardrail split kept touched files under 400 lines. Validation passed with Python py_compile for new server files, line-count checks, `npm run lint` and `npm run build` in `admin-frontend/`, `npm run lint` and `npm run build` in `frontend/`, `python -m compileall server`, `docker compose up -d --build`, and an in-container payment probe confirming `/api/pay/info` returned `{ "historyId": 15, "success": true }` and the ticket status became `paid`.
- [x] Simplify payment hook success payload - Updated `server/iap_routes.py` so `/api/pay/info` now returns the minimal success response shape `{ "historyId": <number>, "success": true }` instead of exposing `apiKeyId`, `ok`, and `received`. Validation passed with `python -m py_compile server/iap_routes.py`, `docker compose up -d --build`, and an in-container probe using an active IAP API key confirming a `200` response body of `{"historyId": 12, "success": true}` with the removed fields absent.
- [x] Tighten credit popup and admin IAP layout polish - Restricted the desktop credit purchase flow to `addCredit` packages only in `frontend/src/utils/iapClient.js`, hid the admin left sidebar when no admin session is present by switching `admin-frontend/src/components/AdminLayout.jsx` to real signed-in state, centered the `Recommend` status cell in `admin-frontend/src/components/IapPackagesPanel.jsx`, and made the pack-function table fill the available panel width through `admin-frontend/src/components/IapPackFunctionPanel.jsx` plus `admin-frontend/src/styles/iap.css`. Validation passed with `npm run lint` and `npm run build` in `frontend/`, `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and a fresh `npm run desktop:start` relaunch with Electron confirmed running.
- [x] Refine header purchase popups and IAP admin actions - Updated `frontend/src/components/AppShell/PremiumPackagesDialog.jsx` so dash-delimited description text becomes separate bullet items in premium cards, added `frontend/src/components/AppShell/CreditPackagesDialog.jsx` plus expanded `frontend/src/utils/iapClient.js` filtering so the authenticated header credit pill opens a server-backed credit purchase popup, changed `frontend/src/components/Auth/AuthHeaderActions.jsx` and `frontend/src/components/AppShell/AppHeader.jsx` so logout now lives inside the username dropdown and the premium button is hidden for signed-out clients, switched the recommend column in `admin-frontend/src/components/IapPackagesPanel.jsx` to a green check icon or `-`, hid raw API keys in `admin-frontend/src/components/IapApiKeyPanel.jsx` behind a copy-only icon action, and added real pack-function edit support across `admin-frontend/src/components/IapPackFunctionPanel.jsx`, `admin-frontend/src/api/adminApi.js`, `server/iap_admin_store.py`, and `server/iap_routes.py`. Validation passed with `npm run lint` and `npm run build` in `frontend/`, `npm run lint` and `npm run build` in `admin-frontend/`, `python -m py_compile server/iap_admin_store.py server/iap_routes.py`, a runtime probe confirming pack-function credits update from `100` to `250`, a runtime probe confirming the API-key `apikey <API_KEY>` format still validates, `python -m compileall server`, `docker compose up -d --build`, and live `200` checks for `/api/health` plus `/api/iap/packages`.
- [x] Refine premium popup scroll and IAP admin controls - Updated `frontend/src/components/AppShell/PremiumPackagesDialog.css` to hide the bottom horizontal scrollbar while keeping swipe-based page navigation in the desktop premium popup, changed `admin-frontend/src/components/IapPackagesPanel.jsx` to truncate package detail text to 30 characters and render row actions as a compact grouped icon button set, added the supporting IAP admin styles in `admin-frontend/src/styles/iap.css`, and extended the API-key flow through `admin-frontend/src/components/IapApiKeyPanel.jsx`, `server/iap_api_key_store.py`, and `server/iap_routes.py` so admins can configure a header value format using the `<API_KEY>` placeholder such as `apikey <API_KEY>`. Validation passed with `npm run lint` and `npm run build` in `frontend/`, `npm run lint` and `npm run build` in `admin-frontend/`, `python -m py_compile server/iap_api_key_store.py server/iap_routes.py`, a runtime probe confirming `apikey <API_KEY>` matches during hook validation, `python -m compileall server`, `docker compose up -d --build`, and live `200` checks for `/api/health` plus `/api/iap/packages`.
- [x] Refine premium popup paging and recommend flag - Reworked `frontend/src/components/AppShell/PremiumPackagesDialog.jsx` plus `frontend/src/components/AppShell/PremiumPackagesDialog.css` so the desktop premium popup now keeps package cards at a consistent width, shows at most 3 plans per page, supports horizontal page-swiping with snap pagination and next/previous controls, and renders the recommended badge inline beside the package name instead of overlapping the card. Added `isRecommended` normalization and recommended-first sorting in `frontend/src/utils/iapClient.js`, persisted the new recommend flag in `server/iap_store.py`, exposed it through both public and admin package APIs in `server/iap_routes.py`, and added a recommend checkbox plus table status in `admin-frontend/src/components/IapPackagesPanel.jsx`. Validation passed with `npm run lint` and `npm run build` in `frontend/`, `python -m py_compile server/iap_store.py server/iap_routes.py`, and `npm run lint` plus `npm run build` in `admin-frontend/`.
- [x] Add desktop premium purchase popup - Updated `frontend/src/components/AppShell/AppHeader.jsx` so the shared desktop premium button opens a new server-backed premium plans popup instead of acting as a static status badge, added `frontend/src/components/AppShell/PremiumPackagesDialog.jsx` plus `frontend/src/components/AppShell/PremiumPackagesDialog.css` for the card layout popup, and added `frontend/src/utils/iapClient.js` to load and filter only `premiumSubscribe` and `creditsAndPremiumPack` records from `/api/iap/packages`. Validation passed with `npm run lint` and `npm run build` in `frontend/`, `docker compose up -d --build`, a live `200` check for `/api/iap/packages`, and a fresh `npm run desktop:start` relaunch. Current server data note: the public packages API returned `0` premium-capable packages, so the popup shows its empty state until active premium packages are created.
- [x] Fix bank hook transaction time display - Updated `admin-frontend/src/components/IapBankHookHistoryPage.jsx` and `admin-frontend/src/components/IapBankHookHistoryDetailPage.jsx` to prefer the original bank-provided `transactionDate` string instead of converting the derived timestamp through local timezone formatting, so `2024-07-02 11:08:33` stays displayed exactly as received from the bank. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `docker compose up -d --build`, and a live `200` check for `/admin/iap/bank-hook-history`.
- [x] Add bank hook history detail, search, and date filters - Extended `server/iap_bank_hook_history_store.py` with searchable and date-filtered pagination plus single-record lookup, exposed the new filters and transaction-detail API routes through `server/iap_routes.py`, served the detail SPA path from `server/admin_web_routes.py`, added filter controls and clickable rows to `admin-frontend/src/components/IapBankHookHistoryPage.jsx`, and created `admin-frontend/src/components/IapBankHookHistoryDetailPage.jsx` for per-transaction detail with raw payload inspection. Validation passed with `python -m py_compile server/iap_bank_hook_history_store.py`, `python -m compileall server`, `npm run lint` and `npm run build` in `admin-frontend/`, a backend probe confirming search/date/detail all matched the sample bank payload and then cleaned it up, `docker compose up -d --build`, `docker compose ps web` showing `audioedit-web-1` healthy, and HTTP `200` checks for `/admin/iap/bank-hook-history` plus `/admin/iap/bank-hook-history/1`.
- [x] Add bank hook history page for IAP API keys - Added persistent bank-hook transaction history storage in `server/iap_bank_hook_history_store.py`, made `/api/pay/info` log each validated bank payload with the matched API key snapshot plus transaction details, exposed paginated admin history reads from `/api/admin/iap/bank-hook-history`, added a `History` action in `admin.react.manage.iap.api-key`, and created the dedicated admin React bank-hook history page under `/admin/iap/bank-hook-history`. Validation passed with `python -m py_compile server/iap_bank_hook_history_store.py`, `python -m compileall server`, `npm run lint` and `npm run build` in `admin-frontend/`, a backend probe confirming the sample bank payload was recorded and then cleaned up, `docker compose up -d --build`, `docker compose ps`, and an HTTP `200` check for `/admin/iap/bank-hook-history`.
- [x] Fix premium-window dialog overlap in admin user detail - Updated `admin-frontend/src/styles/detail.css` so the narrow detail-page `credit-dialog` width override no longer applies to the wider `package-dialog` used by `admin.react.detail.premium-window`, which stops the native `datetime-local` inputs from colliding inside the popup. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, followed by `docker compose up -d --build` and `docker compose ps` showing `audioedit-web-1` healthy.
- [x] Fix desktop blank renderer startup - Hardened Electron startup in `frontend/electron/main.mjs` by moving Chromium session data into a dedicated `session-data` directory, disabling GPU shader disk cache, and adding renderer diagnostics; then fixed the actual renderer crash in `frontend/src/utils/authClient.js` by guarding timestamp-based premium checks against a null unauthenticated user. Validation passed with `npm run build` and `npm run desktop:start` in `frontend/`, fresh Electron relaunches, and latest Electron debug logs showing no more `premiumStartAt` crash plus `Renderer DOM snapshot after load` with `rootChildCount: 1`.
- [x] Fix admin user list runtime error - Restored the missing `_row_to_user_record` import in `server/admin_store.py` so `/api/admin/users` no longer crashes while building the paginated user list. Validation passed with `python -m compileall server`, a direct `list_auth_users_page(page=1, page_size=5)` probe returning user rows plus pagination, and `docker compose up -d --build` with all containers up afterward.
- [x] Convert premium access to timed windows - Replaced the boolean premium model with persisted `premiumStartAt` and `premiumEndAt` timestamps across auth and admin flows, split shared auth-user and premium-window logic into `server/auth_user_record.py` plus refresh-token persistence into `server/auth_refresh_store.py` so `server/auth_store.py` stays below the 400-line guardrail, updated admin React user editing to manage premium date-time windows instead of a true/false toggle, changed IAP pack-function creation to use timed premium duration days, and made the desktop renderer derive premium-active state from timestamps so watermark hiding expires automatically. Validation passed with a backend probe covering active and future premium windows plus timed premium pack-function creation, targeted import and line-count checks confirming `server/auth_store.py` is back under 400 lines, `npm run lint` and `npm run build` in `admin-frontend/`, `npm run lint` and `npm run build` in `frontend/`, and `python -m compileall server`.
- [x] Pin desktop app backend to remote domain only - Removed the remaining Electron localhost override and local-backend spawn paths so the desktop app now only uses `https://audio-test.accstore.pro.vn`, updated the frontend README and workspace coder-agent guidance to match the remote-only desktop policy, and validated the change with targeted searches confirming the desktop runtime path no longer contains `ELECTRON_SERVER_URL`, `ELECTRON_SPAWN_BACKEND`, or `127.0.0.1:5000`, plus `npm run build` in `frontend/`.
- [x] Add admin user account lock controls - Added persisted `isLocked` state in the auth schema, exposed it through auth and admin payloads, blocked locked non-admin accounts from login, refresh, and protected access-token guarded routes, revoked their refresh tokens when an admin locks the account, prevented admin accounts from ending in a locked state, showed `Locked/Active` status in the admin React user table, and added a lock or unlock button in the admin React user detail view with admin accounts disabled in UI. Validation passed with a backend probe that created a temporary user, verified login and refresh before lock, locked the user through the admin store, confirmed login rejection plus refresh-token revocation plus admin-lock protection, `npm run build` in `admin-frontend/`, `python -m compileall server`, `docker compose up -d --build`, live `200` checks for `http://127.0.0.1:5000/api/health` and `http://127.0.0.1:5000/admin/manage`, and a desktop restart against `http://127.0.0.1:5000` with Electron processes confirmed running.
- [x] Switch desktop backend startup to Docker domain flow - Updated the desktop runtime so Electron now defaults to `https://audio-test.accstore.pro.vn` instead of `127.0.0.1:5000`, changed local Flask startup into an explicit `ELECTRON_SPAWN_BACKEND=1` opt-in path, updated the frontend README and the workspace coder agent guidance to use `docker compose up -d` as the standard backend startup flow, and kept the desktop app pointed at the Docker-backed domain by default. Validation passed with VS Code diagnostics on the touched frontend files, `npm run build` in `frontend/`, live `200` checks for `https://audio-test.accstore.pro.vn/api/health`, `docker compose up -d`, a healthy `docker compose ps`, `npm run desktop:start`, and a runtime process check confirming Electron was running with no local Python backend spawned from the workspace.
- [x] Add phpMyAdmin to Docker stack - Extended `docker-compose.yml` with a `phpmyadmin` service connected to the MySQL container and exposed it on port `8080`, then updated `.gitignore` to ignore the local Docker bind-mount directories for backend logs, uploads, MySQL, and Redis. Validation passed with VS Code diagnostics on the touched files, `docker compose config`, `docker compose up -d`, `docker compose ps`, a live `/api/health` check returning HTTP `200`, and a live phpMyAdmin check on `/` returning HTTP `200`.
- [x] Add backend Docker runtime - Added `server/Dockerfile` as a multi-stage backend image that builds `admin-frontend/dist`, installs the Flask server dependencies plus Gunicorn, and exposes the backend on port `5000`; added `docker-compose.yml` with `web`, `mysql`, `redis`, and `cloudflared`; added `.dockerignore`; created a local `.env` with the provided Cloudflare tunnel token and default backend secrets; and ignored `.env` in git. Validation passed with VS Code diagnostics on the new files, `docker compose config`, `python -m compileall server`, `docker compose up -d --build`, and a live `/api/health` check returning HTTP `200` after freeing occupied ports `3306` and `6379`.
- [x] Fix IAP package create failure and popup error visibility - Corrected the `server/iap_store.py` schema-migration helper call so IAP package creation no longer crashes during schema setup, and updated `admin-frontend/src/components/IapPackagesPanel.jsx` so create or edit save errors render directly inside the package popup instead of only at the page level. Validation passed with a direct create/load/delete package probe through `server.iap_store`, VS Code diagnostics on the touched files, `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, a clean `frontend/npm run desktop:start` restart, and live `200` checks for `/api/health` and `/admin/iap`.
- [x] Refine IAP admin dialogs and pack types - Split `admin-frontend/src/components/IapManagementTabs.jsx` into dedicated API-key, pack-function, and sale panels so the IAP admin surface stays below the 400-line guardrail, moved every non-package `Add new` flow into modal dialogs, added package-level `packType` storage and serialization for `addCredit`, `premiumSubscribe`, and `creditsAndPremiumPack`, removed package-level credits from the admin package form and table, and changed pack-function creation to select from existing package names while showing only the inputs implied by the selected package type. Validation passed with VS Code diagnostics on the touched frontend and backend files, `npm run lint` and `npm run build` in `admin-frontend/`, `python -m compileall server`, a clean workspace-process shutdown, `npm run desktop:start` in `frontend/`, and live `200` checks for `/api/health` plus `/admin/iap`.
- [x] Add IAP admin tabs and payment hook config - Reworked `admin.react.manage.iap` into a four-tab admin surface for `IAP package`, `API key`, `Pack function`, and `Sale`, keeping the existing package CRUD in the first tab and adding MySQL-backed admin APIs for bank-hook API keys, pack function mappings such as add credits or unlock premium, and sale rules with time windows, first-pack purchase, first-IAP purchase, and packId conditions. The API key tab now also stores the allowed HTTP method and inbound header name for `/api/pay/info`, and the payment hook validates requests against those configured values instead of a hard-coded POST or `X-Api-Key`. Validation passed with Python compilation for `server/iap_api_key_store.py`, `server/iap_admin_store.py`, `server/iap_routes.py`, and `server/app.py`, `npm run lint` and `npm run build` in `admin-frontend/`, backend probes for payment hook 200/401 behavior, configurable method and header-name checks, pack function CRUD, sale CRUD, cleanup of probe data, and line-count checks under 400 lines.
- [x] Move admin back and refresh actions into header - Added a shared route-level action slot in the admin React header, moved page-specific `Back` and `Refresh` actions for manage, user-detail, and IAP routes into that header, removed the manage and user-detail summary panels from the page body, and simplified the IAP panel heading so the remaining content starts directly with the main working panels. Validation passed with VS Code diagnostics on the touched admin frontend files plus `npm run lint` and `npm run build` in `admin-frontend/`.
- [x] Fix admin fixed shell layering - Made `admin.react.header` a fixed full-width top bar, kept `admin.react.sidebar` fixed with a higher z-index so it overlays the header's top-left corner, and added shell spacing so the admin page body clears both fixed surfaces. Follow-up fix restored the intended fixed positioning after the shared `dev-host` developer-marker wrapper was found overriding both surfaces back to `position: relative`. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/` plus a rebuilt live CSS check.
- [x] Add fixed left nav for admin web sections - Reworked the React admin shell to use a fixed full-height left sidebar with dedicated `User manager` and `IAP` tabs, split IAP package management onto its own `/admin/iap` page, kept user detail under the user-manager nav state, and updated Flask admin web serving to support direct SPA reloads on `/admin/iap`. The sidebar now also stays visible on login or signed-out admin routes so the admin shell always shows the left navigation. Validation passed with VS Code diagnostics on the touched admin frontend files, `python -m py_compile server/admin_web_routes.py`, `npm run lint` and `npm run build` in `admin-frontend/`, and a final desktop runtime restart with live route checks.
- [x] Add IAP package admin and Redis-backed client API - Added MySQL-backed IAP package persistence with admin CRUD routes, a Redis-backed public `/api/iap/packages` cache keyed under the `audio_editor` prefix on `localhost:6379`, and a new admin React package-management panel for creating, editing, hiding, and deleting packages with fields including `id`, `name`, `price`, `currency`, `credits`, and `description`. Validation passed with `python -m py_compile server/iap_store.py server/iap_cache.py server/iap_routes.py server/app.py`, a backend import probe through `server/app.py`, `npm run lint` and `npm run build` in `admin-frontend/`, `python -m pip install redis` plus a live Redis ping to `localhost:6379`, a backend functional probe covering admin create/list/update/delete, public package reads, Redis cache population and invalidation, `python -m compileall server`, and line-count checks confirming all touched source files remain below 400 lines.
- [x] Add premium header button to dashboard and editor - Added a premium button with a crown icon to both `header.dashboard` and `header.editor`, surfaced its visual state from the signed-in account premium flag, styled the non-premium state with a more muted gray appearance, and let anonymous users open the login dialog from that header control while keeping authenticated account states read-only. Validation passed with VS Code diagnostics on `frontend/src/components/AppShell/AppHeader.jsx`, `frontend/src/App.jsx`, and `frontend/src/App.css`, plus `npm run lint` and `npm run build` in `frontend/`, followed by a clean `npm run desktop:dev` restart with Electron running, port `5000` listening, and `/api/health` returning HTTP `200`.
- [x] Add premium account watermark gating - Added persisted `isPremium` account support across the MySQL auth schema, auth session payloads, and admin user update API, added premium status plus enable or disable controls to the React admin web user-management flow, and disabled the `G Studio` watermark in `panel.video-player` preview plus both native and fallback export paths whenever the signed-in account is premium. Also split FFmpeg export logging helpers into `frontend/src/utils/ffmpegExportLogging.js` so the touched `frontend/src/utils/ffmpegManager.js` returns below the 400-line guardrail. Validation passed with `python -m py_compile server/auth_store.py server/auth_identity.py server/auth_routes.py server/admin_store.py server/admin_routes.py`, `npm run lint` and `npm run build` in `admin-frontend/`, `npm run lint` and `npm run build` in `frontend/`, `python -m compileall server`, and final line-count checks confirming `frontend/src/utils/ffmpegManager.js` is now 357 lines.
- [x] Move backend admin web to React admin frontend - Added a standalone Vite React admin app in `admin-frontend/` with a dark desktop-app-matched theme, login, temporary-admin setup, user management, user detail, request history, credit history, add-credit dialog, account dropdown, and developer locator markers. Removed the old backend static admin source files from `server/static/admin/`, changed `server/admin_web_routes.py` to serve the built React app from `admin-frontend/dist`, and updated `MAP.md`. Validation passed with `npm run lint` and `npm run build` in `admin-frontend/`, `python -m py_compile server/app.py server/admin_web_routes.py`, `python -m compileall server`, `npm run build` in `frontend/`, line-count checks confirming changed source files are below 400 lines, a clean restart through `npm run desktop:dev`, active Electron processes, port `5000` listening, live HTTP `200` for `/admin/manage`, and live asset `200` for the hashed React bundle under `/admin/assets/`.
- [x] Adjust admin web header width and manage actions - Made the shared admin header render as a full-width band, raised the user dropdown above the management summary panel, removed the manage-summary refresh or logout buttons, and added a refresh action inside the header user menu while keeping logout there. Validation passed with `python -m py_compile server/app.py`, `node --check server/static/admin/header.js server/static/admin/manage.js`, VS Code diagnostics on the touched admin files, a clean restart through `npm run desktop:dev`, active Electron processes, port `5000` listening, HTTP `200` for `/admin/manage`, and live HTML checks confirming `data-admin-header-refresh="true"` is present while `id="refresh-button"` and `id="logout-button"` are absent.
- [x] Refine admin web detail spacing and account header - Restored the missing detail-page dashboard spacing so the summary panel no longer sticks to the requests section, added shared admin header chrome plus a user-icon account dropdown across the login, setup, management, and detail pages through new `server/static/admin/header.js` and `server/static/admin/header.css` files, and kept the larger existing admin assets untouched to stay inside the 400-line guardrail. Validation passed with `node --check server/static/admin/header.js server/static/admin/login.js server/static/admin/setup.js server/static/admin/manage.js server/static/admin/detail.js`, VS Code diagnostics on the touched admin HTML/CSS/JS files, `python -m py_compile server/app.py`, a clean restart through `npm run desktop:dev`, active Electron processes, port `5000` listening, and live `200` checks for `/admin/login`, `/admin/manage`, `/admin/assets/header.js`, and `/admin/assets/header.css`.
- [x] Refine admin manage layout and detail header - Separated the admin manage summary panel from the users section by reusing the admin dashboard spacing layout, added backend-backed user search by name or email on the admin manage page, removed inline role editing plus save actions from the manage table so it is now a read-only navigation list, and moved the current admin identity into the user-detail page header so the selected user summary stays distinct. Validation passed with `python -m py_compile server/admin_store.py server/admin_routes.py`, `node --check server/static/admin/manage.js server/static/admin/detail.js`, and VS Code diagnostics on the touched admin files.
- [x] Add isDeveloper markers to admin web - Moved the admin-web `isDeveloper` source of truth into `server/static/admin/shared.js` as a local JS constant, kept the query-string and stored overrides inside the same admin-web layer, and removed the backend-provided `/admin/assets/runtime-config.js` route plus the matching HTML script tags from the login, setup, manage, and user-detail pages so admin marker visibility is no longer derived from backend runtime state. Validation passed with `python -m py_compile server/admin_web_routes.py`, `node --check server/static/admin/shared.js server/static/admin/login.js server/static/admin/setup.js server/static/admin/manage.js server/static/admin/detail.js`, VS Code diagnostics on the touched admin files, and a repo search confirming the backend `runtime-config.js` and `adminWebRuntimeConfig` references were removed from `server/**`.
- [x] Add paginated admin user detail flow - Added a paginated backend user-management API, a dedicated backend-served admin user-detail page with per-user request history, paginated credit-history reads, and an add-credits dialog, removed the old recent-requests panel from browser user management, and split credit-ledger logic into `server/auth_credit_store.py` so the admin history feature fits under the 400-line guardrail. Validation passed with `python -m py_compile server/auth_store.py server/auth_credit_store.py`, `python -m py_compile server/admin_store.py server/request_store.py server/admin_routes.py server/admin_web_routes.py`, `python -m py_compile server/proxy_routes.py`, `node --check server/static/admin/shared.js`, `node --check server/static/admin/manage.js`, and `node --check server/static/admin/detail.js`, `python -m compileall server`, `npm run build` in `frontend/`, and a clean relaunch through `npm run desktop:start` followed by live `200` checks for `/api/health`, `/admin/manage`, and `/admin/users/test-user`.
- [x] Log bootstrap admin status to backend log file - Routed temporary-admin bootstrap messages through the shared backend logger so `logs/backend.log` now records either the generated temporary admin credentials when no persisted admin exists or the explicit skip reason when a real admin is already present. Validation passed with `python -m py_compile server/admin_bootstrap.py`, a targeted import probe through `server.app`, `python -m compileall server`, final clean `npm run desktop:start`, live `/api/health` 200, and `logs/backend.log` entries confirming temporary bootstrap is disabled because a persisted admin already exists in the current database.
- [x] Add hourly backend log files - Added backend file logging with an hourly rotating handler in `server/logging_setup.py`, writing UTF-8 log files into the root `logs/` directory and capturing Flask plus Werkzeug activity through the shared logger setup. Validation passed with `python -m py_compile server/app.py server/logging_setup.py server/admin_web_routes.py`, `python -m compileall server`, final clean `npm run desktop:start`, live `/api/health` 200, and a generated `logs/backend.log` containing the latest request entries.
- [x] Add backend-served admin web page - Added a real backend admin web flow served directly by Flask, split into `/admin/login`, `/admin/setup`, and `/admin/manage`, with `/admin` redirecting to login and `/console` redirecting to management. The browser flow now matches the requested sequence: login page, one-time username or password setup page for the temporary bootstrap admin, then the user-management page with role, credit, and recent-request controls, and the final pass stripped the extra guidance panels and descriptive copy so all three pages stay minimal. Validation passed with `python -m py_compile server/app.py server/admin_web_routes.py`, `node --check server/static/admin/admin.js server/static/admin/shared.js server/static/admin/login.js server/static/admin/setup.js server/static/admin/manage.js`, `python -m compileall server`, `npm run build`, final clean `npm run desktop:start`, and live checks confirming `/admin -> 302 -> /admin/login`, `/admin/login`, `/admin/setup`, `/admin/manage`, `/console`, and `/admin/assets/shared.js` all serve successfully.
- [x] Add backend admin console and user roles - Added persistent `user` and `admin` roles, username-based login for real accounts, a temporary in-memory bootstrap admin when no persisted admin exists, admin-only overview or user-management or request-list APIs, and the frontend admin console plus forced bootstrap setup form. Validation passed with a backend smoke test covering current-DB bootstrap disablement, temporary-admin bootstrap login or completion under a simulated no-admin condition, admin user listing or updates, admin request listing, and `/api/auth/me`, plus `python -m compileall server`, `npm run lint`, `npm run build`, final `npm run desktop:start`, and live `/api/health` 200 after restart.
- [x] Split app shell for admin flow - Extracted the shared app header and editor workspace out of `frontend/src/App.jsx` so the admin bootstrap and console states fit under the 400-line guardrail. Validation passed with `npm run lint`, `npm run build`, and final `npm run desktop:start`.
- [x] Split auth identity helpers for admin flow - Moved shared username normalization and public-user shaping helpers into `server/auth_identity.py` so `server/auth_routes.py` stayed under the 400-line guardrail while roles and temporary-admin support were added. Validation passed with the backend smoke test and `python -m compileall server`.
- [x] Add credit pricing for subtitle actions - Added MySQL-backed per-user credit balances with a default `AUTH_INITIAL_CREDITS` registration balance of 1000, charged subtitle generation, translation, and voiceover starts at 20/100/200 credits, returned updated `creditBalance` payloads from successful start responses, surfaced the current balance plus action costs in the client header and subtitle tools, and kept the renderer session in sync after each charged action. Validation passed with `python -m py_compile server/auth_store.py server/auth_routes.py server/proxy_routes.py server/proxy_route_helpers.py server/app.py`, a Flask smoke test covering register `1000 -> 980 -> 880 -> 680 -> 80`, `/api/auth/me` balance reads, and 402 insufficient-credit enforcement, `python -m compileall server`, `npm run lint`, `npm run build`, final `npm run desktop:start`, and live `/api/health` 200 after restart.
- [x] Split oversized proxy route helpers - Moved shared proxy request, fallback, owner-check, and response-shaping helpers into `server/proxy_route_helpers.py` so the credit work could land without leaving `server/proxy_routes.py` oversized; final line counts were `proxy_routes.py` 400 and `proxy_route_helpers.py` 130. Validation passed with `python -m compileall server`, `npm run lint`, and `npm run build`.
- [x] Make watermark wall bounce reflect angles - Replaced the watermark's pseudo-random post-collision angle selection with reflective wall and corner bounce math so the incoming and outgoing angles mirror like a normal wall bounce. Validation passed with `node --check frontend/src/utils/watermarkMotion.js`, a 30-second motion probe confirming 13 reflected collisions with no mismatches, `npm run lint`, `npm run build`, final `npm run desktop:start`, and live `/api/health` 200 after restart.
- [x] Make G Studio watermark bounce path - Replaced the fixed sine-loop watermark motion with shared deterministic straight-line bounce segments in `watermarkMotion.js`; the `G Studio` watermark now moves in a line, picks a new trajectory at every edge collision, and stays continuous in preview, fallback canvas export, and native FFmpeg export. Native export now builds piecewise `drawtext` x/y expressions from the same path segments with chunk duration and time offsets, avoiding repeated `sin()` loops. Validation passed with VS Code diagnostics, `node --check frontend/src/utils/watermarkMotion.js frontend/electron/export/frameFilterGraph.mjs frontend/electron/export/framePipeline.mjs`, a motion sample probe confirming all sampled points stay in bounds with 12 direction changes over 30 seconds, a native FFmpeg probe confirming no `sin(` remains and 10 test frames render successfully, `npm run lint`, `npm run build`, final `npm run desktop:start` rebuild in 138ms, and live `/api/health` 200 after restart.
- [x] Add animated G Studio watermark - Added a moving `G Studio` text watermark to the shared preview canvas, fallback canvas export, and native fast FFmpeg export, keeping it below subtitle overlays and continuous across native export chunks through chunk time offsets. Split the player preview media stage into `VideoPlayerPreviewStage.jsx` so the touched `VideoPlayer.jsx` stays under the 400-line guardrail, and updated `MAP.md`. Validation passed with VS Code diagnostics, `node --check frontend/electron/export/frameFilterGraph.mjs frontend/electron/export/framePipeline.mjs`, native FFmpeg drawtext probe, native filter ordering probe confirming watermark renders below subtitle overlays, `npm run lint`, `npm run build`, final `npm run desktop:start` rebuild in 147ms, and live `/api/health` 200 after the desktop/backend restart.
- [x] Attach user id to all server requests - Added the generic MySQL `server_requests` table plus `user_id` on translation fallback jobs, records transcription, translation, and voiceover requests with the authenticated user id, protects owned status/download routes, sends bearer headers from the renderer, gates translation behind login, and now syncs request status from downstream polling so completed transcription, translation, voiceover, and local fallback jobs move from `running` to `success` in `audio_studio.server_requests`. Validation passed with VS Code diagnostics, `python -m py_compile server/request_store.py server/translation_fallback.py server/proxy_routes.py server/app.py`, `python -m compileall server`, MySQL schema checks for `server_requests.user_id` and `request_translation_jobs.user_id`, Flask smoke tests for per-user ownership and voiceover download ownership, DB status-sync smoke tests for transcription/translation/voiceover/local fallback `running -> success`, `npm run lint`, `npm run build`, final `npm run desktop:start` rebuild in 156ms, live `/api/health` 200, and the restarted desktop/backend runtime confirmed.
- [x] Store server request jobs in MySQL - Added `server/request_store.py` as MySQL-backed persistence for server-managed request/job state, moved local translation fallback metadata and translated SRT output out of `server/uploads/translation-jobs` writes and into the `request_translation_jobs` table, kept the translation start/status/download API contract unchanged, and now serves local translation downloads from DB content. Validation passed with VS Code diagnostics, `python -m py_compile server/request_store.py server/translation_fallback.py server/proxy_routes.py server/app.py`, local request-store schema/table smoke checks, DB-backed helper and Flask route status/download checks with no new translation files created, `python -m compileall server`, `npm run build`, final `npm run desktop:start` rebuild in 120ms, live `/api/health` 200, live translation status/download DB checks, and active Electron runtime processes confirmed.
- [x] Store auth data in MySQL - Added `server/auth_store.py` as the MySQL-backed auth persistence layer for `auth_users` and `auth_refresh_tokens`, changed backend defaults to `localhost:3306/root/12345678/audio_studio`, provisioned the Docker `mysql_server` grants for `root@%` and `root@172.19.0.1`, kept route response shapes unchanged, and declared `PyMySQL` plus `cryptography` dependencies. Validation passed with VS Code diagnostics, host PyMySQL connection to `audio_studio`, schema/table checks, MySQL-backed Flask register/duplicate/me/login/refresh rotation/logout/demo-login smoke test, `python -m py_compile server/auth_store.py server/auth_routes.py server/app.py`, `python -m compileall server`, `npm run build` in 116ms, final `npm run desktop:start` rebuild in 117ms, live `/api/health` 200, live register/login/refresh/logout checks, live protected transcription and voiceover 401 checks, live authorized no-file 400 checks, and active Electron runtime processes confirmed.
- [x] Require login for subtitle and voiceover generation - Added a reusable Flask access-token guard, protected `/api/transcription/start` and `/api/voiceover/start`, sent the saved bearer token from the renderer for those two start requests, and made the subtitle panel open the login dialog before subtitle or voiceover generation when anonymous while keeping translation unchanged. Validation passed with VS Code diagnostics, `python -m py_compile server/auth_routes.py server/proxy_routes.py server/app.py`, Flask test-client protected-route smoke checks for 401 and authorized file validation, `python -m compileall server`, `npm run lint`, `npm run build` in 124ms, final `npm run desktop:start` rebuild in 129ms, live `/api/health` 200, live protected-route 401 checks, live authorized no-file 400 checks, and active Electron runtime processes confirmed.
- [x] Add optional account registration - Added `/api/auth/register` with persistent JSON-backed local accounts under the server uploads area, PBKDF2 password hashing, duplicate-email checks, and automatic JWT token-pair issuance; the renderer auth client and session hook now support registration, and the existing auth popup has login/register modes without making authentication required. Validation passed with VS Code diagnostics, `python -m py_compile server/auth_routes.py server/app.py`, temp-store Flask register/login/me/refresh/logout smoke test, `python -m compileall server`, `npm run lint`, `npm run build` in 117ms, final `npm run desktop:start` rebuild in 118ms, live `/api/health` 200, live demo login/me check, live register-route duplicate check returning 409, and active Electron runtime processes confirmed.
- [x] Add optional JWT login flow - Added a non-blocking header login action and popup login UI with developer locator markers, localStorage-backed renderer auth state, automatic access-token refresh via rotating refresh tokens, logout, and Flask auth endpoints for login, refresh, current-user lookup, and refresh-token revocation. Validation passed with VS Code diagnostics, `python -m py_compile server/app.py server/auth_routes.py`, Flask test-client auth smoke test, `python -m compileall server`, `npm run lint`, `npm run build` in 116ms, final `npm run desktop:start` rebuild in 125ms, live `/api/health` 200, live auth login/me/refresh/logout token-rotation smoke test, and active Electron runtime processes confirmed.
- [x] Stabilize native-fast zoom export without slowing build - Native frame exports now split timeline work on detected-frame-rate boundaries instead of arbitrary floating-second chunk starts, preserve higher FFmpeg seek/duration precision, and compose only motion scenes through a 4:4:4 intermediate path so animated zoom overlay positions are not snapped by 4:2:0 chroma subsampling before final H.264 output. Dynamic zoom sizing now preserves source aspect through FFmpeg's derived height path while keeping native chunk workers and NVENC active. Validation passed with VS Code diagnostics, `node --check`, line-count guardrail checks, an automated native FFmpeg/NVENC zoom probe showing 3 frame-aligned chunks at 29.97fps and no renderer fallback, an automated boundary jitter probe with no visible chunk-boundary spike, `npm run lint`, `npm run build` in 122ms, final `npm run desktop:start` rebuild in 117ms, Electron runtime restart, Flask `/api/health` 200, renderer load, and debug log confirmation of the restarted desktop/backend lifecycle.
- [x] Restore native-fast export speed for scene motion - Removed the renderer-compositor opt-out from `frontend/src/utils/nativeExportClient.js` after logs showed scene-motion export entering `Start record-frame compositor from preview renderer`, which runs near realtime and made export/build much slower than the previous native-fast flow. Scene-motion exports now route to native-fast again. Validation passed with a route test showing `motionRoute=native-fast`, VS Code diagnostics, `node --check`, `npm run lint`, `npm run build`, clean desktop restart, renderer load, and Flask `/api/health` 200.
- [x] Preserve export source cadence for zoom motion - Native export now probes the merged source-video frame rate and uses that detected FPS for scene-motion filter cadence, generated background cadence, and chunk encoder CFR output instead of forcing every export to 30fps; this keeps zoom animation cadence aligned with `panel.video-player` playback for 24/25/30fps sources. Validation passed with VS Code diagnostics, line-count checks, `node --check`, a 25fps FFmpeg-static probe verifying detected FPS 25 and filter output 25fps without `fps=30`, `npm run lint`, `npm run build`, final `npm run desktop:start` with Flask health 200, and clean post-restart debug logs.
- [x] Split native frame pipeline filter graph - Moved native frame filter graph construction, background composition, subtitle overlay filter wiring, and the shared native frame-rate constant into `frontend/electron/export/frameFilterGraph.mjs`, reducing `framePipeline.mjs` to 364 lines while keeping the new file at 90 lines. Validation passed with VS Code diagnostics, line-count checks, `node --check`, exact native FFmpeg filter probe, `npm run lint`, `npm run build`, and final desktop restart with Flask health 200 plus active Electron processes confirmed.
- [x] Eliminate residual zoom export jitter - Stabilized native export zoom animation further by normalizing source video to constant 30fps before scene-motion zoom math, resetting final filtered video PTS, and forcing encoded chunks to CFR 30fps so zoom animation does not inherit irregular source timestamps or chunk cadence. Validation passed with VS Code diagnostics, `node --check`, an exact `buildFrameFilter` FFmpeg-static probe showing no `undefined` filter values and 30fps output, `npm run lint`, `npm run build`, final `npm run desktop:start` with Flask health 200, and clean post-restart debug logs.
- [x] Stabilize zoom animation export - Removed subtle native export jitter in animated zoom scenes by changing motion segment matching to half-open time ranges, rounding dynamic scaled dimensions to the nearest even pixel instead of always truncating, enabling accurate FFmpeg scaling flags, and snapping zoom overlay x/y positions to whole pixels before clipping into the original video box. Validation passed with VS Code diagnostics, `node --check`, a 4-second native FFmpeg video-fade zoom probe, `npm run lint`, `npm run build`, final `npm run desktop:start` with Flask health 200, and clean post-restart debug logs.
- [x] Split native export coordinator helpers - Split source materialization, overlay/background asset writing, frame preset resolution, progress/debug logging, renderer-detach tracking, and locked job-directory cleanup into `frontend/electron/export/nativeExportJobHelpers.mjs`, reducing `exportCoordinator.mjs` to 200 lines and keeping the helper at 273 lines. Validation passed with VS Code diagnostics, line-count checks, `node --check`, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Fix native export cleanup lock - Native export now retries Windows `EBUSY` or `EPERM` temp job-directory cleanup and logs a warning instead of letting cleanup failure throw through IPC; the helper split keeps the coordinator under the 400-line guardrail. Validation passed with VS Code diagnostics, `node --check`, `npm run lint`, `npm run build`, final desktop restart with Flask health 200, and no fresh post-restart export errors in the desktop debug log.
- [x] Crop zoom motion to original video bounds - Preview and fallback canvas rendering clip zoomed foreground video to the original fit-contain video box, and native FFmpeg now renders zoom into that original video box with overlay clipping instead of dynamic crop expressions, avoiding the Windows `3221225477` crash seen in older logs. Validation passed with VS Code diagnostics, native FFmpeg 3-second video-fade zoom probe, `node --check`, `npm run lint`, `npm run build`, final desktop restart with Flask health 200, and clean post-restart debug log checks.
- [x] Increase scene face detection sensitivity - Improved scene face detection by sampling multiple higher-resolution frames across each scene, retrying browser `FaceDetector` with fast, accurate, and enhanced high-contrast passes, selecting the largest detected face across all sampled frames, and keeping center fallback only after all sensitivity passes fail. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Persist bulk config and audio controls - Saved and restored the scene bulk rules opened from `panel.scene-list.bulk-config.launcher` by lifting them into project-scoped editor state, persisted `panel.video-player.audio-controls` audio mix values with the project metadata, restored those values on project load, kept voiceover audio mix matching stable through stored voiceover filenames, split derived voiceover state into `useVideoEditorVoiceoverState.js` to keep `useVideoEditor.js` under the 400-line guardrail, and updated `MAP.md`. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Make bulk groups segmented - Restyled the left-nav bulk `Match` and `Case` controls as attached segmented groups with no internal gaps, shared outer border, rounded outer corners only, and divider lines between label, select, and action segments. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Group bulk match and case controls - Restructured the left-nav bulk rule UI so `Match` is a grouped control with label, operator select, and add-condition button, and each `Case` is a grouped control with label, case select, and delete button while keeping duration seconds as a secondary field. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load and active Electron processes confirmed.
- [x] Add AND OR bulk scene conditions - Extended each bulk motion action rule to support multiple conditions with `AND` or `OR` matching, kept compatibility with older single-condition rules, added add/remove condition controls in the left-nav rule builder, and changed the apply status to show exactly how many scenes were applied versus total candidates. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Move scene bulk config to left nav - Replaced the inline scene-list bulk config with a scene-list `Quick config` launcher button, moved the rule builder into the player left-nav as the `scene-bulk` section, passed kept scenes and the bulk apply callback through the sidebar plumbing, renamed developer locator codes for the left-nav location, and kept touched files under 400 lines. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Add scene-list bulk motion config - Added a quick scene config rule builder inside the scene-list panel where each addable condition can target face detected, no detected face, duration greater than X seconds, or duration less than X seconds, with per-rule zoom action mode, ratio, and target behavior; bulk apply updates matching kept scenes with face detection and center fallback. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Add selectable scene zoom modes - Replaced the scene motion checkbox with a zoom-mode select for none, static zoom-in, animated zoom-out, and animated zoom-in; preserved saved-project compatibility; applied the modes across preview, fallback export, and native export; and defaulted face targeting to scene center when detection is unavailable or finds no face. Validation passed with `npm run lint`, `npm run build`, scene-motion math probes, native FFmpeg filter probes for all zoom modes, VS Code diagnostics, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Add per-scene face zoom motion config - Added scene-card config entrypoints, a left-nav scene motion panel with face-detect/manual target controls and configurable zoom ratio, preview/fallback/native export scene-motion rendering, Electron FaceDetector runtime flag, helper splits to keep touched files under 400 lines, and map updates; validation passed with `npm run lint`, `npm run build`, native FFmpeg filter probes, VS Code diagnostics, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Fix native export subtitle and voiceover parity - Native export now sends explicit frame dimensions through IPC, resolves/logs the main-process frame size, and overwrites final mux output deterministically; validation passed with `npm run lint`, `npm run build`, and `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
