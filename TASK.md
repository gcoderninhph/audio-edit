# TASK

## Active
- [ ] Find exact residual native export zoom jitter cause
	- Scope: frontend native export
	- Owner files: `frontend/electron/export/frameMotionFilter.mjs`, `frontend/electron/export/frameFilterGraph.mjs`, `frontend/electron/export/framePipeline.mjs`, `frontend/src/utils/frameCanvasRenderer.js`, `frontend/src/utils/sceneMotion.js`
	- Plan: compare preview motion math against native FFmpeg motion expressions, build automated probes that expose frame-to-frame shake in exported zoom frames, fix the root native-export mismatch, then validate with diagnostics, syntax checks, targeted FFmpeg probes, lint, build, and desktop restart.
	- Validation: pending
## Backlog
## Completed
- [x] Preserve export source cadence for zoom motion - Native export now probes the merged source-video frame rate and uses that detected FPS for scene-motion filter cadence, generated background cadence, and chunk encoder CFR output instead of forcing every export to 30fps; this keeps zoom animation cadence aligned with `panel.video-player` playback for 24/25/30fps sources. Validation passed with VS Code diagnostics, line-count checks, `node --check`, a 25fps FFmpeg-static probe verifying detected FPS 25 and filter output 25fps without `fps=30`, `npm run lint`, `npm run build`, final `npm run desktop:start` with Flask health 200, and clean post-restart debug logs.
- [x] Split native frame pipeline filter graph - Moved native frame filter graph construction, background composition, subtitle overlay filter wiring, and the shared native frame-rate constant into `frontend/electron/export/frameFilterGraph.mjs`, reducing `framePipeline.mjs` to 364 lines while keeping the new file at 90 lines. Validation passed with VS Code diagnostics, line-count checks, `node --check`, exact native FFmpeg filter probe, `npm run lint`, `npm run build`, and final desktop restart with Flask health 200 plus active Electron processes confirmed.
- [x] Eliminate residual zoom export jitter - Stabilized native export zoom animation further by normalizing source video to constant 30fps before scene-motion zoom math, resetting final filtered video PTS, and forcing encoded chunks to CFR 30fps so zoom animation does not inherit irregular source timestamps or chunk cadence. Validation passed with VS Code diagnostics, `node --check`, an exact `buildFrameFilter` FFmpeg-static probe showing no `undefined` filter values and 30fps output, `npm run lint`, `npm run build`, final `npm run desktop:start` with Flask health 200, and clean post-restart debug logs.
- [x] Stabilize zoom animation export - Removed subtle native export jitter in animated zoom scenes by changing motion segment matching to half-open time ranges, rounding dynamic scaled dimensions to the nearest even pixel instead of always truncating, enabling accurate FFmpeg scaling flags, and snapping zoom overlay x/y positions to whole pixels before clipping into the original video box. Validation passed with VS Code diagnostics, `node --check`, a 4-second native FFmpeg video-fade zoom probe, `npm run lint`, `npm run build`, final `npm run desktop:start` with Flask health 200, and clean post-restart debug logs.
- [x] Split native export coordinator helpers - Split source materialization, overlay/background asset writing, frame preset resolution, progress/debug logging, renderer-detach tracking, and locked job-directory cleanup into `frontend/electron/export/nativeExportJobHelpers.mjs`, reducing `exportCoordinator.mjs` to 200 lines and keeping the helper at 273 lines. Validation passed with VS Code diagnostics, line-count checks, `node --check`, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Fix native export cleanup lock - Native export now retries Windows `EBUSY` or `EPERM` temp job-directory cleanup and logs a warning instead of letting cleanup failure throw through IPC; the helper split keeps the coordinator under the 400-line guardrail. Validation passed with VS Code diagnostics, `node --check`, `npm run lint`, `npm run build`, final desktop restart with Flask health 200, and no fresh post-restart export errors in the desktop debug log.
- [x] Crop zoom motion to original video bounds - Preview and fallback canvas rendering clip zoomed foreground video to the original fit-contain video box, and native FFmpeg now renders zoom into that original video box with overlay clipping instead of dynamic crop expressions, avoiding the Windows `3221225477` crash seen in older logs. Validation passed with VS Code diagnostics, native FFmpeg 3-second video-fade zoom probe, `node --check`, `npm run lint`, `npm run build`, final desktop restart with Flask health 200, and clean post-restart debug log checks.
- [x] Increase scene face detection sensitivity - Improved scene face detection by sampling multiple higher-resolution frames across each scene, retrying browser `FaceDetector` with fast, accurate, and enhanced high-contrast passes, selecting the largest detected face across all sampled frames, and keeping center fallback only after all sensitivity passes fail. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Persist bulk config and audio controls - Saved and restored the scene bulk rules opened from `panel.scene-list.bulk-config.launcher` by lifting them into project-scoped editor state, persisted `panel.video-player.audio-controls` audio mix values with the project metadata, restored those values on project load, kept voiceover audio mix matching stable through stored voiceover filenames, split derived voiceover state into `useVideoEditorVoiceoverState.js` to keep `useVideoEditor.js` under the 400-line guardrail, and updated `MAP.md`. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Make bulk groups segmented - Restyled the left-nav bulk `Match` and `Case` controls as attached segmented groups with no internal gaps, shared outer border, rounded outer corners only, and divider lines between label, select, and action segments. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus active Electron processes confirmed.
- [x] Group bulk match and case controls - Restructured the left-nav bulk rule UI so `Match` is a grouped control with label, operator select, and add-condition button, and each `Case` is a grouped control with label, case select, and delete button while keeping duration seconds as a secondary field. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load and active Electron processes confirmed.
- [x] Add AND OR bulk scene conditions - Extended each bulk motion action rule to support multiple conditions with `AND` or `OR` matching, kept compatibility with older single-condition rules, added add/remove condition controls in the left-nav rule builder, and changed the apply status to show exactly how many scenes were applied versus total candidates. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Move scene bulk config to left nav - Replaced the inline scene-list bulk config with a scene-list `Quick config` launcher button, moved the rule builder into the player left-nav as the `scene-bulk` section, passed kept scenes and the bulk apply callback through the sidebar plumbing, renamed developer locator codes for the left-nav location, and kept touched files under 400 lines. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Add scene-list bulk motion config - Added a quick scene config rule builder inside the scene-list panel where each addable condition can target face detected, no detected face, duration greater than X seconds, or duration less than X seconds, with per-rule zoom action mode, ratio, and target behavior; bulk apply updates matching kept scenes with face detection and center fallback. Validation passed with VS Code diagnostics, line-count guardrail checks, `npm run lint`, `npm run build`, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Add selectable scene zoom modes - Replaced the scene motion checkbox with a zoom-mode select for none, static zoom-in, animated zoom-out, and animated zoom-in; preserved saved-project compatibility; applied the modes across preview, fallback export, and native export; and defaulted face targeting to scene center when detection is unavailable or finds no face. Validation passed with `npm run lint`, `npm run build`, scene-motion math probes, native FFmpeg filter probes for all zoom modes, VS Code diagnostics, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Add per-scene face zoom motion config - Added scene-card config entrypoints, a left-nav scene motion panel with face-detect/manual target controls and configurable zoom ratio, preview/fallback/native export scene-motion rendering, Electron FaceDetector runtime flag, helper splits to keep touched files under 400 lines, and map updates; validation passed with `npm run lint`, `npm run build`, native FFmpeg filter probes, VS Code diagnostics, and final `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
- [x] Fix native export subtitle and voiceover parity - Native export now sends explicit frame dimensions through IPC, resolves/logs the main-process frame size, and overwrites final mux output deterministically; validation passed with `npm run lint`, `npm run build`, and `npm run desktop:start` with Flask health 200 plus renderer load confirmed.
