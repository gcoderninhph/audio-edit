# Repository Map

## .github
- `.github/agents/coder.agent.md` - Defines the workspace coding agent workflow, including planning, validation, `TASK.md`, `MAP.md`, and 400-line guardrails.
- `.github/roles/map.role.md` - Documents how to create and update `MAP.md` as the repository query map.
- `.github/roles/task.role.md` - Documents how to create and update `TASK.md` as the persistent task tracker.

## frontend
- `frontend/index.html` - Hosts the Vite root element for the React application.
- `frontend/package.json` - Declares React renderer scripts, Electron desktop launch commands, and frontend runtime or build dependencies; desktop dev now rebuilds `dist/` in watch mode and launches Electron without opening a renderer web port.
- `frontend/README.md` - Describes the Electron-first frontend workflow, the desktop-protocol renderer runtime with no client web port, local desktop persistence, and Flask subtitle-service environment variables.
- `frontend/eslint.config.js` - Configures frontend lint rules.
- `frontend/vite.config.js` - Configures the Vite frontend build and dev server.
- `frontend/electron/debugLog.mjs` - Writes structured JSONL desktop debug logs under the Electron user data directory, rotates the current log file when it grows too large, and exposes IPC handlers for writing or locating export-debug traces.
- `frontend/electron/main.mjs` - Starts the Electron desktop shell, enables Chromium APIs needed by renderer face detection, serves the built renderer through `desktop://app` when no external dev URL is injected, manages the local Flask runtime, writes desktop or backend lifecycle events to the structured debug log file, registers both native export IPC and export-output IPC for choosing folders or revealing saved files, and defines the desktop `isDeveloper` toggle used to reveal UI locator markers.
- `frontend/electron/preload.mjs` - Exposes desktop runtime configuration, the `isDeveloper` UI-marker flag, debug-log IPC methods, native export IPC methods, export-output IPC methods for default folder, directory picking, save-to-file, and reveal actions, plus local project storage IPC methods including project-scoped voiceover save and restore calls, to the React renderer through a safe preload bridge.
- `frontend/electron/projectMediaProtocol.mjs` - Serves both renderer assets and stored project videos over the `desktop://app` Electron protocol, including byte-range video responses plus cross-origin resource headers so restored desktop videos can load inside the frame-preview canvas runtime without a local renderer HTTP server.
- `frontend/electron/projectStore.mjs` - Persists project metadata, creation timestamps, output-frame settings, subtitle layout and appearance settings, export-quality profile selection, preview/export audio mix settings, scene-list bulk motion rules, source videos, subtitle-track metadata, and the top-level compatibility fields for saved voiceover audio under the workspace-local `projects/` folder during desktop development, delegates voiceover manifest or migration helpers to `projectVoiceoverStore.mjs`, emits `desktop://app/project-media/...` URLs for dashboard previews and editor playback, and retries local project deletion when Windows still holds a transient file lock.
- `frontend/electron/projectSubtitleStore.mjs` - Saves each subtitle language track into its own JSON file inside the project subtitle directory, removes stale language files after autosave, and hydrates the stored track files back into project loads.
- `frontend/electron/projectStoreShared.mjs` - Centralizes Electron project-store root selection, filename builders, payload normalization, directory creation, and project metadata read or write helpers, including the in-project `voiceovers/` directory layout plus legacy sibling or root voiceover path fallbacks, so the main project store can stay below the 400-line guardrail.
- `frontend/electron/projectVoiceoverStore.mjs` - Normalizes the project voiceover manifest keyed by language, resolves the current voiceover track from compatibility metadata, and migrates older sibling-folder voiceover files into the new in-project `voiceovers/` layout while updating project metadata.
- `frontend/electron/export/exportCoordinator.mjs` - Coordinates the native fast export job in Electron main: resolves the chosen output folder plus file name into a final local MP4 path, delegates native job input, asset, progress, and cleanup helpers to `nativeExportJobHelpers.mjs`, forwards the selected export-quality profile into native framing, merges kept scenes, remuxes the final audio mix using the configured video and narration volumes when needed, and returns saved-file metadata instead of loading the full output back into renderer RAM.
- `frontend/electron/export/exportAudioStage.mjs` - Runs the native FFmpeg audio-stage helper that renders video-only, voiceover-only, or mixed export audio tracks before the final native remux so `exportCoordinator.mjs` stays under the 400-line guardrail.
- `frontend/electron/export/exportOutputIpc.mjs` - Owns Electron main IPC helpers for export output targets: default download-directory lookup, directory picker, safe MP4 filename resolution, local-file byte writes for fallback export, and reveal-in-folder actions for completed exports.
- `frontend/electron/export/frameFilterGraph.mjs` - Builds the native FFmpeg frame filter graph for each export chunk, normalizing source video to the detected merged-video frame rate before scene-motion zoom math, using a 4:4:4 intermediate compositor only for motion scenes to avoid chroma-subsampled overlay snap, composing preset color, image, or video-fade backgrounds, applying subtitle overlays, and resetting final video PTS.
- `frontend/electron/export/framePipeline.mjs` - Splits the native frame-render stage into adaptive frame-grid-aligned timeline chunks, detects the merged-video frame rate for export cadence parity with preview playback, delegates chunk filter graph construction to `frameFilterGraph.mjs`, runs several FFmpeg framing workers in parallel, aggregates chunk progress back to the renderer, and concatenates the finished framed chunks into either a job-local temp file or the final caller-supplied output MP4 path.
- `frontend/electron/export/frameMotionFilter.mjs` - Builds the native FFmpeg foreground scale, aspect-stable dynamic zoom sizing, pixel-stabilized original-video-box clipping, and overlay expressions for per-scene zoom modes, including static zoom-in, animated zoom-out, animated zoom-in, and chunk-local motion segment slicing.
- `frontend/electron/export/nativeExportJobHelpers.mjs` - Owns native export job helpers split from `exportCoordinator.mjs`, including source materialization, subtitle overlay and image-background asset writes, frame preset resolution, progress/debug logging, renderer-detach tracking, and retrying locked job-directory cleanup on Windows.
- `frontend/electron/export/nativeFfmpeg.mjs` - Resolves the bundled `ffmpeg-static` binary, probes available hardware encoders and source video frame rate, derives aggressive adaptive scene and frame worker plans from host CPU capacity plus timeline size, maps the selected export-quality profile onto hardware or CPU encoder arguments, and runs native FFmpeg child processes while capturing stderr and progress output.
- `frontend/electron/export/scenePipeline.mjs` - Splits kept-scene extraction and concat into a parallel native FFmpeg pipeline with adaptive bounded worker concurrency for CPU-heavy export stages.
- `frontend/electron/subtitleFont.mjs` - Resolves a usable desktop font file from the host OS and exposes it over IPC so the FFmpeg subtitle burn-in path can render text inside the Electron runtime.
- `frontend/src/main.jsx` - Boots the React app into the DOM.
- `frontend/src/App.jsx` - Composes the dashboard and editor UI around the `useVideoEditor` hook, keeps both the timeline and export panels visible whenever a video is loaded instead of waiting for scene detection to finish, passes subtitle translation plus Vbee voiceover actions into the subtitle panel, threads the attached voiceover track plus shared subtitle layout settings plus grouped export config or result state into both the preview player and `panel.export`, passes the active subtitle language selector into the subtitle panel, owns the shared left-nav section state so timeline voiceover, subtitle, export-config, scene-card config, or scene-list quick-config clicks can open the matching player config directly, and preserves the active editor session when the user returns to the dashboard during an in-flight background job.
- `frontend/src/App.css` - Styles the top-level app shell and shared layout states.
- `frontend/src/index.css` - Defines global theme tokens, resets, and shared utility styles.
- `frontend/src/hooks/useEditorPersistence.js` - Handles desktop-local project persistence, lightweight source video restore, autosave, output-frame setting restore, restores any saved project voiceover track plus subtitle layout, subtitle appearance, export-quality profile settings, audio mix settings, scene-list bulk motion rules, subtitle track manifest, and active subtitle language alongside the project session, delegates restore or resume helpers to a dedicated module, and cleans up the active project before local deletion.
- `frontend/src/hooks/useEditorPersistenceRestore.js` - Owns the restore-side helpers for project video, saved voiceover, voiceover language assignment, and in-flight subtitle job resume so `useEditorPersistence.js` stays under the 400-line guardrail while persistence behavior grows.
- `frontend/src/hooks/editorPersistenceJobRestore.js` - Restores saved transcription and translation jobs on project load, keeps subtitle-job progress tied to the active session, and clears only confirmed stale job markers.
- `frontend/src/hooks/useVideoEditor.js` - Orchestrates editor state, detection, subtitle actions, and delegates persistence, subtitle-track state, scene-motion configuration, derived voiceover state, extracted subtitle action callbacks, plus frame-aware export concerns to dedicated hooks and helpers, including the source video duration needed by pre-cut export fallback, the attached voiceover track state, active-language filtering, kept-timeline subtitle mapping, and grouped export config or result objects surfaced by `useFrameExport`.
- `frontend/src/hooks/useSceneMotionConfig.js` - Owns editor-side per-scene motion updates, project-scoped bulk scene-list motion rule state and application with AND/OR condition groups, and sensitive face-detection actions that sample multiple frames across each scene, store detected focus targets, and fall back to the center target when detection is unavailable or no face is found.
- `frontend/src/hooks/useVideoEditorVoiceoverState.js` - Derives the active-language voiceover track, display name, and kept-timeline voiceover subtitles so `useVideoEditor.js` stays below the 400-line guardrail.
- `frontend/src/hooks/useVideoEditorSubtitleActions.js` - Owns the editor-side subtitle action callbacks for transcription, translation, voiceover generation, and inline subtitle edits, forwarding the active subtitle language into narration generation so `useVideoEditor.js` stays under the 400-line guardrail while subtitle-track behavior grows.
- `frontend/src/hooks/useSubtitleTracks.js` - Keeps the in-memory subtitle track collection, active display language, visible subtitle list, and per-track edit updates separate from the top-level editor hook.
- `frontend/src/hooks/subtitleJobActions.js` - Runs the editor-side transcription, translation, and Vbee voiceover job flows with session guards so `useVideoEditor.js` can stay below the 400-line guardrail while reusing the same progress and autosave wiring, now saves generated voiceover audio bytes into the current desktop project with a language key before attaching the in-app track, and currently blocks narration generation for unsupported non-Vietnamese subtitle languages.
- `frontend/src/hooks/useFrameExport.js` - Owns the editable output-frame settings, shared subtitle layout and appearance settings, the shared export-quality profile, export output file name plus folder state, the persisted source-video and voiceover audio-mix state used by both preview and export, export lifecycle state, saved-file export result state, export diagnostics state, and the frame-aware export entrypoint shared by the player preview and export panel, including scene-motion settings in export freshness checks.
- `frontend/src/hooks/useUndoHistory.js` - Tracks undo and redo snapshots for editor state changes.
- `frontend/src/utils/audioExtractor.js` - Extracts audio with FFmpeg, materializes stored project video only when transcription starts, and exposes transcription job snapshot helpers used to restore or clear saved jobs without flashing stale progress.
- `frontend/src/utils/desktopLogger.js` - Wraps the Electron debug-log bridge so renderer export flows can persist structured diagnostics to the desktop JSONL log file.
- `frontend/src/utils/editorSelectors.js` - Provides pure selectors for the current scene and subtitle visibility after scene deletions.
- `frontend/src/utils/exportAudioMix.js` - Defines the shared export-audio helpers for normalizing preview/export mix state, computing kept-timeline duration, and building FFmpeg argument sets for mixed, voiceover-only, video-only, or final-mux audio stages, including optional final video encode settings for the fallback mux pass.
- `frontend/src/utils/exportQualityProfile.js` - Defines the shared export-quality profile presets, including the extra-low `Draft smallest` option, plus normalization helpers, fallback mux settings, and native encoder argument mapping used by the export-config sidebar, project persistence, and both export backends.
- `frontend/src/utils/subtitleTracks.js` - Defines the shared subtitle-track language options, active-language normalization, voiceover-language helpers, and helpers for reading or updating the dedicated `original` track plus translated language tracks without overwriting each other.
- `frontend/src/utils/exportOutputTarget.js` - Wraps the preload export-output bridge for the renderer, building default export file names, user-facing folder or file labels, default export directories, directory-picking flows, fallback save-to-file writes, and reveal-saved-file actions.
- `frontend/src/utils/exportAudioStage.js` - Materializes the saved voiceover blob and runs the FFmpeg.wasm fallback audio-stage helper so `ffmpegManager.js` can render the configured export mix without exceeding the 400-line guardrail.
- `frontend/src/utils/ffmpegManager.js` - Tries the native fast export backend first for both color-backed and image-backed frames, then falls back to the FFmpeg.wasm path when native export is unavailable; the fallback path mounts source files through `WORKERFS`, delegates canvas frame recording to `fallbackFrameRecorder.js`, applies export-quality and audio-mix settings, and saves fallback bytes into the chosen local export target when configured.
- `frontend/src/utils/fallbackFrameRecorder.js` - Records the FFmpeg.wasm fallback canvas-composited video from `cut.mp4`, applying frame settings, subtitle settings, export-quality bitrate, and scene-motion segments before final muxing.
- `frontend/src/utils/ffmpegSceneMerge.js` - Re-encodes multiple kept scenes into temporary segment files and concatenates them via a manifest so export avoids the oversized `filter_complex` graph that previously stalled in FFmpeg.wasm.
- `frontend/src/utils/frameComposer.js` - Defines shared output-frame presets, color, image, or video-fade background models, including preset-driven fade configuration plus background serialization and subtitle timing helpers used by the preview frame, persistence, and export compositor.
- `frontend/src/utils/frameCanvasRenderer.js` - Draws preset color, cover-image, or blurred video-derived fade backgrounds plus scene-motion-aware foreground video clipped to the original fit-contain video box and active subtitle cards into a canvas, honors shared subtitle appearance settings, and exposes subtitle-card layout helpers so native export overlay assets reuse the same subtitle geometry as the preview.
- `frontend/src/utils/frameCanvasExport.js` - Records the shared canvas frame renderer, including selected image backgrounds, scene-motion segments, and shared subtitle layout settings, as a video stream during fallback export with configurable recording bitrate.
- `frontend/src/utils/nativeExportClient.js` - Builds the renderer-side native export request, including image-backed frame background payloads, per-scene motion config, shared subtitle layout settings, the selected export-quality profile, the chosen output file target, configured export audio mix, and optional saved voiceover payload, then accepts either saved-file metadata for direct-to-disk native exports or raw bytes for older blob-style results.
- `frontend/src/utils/faceDetection.js` - Wraps the browser `FaceDetector` API with fast, accurate, and enhanced-image retry passes for detecting the largest face across one or more captured scene frames and returning a normalized focus target for scene motion.
- `frontend/src/utils/sceneMotion.js` - Normalizes selectable per-scene zoom modes, serializes scene motion for export freshness, computes preview/fallback render state, and builds kept-timeline motion segments for export.
- `frontend/src/utils/sceneMotionBulkConfig.js` - Defines scene-list bulk motion rule conditions, AND/OR operators, action targets, normalization, duration matching, and action-to-motion config helpers used by the bulk scene config UI and hook.
- `frontend/src/utils/projectStorage.js` - Wraps the Electron desktop bridge for listing, saving, restoring, and lazily materializing locally stored project videos and voiceover audio from the current desktop project roots, including sibling voiceover folders, voiceover language metadata, legacy fallback reads, and source descriptors for native desktop export jobs.
- `frontend/src/utils/runtimeConfig.js` - Resolves the backend origin for browser and Electron runtimes, builds API request URLs, and exposes the renderer-side developer-marker toggle.
- `frontend/src/components/DeveloperLocator/DeveloperLocator.jsx` - Renders the developer-only corner marker button that copies a stable location code for UI targeting and review.
- `frontend/src/components/DeveloperLocator/DeveloperLocator.css` - Styles the shared developer locator host wrapper and copy-to-clipboard marker button.
- `frontend/src/utils/sceneDetection.js` - Detects scene boundaries and generates thumbnails from video frames while reusing a playable local video source when available.
- `frontend/src/utils/subtitleAss.js` - Generates an ASS subtitle track from the shared subtitle render model so FFmpeg burn-in can match preview sizing, font family, text color, background color, anchor, margins, and wrapped text instead of relying on approximate force-style overrides.
- `frontend/src/utils/subtitleFontAsset.js` - Fetches a desktop-provided font asset through Electron IPC and writes it into the FFmpeg virtual file system so the ASS burn-in path can render text with a known font.
- `frontend/src/utils/subtitleOverlayAssets.js` - Renders deduplicated transparent PNG subtitle cards from the shared canvas subtitle layout, including configurable subtitle font-size, font family, text color, background color, background opacity, and anchor placement, so the native fast export backend can overlay them offline without browser recording.
- `frontend/src/utils/subtitleRenderModel.js` - Defines the shared subtitle render specification, line-wrapping logic, configurable font-size scaling, 24 subtitle font presets, text color, background color, background opacity, and anchor presets, plus the normalized values and scaled preview styles used by both the in-app frame preview and the exported subtitle burn-in path.
- `frontend/src/utils/subtitleUtils.js` - Converts subtitles to and from SRT, manages translation jobs, builds and parses persisted translation job ids that include the target language key, downloads completed translation results, probes saved translation job state during restore, and normalizes structured translation API errors into user-facing messages for the subtitle panel.
- `frontend/src/utils/voiceoverUtils.js` - Converts current subtitle JSON into an SRT upload for Vbee, polls the local voiceover proxy every 0.5 seconds, proxies the returned audio download back through the local server, and returns the generated narration blob plus duration and mime metadata so the desktop renderer can persist it inside the current project and sync the attached in-app voiceover track accurately.
- `frontend/src/utils/timeMapping.js` - Maps timestamps between original video time and kept-scene time.
- `frontend/src/components/ProjectDashboard/ProjectDashboard.jsx` - Lists saved projects with creation-date sorting and first-frame preview thumbnails, starts a new project from a selected video file, and marks dashboard regions and project cards with developer locator buttons.
- `frontend/src/components/ProjectDashboard/ProjectDashboard.css` - Styles the project dashboard, creation-date sort controls, project cards, and first-frame thumbnail states.
- `frontend/src/components/UploadZone/UploadZone.jsx` - Handles drag-and-drop or file-picker video selection.
- `frontend/src/components/UploadZone/UploadZone.css` - Styles the upload drop zone and selected-file summary.
- `frontend/src/components/VideoPlayer/VideoPlayer.jsx` - Plays the edited timeline inside an editable output-frame preview, consumes the shared left-nav section state from `App.jsx`, delegates the fixed portal sidebar shell, bottom transport row, voiceover playback, scene-motion controls, and bulk scene-motion rules to child modules, renders the visible frame through the shared canvas renderer with active scene motion, and applies the shared preview/export controls owned above the player.
- `frontend/src/components/VideoPlayer/useVideoPlayerVoiceover.js` - Owns only the voiceover preview playback timing, timeline re-sync rules, and clamp-at-audio-end behavior that prevents short voiceovers from looping their opening audio over the remaining video now that mix state lives in `useFrameExport`.
- `frontend/src/components/VideoPlayer/VideoPlayerSidebar.jsx` - Renders the fixed portal-based left-nav shell used by the player, including the shared dialog container, backdrop, title area, and developer locator marker.
- `frontend/src/components/VideoPlayer/VideoPlayerFrameSummaryBar.jsx` - Renders the compact frame-summary row above the preview, exposing only one-line `Frame` and `Background` buttons that show the current value inline and toggle the matching left-nav editing section.
- `frontend/src/components/VideoPlayer/VideoPlayerFrameControls.jsx` - Renders the section-specific left-nav editor for the player, so frame, background, audio, export, subtitle, per-scene motion, and bulk scene-motion entries each show focused controls while delegating larger subsections to dedicated child modules.
- `frontend/src/components/VideoPlayer/VideoPlayerSceneMotionControls.jsx` - Renders per-scene motion controls in the player sidebar, including the zoom-mode select, zoom ratio, face-detection action with center fallback, manual focus target fields, reset, and developer locator markers.
- `frontend/src/components/VideoPlayer/VideoPlayerFrameControls.css` - Styles the compact left-nav frame editor, including its section-focused layout, preset grid, background mode switcher, detail-on-demand panels for color, fade, image backgrounds, shared slider controls, text inputs for export file names, and responsive sidebar behavior.
- `frontend/src/components/VideoPlayer/VideoPlayerExportControls.jsx` - Renders the dedicated `export` section inside the player sidebar, exposing the shared quality preset selector, output file name field, current output folder, and folder-picker action used by `panel.export` configuration.
- `frontend/src/components/VideoPlayer/VideoPlayerSubtitleControls.jsx` - Renders the dedicated subtitle section inside the player sidebar as two compact groups, `Font` and `Text background`, exposing size, font, text color, background color, background opacity, and the 3x3 anchor controls that all write into the shared subtitle settings state used by preview, export, and persistence.
- `frontend/src/components/VideoPlayer/VideoPlayerSubtitleControls.css` - Styles the dedicated subtitle appearance controls, including the color pickers, appearance field layout, and the 3x3 subtitle anchor picker.
- `frontend/src/components/VideoPlayer/VideoPlayerTransportControls.jsx` - Renders the bottom transport row for play/pause, time, seek bar, and quick source-video volume control so the main player component can stay focused on playback state and frame composition.
- `frontend/src/components/VideoPlayer/VideoPlayer.css` - Styles the player shell, the two-button one-line frame-summary bar, the fixed portal-based left-nav overlay with backdrop and left-to-right entrance animation, the independently scrollable frame-editing nav container, the editable output-frame preview surface, playback controls, the shared canvas preview stage, and scene indicator.
- `frontend/src/components/SceneList/SceneList.jsx` - Displays detected scenes, a scene-list quick-config launcher button, sensitivity controls, delete or restore actions, and per-scene config buttons that open the selected scene in the player left-nav motion settings, with developer locator markers across visible scene-list states and cards.
- `frontend/src/components/SceneList/SceneBulkMotionConfig.jsx` - Renders the controlled left-nav quick config rule builder where users add persisted grouped face or duration conditions with AND/OR matching, assign zoom-mode actions, and see how many kept scenes were applied.
- `frontend/src/components/SceneList/SceneList.css` - Styles the scene list, quick-config launcher, AND/OR bulk motion rule builder, scene cards, and detection states.
- `frontend/src/components/Timeline/Timeline.jsx` - Renders the kept-scene timeline, playhead, subtitle markers, and the attached voiceover track when scene cuts exist, falls back to a raw-video timeline with full-duration seeking before scene detection has produced any cuts, and now lets both the subtitle row and voiceover row act as interactive entrypoints that open the matching player config nav.
- `frontend/src/components/Timeline/Timeline.css` - Styles the timeline bar, scene blocks, subtitles, and tooltip.
- `frontend/src/components/SubtitlePanel/SubtitlePanel.jsx` - Shows subtitle editing plus transcription, translation, and Vbee voiceover controls, including a grouped display-language selector plus translate action, empty states for untranslated languages, a Vietnamese-only narration action that is disabled for other display languages, and an explicit note that translation always starts from the original subtitle track while the latest generated narration is stored in the current project and anchored at `00:00`, with developer locator markers for subtitle states and cards.
- `frontend/src/components/SubtitlePanel/SubtitlePanel.css` - Styles the subtitle list, edit mode, grouped display-language controls, untranslated-language empty states, and subtitle tool states.
- `frontend/src/components/ExportPanel/ExportPanel.jsx` - Shows export progress, process diagnostics, the active frame summary, a `Config` launcher for the dedicated left-nav export settings, and result actions, while staying visible before scene detection by explaining the full-video fallback export state; it no longer renders saved-session history inside `panel.export`, summarizes the selected export quality plus output target, and switches the result action from browser download to `Show File` when export already wrote the MP4 locally.
- `frontend/src/components/ExportPanel/ExportPanel.css` - Styles the export panel, export-config launcher and summary, diagnostic log view, frame summary, saved-file path state, and result actions.

## notebooks

## server
- `server/app.py` - Flask API entrypoint for the desktop subtitle service, with backend health check and proxy route composition only.
- `server/proxy_routes.py` - Registers transcription, translation, and Vbee voiceover proxy endpoints, preserves downstream status codes and error payloads for the desktop client, falls back to a local translation job when the upstream worker-based service is unavailable, and proxies Vbee audio downloads back through Flask so the Electron renderer can auto-download the result without cross-origin issues.
- `server/translation_fallback.py` - Runs local subtitle translation jobs via `deep-translator`, persists lightweight job metadata under `server/uploads/translation-jobs`, and serves the fallback `start -> status -> download` flow used when the upstream translation service cannot accept work.
- `server/requirements.txt` - Lists Flask and HTTP client dependencies for the subtitle-service backend.

## root
- `EXPORT_ARCHITECTURE.md` - Describes the target fast-export architecture: Electron-main native FFmpeg coordination, shared render manifests and overlay assets for parity, hardware probing, and chunk-level parallelism.
- `TASK.md` - Tracks active work, validation state, and follow-up refactors for this repository.
- `MAP.md` - Maps files to responsibilities so future tasks can route to the correct owner quickly.