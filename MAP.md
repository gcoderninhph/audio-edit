# Repository Map

## .github
- `.github/agents/coder.agent.md` - Defines the workspace coding agent workflow, including planning, validation, `TASK.md`, `MAP.md`, 400-line guardrails, Docker Compose as the default backend startup path, and the desktop app's required backend domain `https://audio-test.accstore.pro.vn` with localhost desktop overrides disallowed unless the user explicitly asks for a temporary debugging exception.
- `.github/roles/map.role.md` - Documents how to create and update `MAP.md` as the repository query map.
- `.github/roles/task.role.md` - Documents how to create and update `TASK.md` as the persistent task tracker.

## frontend
- `frontend/index.html` - Hosts the Vite root element for the React application.
- `frontend/package.json` - Declares React renderer scripts, Electron desktop launch commands, and frontend runtime or build dependencies including `lucide-react` for renderer icon buttons; desktop dev now rebuilds `dist/` in watch mode and launches Electron without opening a renderer web port.
- `frontend/README.md` - Describes the Electron-first frontend workflow, the desktop-protocol renderer runtime with no client web port, local desktop persistence, and the remote-only desktop backend connection policy pinned to `https://audio-test.accstore.pro.vn`.
- `frontend/eslint.config.js` - Configures frontend lint rules.
- `frontend/vite.config.js` - Configures the Vite frontend build and dev server.
- `frontend/electron/debugLog.mjs` - Writes structured JSONL desktop debug logs under the Electron user data directory, rotates the current log file when it grows too large, and exposes IPC handlers for writing or locating export-debug traces.
- `frontend/electron/main.mjs` - Starts the Electron desktop shell, enables Chromium APIs needed by renderer face detection, moves Chromium session data into a dedicated user-data subfolder while disabling GPU shader disk cache to avoid Windows cache-startup failures, serves the built renderer through `desktop://app` when no external dev URL is injected, pins the desktop backend origin to `https://audio-test.accstore.pro.vn` with no localhost override or Electron-managed local Flask spawn path, writes desktop lifecycle plus renderer console or DOM diagnostics to the structured debug log file, registers both native export IPC and export-output IPC for choosing folders or revealing saved files, and defines the desktop `isDeveloper` toggle used to reveal UI locator markers.
- `frontend/electron/preload.mjs` - Exposes desktop runtime configuration, including the fixed remote backend URL `https://audio-test.accstore.pro.vn`, the `isDeveloper` UI-marker flag, debug-log IPC methods, native export IPC methods, export-output IPC methods for default folder, directory picking, save-to-file, and reveal actions, plus local project storage IPC methods including project-scoped voiceover save and restore calls, to the React renderer through a safe preload bridge.
- `frontend/electron/projectMediaProtocol.mjs` - Serves both renderer assets and stored project videos over the `desktop://app` Electron protocol, including byte-range video responses plus cross-origin resource headers so restored desktop videos can load inside the frame-preview canvas runtime without a local renderer HTTP server.
- `frontend/electron/projectStore.mjs` - Persists project metadata, dashboard series grouping fields, episode numbers, creation timestamps, output-frame settings, subtitle layout and appearance settings, export-quality profile selection, preview/export audio mix settings, scene-list bulk motion rules, source videos, subtitle-track metadata, and the top-level compatibility fields for saved voiceover audio under the workspace-local `projects/` folder during desktop development, delegates voiceover manifest or migration helpers to `projectVoiceoverStore.mjs`, emits `desktop://app/project-media/...` URLs for dashboard previews and editor playback, and retries local project deletion when Windows still holds a transient file lock.
- `frontend/electron/projectSubtitleStore.mjs` - Saves each subtitle language track into its own JSON file inside the project subtitle directory, removes stale language files after autosave, and hydrates the stored track files back into project loads.
- `frontend/electron/projectStoreShared.mjs` - Centralizes Electron project-store root selection, filename builders, payload normalization, directory creation, and project metadata read or write helpers, including the in-project `voiceovers/` directory layout plus legacy sibling or root voiceover path fallbacks, so the main project store can stay below the 400-line guardrail.
- `frontend/electron/projectVoiceoverStore.mjs` - Normalizes the project voiceover manifest keyed by language, resolves the current voiceover track from compatibility metadata, and migrates older sibling-folder voiceover files into the new in-project `voiceovers/` layout while updating project metadata.
- `frontend/electron/export/exportCoordinator.mjs` - Coordinates the native fast export job in Electron main: resolves the chosen output folder plus file name into a final local MP4 path, delegates native job input, asset, progress, and cleanup helpers to `nativeExportJobHelpers.mjs`, forwards the selected export-quality profile into native framing, merges kept scenes, remuxes the final audio mix using the configured video and narration volumes when needed, and returns saved-file metadata instead of loading the full output back into renderer RAM.
- `frontend/electron/export/exportAudioStage.mjs` - Runs the native FFmpeg audio-stage helper that renders video-only, voiceover-only, or mixed export audio tracks before the final native remux so `exportCoordinator.mjs` stays under the 400-line guardrail.
- `frontend/electron/export/exportOutputIpc.mjs` - Owns Electron main IPC helpers for export output targets: default download-directory lookup, directory picker, safe MP4 filename resolution, local-file byte writes for fallback export, and reveal-in-folder actions for completed exports.
- `frontend/electron/export/frameFilterGraph.mjs` - Builds the native FFmpeg frame filter graph for each export chunk, normalizing source video to the detected merged-video frame rate before scene-motion zoom math, using a 4:4:4 intermediate compositor only for motion scenes to avoid chroma-subsampled overlay snap, composing preset color, image, or video-fade backgrounds, applying subtitle overlays, drawing the moving `G Studio` watermark with FFmpeg `drawtext` from straight-line bounce path segments, and resetting final video PTS.
- `frontend/electron/export/framePipeline.mjs` - Splits the native frame-render stage into adaptive frame-grid-aligned timeline chunks, detects the merged-video frame rate for export cadence parity with preview playback, delegates chunk filter graph construction to `frameFilterGraph.mjs` with chunk time offsets so moving overlays stay continuous, runs several FFmpeg framing workers in parallel, aggregates chunk progress back to the renderer, and concatenates the finished framed chunks into either a job-local temp file or the final caller-supplied output MP4 path.
- `frontend/electron/export/frameMotionFilter.mjs` - Builds the native FFmpeg foreground scale, aspect-stable dynamic zoom sizing, pixel-stabilized original-video-box clipping, and overlay expressions for per-scene zoom modes, including static zoom-in, animated zoom-out, animated zoom-in, and chunk-local motion segment slicing.
- `frontend/electron/export/nativeExportJobHelpers.mjs` - Owns native export job helpers split from `exportCoordinator.mjs`, including source materialization, subtitle overlay and image-background asset writes, frame preset resolution, progress/debug logging, renderer-detach tracking, and retrying locked job-directory cleanup on Windows.
- `frontend/electron/export/nativeFfmpeg.mjs` - Resolves the bundled `ffmpeg-static` binary, probes available hardware encoders and source video frame rate, derives aggressive adaptive scene and frame worker plans from host CPU capacity plus timeline size, maps the selected export-quality profile onto hardware or CPU encoder arguments, and runs native FFmpeg child processes while capturing stderr and progress output.
- `frontend/electron/export/scenePipeline.mjs` - Splits kept-scene extraction and concat into a parallel native FFmpeg pipeline with adaptive bounded worker concurrency for CPU-heavy export stages.
- `frontend/electron/subtitleFont.mjs` - Resolves a usable desktop font file from the host OS and exposes it over IPC so the FFmpeg subtitle burn-in path can render text inside the Electron runtime.
- `frontend/src/main.jsx` - Boots the React app into the DOM.
- `frontend/src/App.jsx` - Composes the dashboard, forced temporary-admin setup flow, admin console shell, and editor UI around the `useVideoEditor` and `useAuthSession` hooks, wiring auth-required callbacks, the shared dashboard or editor premium header button, the authenticated credit-purchase popup state, the server-backed premium plans popup state, plus the admin-console entrypoint into the extracted app-shell components.
- `frontend/src/App.css` - Styles the top-level app shell and shared layout states, including the shared dashboard and editor premium header button treatment.
- `frontend/src/index.css` - Defines global theme tokens, resets, and shared utility styles.
- `frontend/src/hooks/useEditorPersistence.js` - Handles desktop-local project persistence, lightweight source video restore, autosave, output-frame setting restore, restores any saved project voiceover track plus subtitle layout, subtitle appearance, export-quality profile settings, audio mix settings, scene-list bulk motion rules, subtitle track manifest, and active subtitle language alongside the project session, delegates restore or resume helpers to a dedicated module, and cleans up the active project before local deletion.
- `frontend/src/hooks/useEditorPersistenceRestore.js` - Owns the restore-side helpers for project video, saved voiceover, voiceover language assignment, and in-flight subtitle job resume so `useEditorPersistence.js` stays under the 400-line guardrail while persistence behavior grows.
- `frontend/src/hooks/editorPersistenceJobRestore.js` - Restores saved transcription and translation jobs on project load, keeps subtitle-job progress tied to the active session, and clears only confirmed stale job markers.
- `frontend/src/hooks/useVideoEditor.js` - Orchestrates editor state, detection, subtitle actions, and delegates persistence, subtitle-track state, scene-motion configuration, derived voiceover state, extracted subtitle action callbacks, plus frame-aware export concerns to dedicated hooks and helpers, including the source video duration needed by pre-cut export fallback, the attached voiceover track state, active-language filtering, kept-timeline subtitle mapping, and grouped export config or result objects surfaced by `useFrameExport`.
- `frontend/src/hooks/useEditorSceneListActions.js` - Owns editor scene-list actions split out of `useVideoEditor.js`, including toggling a scene as deleted or restored, deleting/restoring all scenes, clearing stale export results after scene-list changes, and seeking preview playback to a selected scene.
- `frontend/src/hooks/useSceneMotionConfig.js` - Owns editor-side per-scene motion updates, project-scoped bulk scene-list motion rule state and application with AND/OR condition groups, and sensitive face-detection actions that sample multiple frames across each scene, store detected focus targets, and fall back to the center target when detection is unavailable or no face is found.
- `frontend/src/hooks/useVideoEditorVoiceoverState.js` - Derives the active-language voiceover track, display name, and kept-timeline voiceover subtitles so `useVideoEditor.js` stays below the 400-line guardrail.
- `frontend/src/hooks/useVideoEditorSubtitleActions.js` - Owns the editor-side subtitle action callbacks for transcription, translation, voiceover generation, and inline subtitle edits, forwarding the active subtitle language into narration generation so `useVideoEditor.js` stays under the 400-line guardrail while subtitle-track behavior grows.
- `frontend/src/hooks/useSubtitleTracks.js` - Keeps the in-memory subtitle track collection, active display language, visible subtitle list, and per-track edit updates separate from the top-level editor hook.
- `frontend/src/hooks/subtitleJobActions.js` - Runs the editor-side transcription, translation, and Vbee voiceover job flows with session guards so `useVideoEditor.js` can stay below the 400-line guardrail while reusing the same progress and autosave wiring, now saves generated voiceover audio bytes into the current desktop project with a language key before attaching the in-app track, and currently blocks narration generation for unsupported non-Vietnamese subtitle languages.
- `frontend/src/hooks/useFrameExport.js` - Owns the editable output-frame settings, shared subtitle layout and appearance settings, the shared export-quality profile, export output file name plus folder state, the persisted source-video and voiceover audio-mix state used by both preview and export, export lifecycle state, saved-file export result state, export diagnostics state, and the frame-aware export entrypoint shared by the player preview and export panel, including scene-motion settings in export freshness checks.
- `frontend/src/hooks/useAuthSession.js` - Owns the optional auth session in the renderer, persisting JWT access and refresh tokens, refreshing access tokens before expiry, listening for shared auth-session updates, and exposing login, registration, temporary-admin completion, role flags, logout, and current credit balance state to the UI.
- `frontend/src/hooks/useUndoHistory.js` - Tracks undo and redo snapshots for editor state changes.
- `frontend/src/utils/audioExtractor.js` - Extracts audio with FFmpeg, materializes stored project video only when authenticated transcription starts, sends the saved access token to protected transcription start and status APIs, updates the stored auth credit balance from successful subtitle-start responses, and exposes transcription job snapshot helpers used to restore or clear saved jobs without flashing stale progress.
- `frontend/src/utils/desktopLogger.js` - Wraps the Electron debug-log bridge so renderer export flows can persist structured diagnostics to the desktop JSONL log file.
- `frontend/src/utils/editorSelectors.js` - Provides pure selectors for the current scene and subtitle visibility after scene deletions.
- `frontend/src/utils/exportAudioMix.js` - Defines the shared export-audio helpers for normalizing preview/export mix state, computing kept-timeline duration, and building FFmpeg argument sets for mixed, voiceover-only, video-only, or final-mux audio stages, including optional final video encode settings for the fallback mux pass.
- `frontend/src/utils/exportQualityProfile.js` - Defines the shared export-quality profile presets, including the extra-low `Draft smallest` option, plus normalization helpers, fallback mux settings, and native encoder argument mapping used by the export-config sidebar, project persistence, and both export backends.
- `frontend/src/utils/subtitleTracks.js` - Defines the shared subtitle-track language options, active-language normalization, voiceover-language helpers, and helpers for reading or updating the dedicated `original` track plus translated language tracks without overwriting each other.
- `frontend/src/utils/exportOutputTarget.js` - Wraps the preload export-output bridge for the renderer, building default export file names, user-facing folder or file labels, default export directories, directory-picking flows, fallback save-to-file writes, and reveal-saved-file actions.
- `frontend/src/utils/exportAudioStage.js` - Materializes the saved voiceover blob and runs the FFmpeg.wasm fallback audio-stage helper so `ffmpegManager.js` can render the configured export mix without exceeding the 400-line guardrail.
- `frontend/src/utils/ffmpegManager.js` - Tries the native fast export backend first for both color-backed and image-backed frames, then falls back to the FFmpeg.wasm path when native export is unavailable; the fallback path mounts source files through `WORKERFS`, delegates canvas frame recording to `fallbackFrameRecorder.js`, applies export-quality, audio-mix, and premium watermark-hiding settings, and saves fallback bytes into the chosen local export target when configured.
- `frontend/src/utils/ffmpegExportLogging.js` - Formats FFmpeg progress timestamps and selected log lines and emits user-visible export log entries so `ffmpegManager.js` can stay below the 400-line guardrail.
- `frontend/src/utils/fallbackFrameRecorder.js` - Records the FFmpeg.wasm fallback canvas-composited video from `cut.mp4`, applying frame settings, subtitle settings, export-quality bitrate, and scene-motion segments before final muxing.
- `frontend/src/utils/ffmpegSceneMerge.js` - Re-encodes multiple kept scenes into temporary segment files and concatenates them via a manifest so export avoids the oversized `filter_complex` graph that previously stalled in FFmpeg.wasm.
- `frontend/src/utils/frameComposer.js` - Defines shared output-frame presets, color, image, or video-fade background models, including preset-driven fade configuration plus background serialization and subtitle timing helpers used by the preview frame, persistence, and export compositor.
- `frontend/src/utils/frameCanvasRenderer.js` - Draws preset color, cover-image, or blurred video-derived fade backgrounds plus scene-motion-aware foreground video clipped to the original fit-contain video box, conditionally draws the bouncing straight-line `G Studio` watermark only for non-premium accounts, and paints active subtitle cards into a canvas while exposing subtitle-card layout helpers so native export overlay assets reuse the same subtitle geometry as the preview.
- `frontend/src/utils/watermarkMotion.js` - Defines the shared deterministic `G Studio` watermark motion model, generating continuous straight-line path segments that mirror the incoming angle at every frame-edge or corner collision for preview, fallback export, and native export parity.
- `frontend/src/utils/frameCanvasExport.js` - Records the shared canvas frame renderer, including selected image backgrounds, scene-motion segments, the moving `G Studio` watermark, and shared subtitle layout settings, as a video stream during fallback export with configurable recording bitrate.
- `frontend/src/utils/nativeExportClient.js` - Builds the renderer-side native export request, including image-backed frame background payloads, per-scene motion config, shared subtitle layout settings, premium watermark-hiding state, the selected export-quality profile, the chosen output file target, configured export audio mix, and optional saved voiceover payload, then accepts either saved-file metadata for direct-to-disk native exports or raw bytes for older blob-style results.
- `frontend/src/utils/faceDetection.js` - Wraps the browser `FaceDetector` API with fast, accurate, and enhanced-image retry passes for detecting the largest face across one or more captured scene frames and returning a normalized focus target for scene motion.
- `frontend/src/utils/sceneMotion.js` - Normalizes selectable per-scene zoom modes, serializes scene motion for export freshness, computes preview/fallback render state, and builds kept-timeline motion segments for export.
- `frontend/src/utils/sceneMotionBulkConfig.js` - Defines scene-list bulk motion rule conditions, AND/OR operators, action targets, normalization, duration matching, and action-to-motion config helpers used by the bulk scene config UI and hook.
- `frontend/src/utils/projectStorage.js` - Wraps the Electron desktop bridge for listing, saving, restoring, and lazily materializing locally stored project videos and voiceover audio from the current desktop project roots, including sibling voiceover folders, voiceover language metadata, legacy fallback reads, and source descriptors for native desktop export jobs.
- `frontend/src/utils/runtimeConfig.js` - Resolves the backend origin for browser and Electron runtimes, now defaulting the desktop fallback URL to `https://audio-test.accstore.pro.vn`, builds API request URLs, and exposes the renderer-side developer-marker toggle.
- `frontend/src/utils/authClient.js` - Wraps the Flask auth API for optional login, account registration, JWT access-token refresh, current-user reads, logout, localStorage-backed session persistence, auth-session change notifications, normalized role, premium window, username, or temporary-admin flags, client-side credit-balance updates, timestamp-aware premium-active computation, and bearer authorization headers for protected generation APIs.
- `frontend/src/utils/premiumWindow.js` - Centralizes premium-window normalization plus popup-facing helpers for formatting premium expiry timestamps, rendering human-readable countdown strings, and deriving active or scheduled premium bar status from `premiumStartAt` and `premiumEndAt`.
- `frontend/src/utils/adminClient.js` - Wraps the admin-only backend API for temporary-admin completion, overview reads, user management, and recent request listing.
- `frontend/src/components/AppShell/AppHeader.jsx` - Renders the shared dashboard, admin, and editor header shell around the logo, auth actions, page-specific controls, and the optional dashboard or editor premium crown button keyed off the signed-in account's timestamp-aware premium state, now hiding that premium button for signed-out clients and delegating both premium-plan and credit-purchase popup opens so `App.jsx` stays below the 400-line guardrail.
- `frontend/src/components/AppShell/PremiumPackagesDialog.jsx` - Renders the desktop premium purchase popup opened from the shared header, loads public server packages through `frontend/src/utils/iapClient.js`, derives the current premium window through `frontend/src/utils/premiumWindow.js`, shows the premium expiry note plus a single HP-style countdown bar in the popup status area, groups premium-capable plans into horizontally swipable pages of up to three fixed-width cards, splits dash-delimited description text into separate bullet items, and opens the QR payment overlay from each buy button.
- `frontend/src/components/AppShell/CreditPackagesDialog.jsx` - Renders the authenticated desktop credit-purchase popup opened from the header credit pill, loads public credit-only packages through `frontend/src/utils/iapClient.js`, reuses the shared fixed-width swipeable purchase-card layout, and opens the QR payment overlay from each buy button.
- `frontend/src/components/AppShell/PaymentQrDialog.jsx` - Renders the Sepay QR checkout overlay in a portal above premium or credit package popups, wires the pending close path to a server-side ticket cancel call before dismissing the UI, pauses polling while cancellation is in flight, and delegates the shared QR preview/table subcomponents plus pure QR dialog data builders to split files so the main popup stays below the 400-line guardrail.
- `frontend/src/components/AppShell/PaymentQrDialogShared.jsx` - Holds the shared QR payment popup preview-card and key-value table components used by the main dialog so the popup render stays compact.
- `frontend/src/components/AppShell/paymentQrDialogModel.js` - Holds the pure QR payment dialog formatting and row-building helpers, including package benefits, summary rows, pending transfer rows, and price formatting.
- `frontend/src/components/AppShell/PremiumPackagesDialog.css` - Styles the desktop premium and credit purchase popups, including the simplified single-bar premium countdown status block, the horizontal snap-paged package carousel, fixed-width card layout, hidden swipe-only scrollbar treatment, compact inline recommended badge, pagination controls, and loading/error/empty states, with QR overlay styles split into `PaymentQrDialog.css`.
- `frontend/src/components/AppShell/PaymentQrDialog.css` - Styles the nested QR payment overlay surface, the two-column package-card plus detail-table layout, synchronized secondary or primary action buttons, QR visual panel, countdown or result states, and mobile layout for the authenticated checkout portal.
- `frontend/src/utils/iapClient.js` - Loads the public `/api/iap/packages` catalog through the shared runtime API helper, normalizes the server-provided `isRecommended` flag, filters it into premium-purchase packages plus credit-only purchase packages, sorts recommended plans to the front, and wraps authenticated IAP payment ticket create/status/cancel plus QR-image blob APIs for desktop QR checkout.
- `frontend/src/components/AppShell/AppEditorWorkspace.jsx` - Renders the extracted editor workspace panels, player, scene list, subtitle tools, timeline, and export panel so the app shell can switch between dashboard, admin, and editor modes without growing oversized, while threading the signed-in account's timestamp-aware premium watermark-hiding state into preview and export entrypoints.
- `frontend/src/components/Admin/AdminBootstrapSetup.jsx` - Renders the forced setup form shown after temporary-admin login, collecting the permanent admin username and password before the admin console can open.
- `frontend/src/components/Admin/AdminBootstrapSetup.css` - Styles the temporary-admin setup screen and form.
- `frontend/src/components/Admin/AdminConsole.jsx` - Renders the admin console for backend management, loading admin overview cards, user role or credit editing controls, and recent request cards from the admin API.
- `frontend/src/components/Admin/AdminConsole.css` - Styles the admin console hero, summary cards, user table, and recent request list.
- `frontend/src/components/DeveloperLocator/DeveloperLocator.jsx` - Renders the developer-only corner marker button that copies a stable location code for UI targeting and review.
- `frontend/src/components/DeveloperLocator/DeveloperLocator.css` - Styles the shared developer locator host wrapper and copy-to-clipboard marker button.
- `frontend/src/utils/sceneDetection.js` - Detects scene boundaries and generates thumbnails from video frames while reusing a playable local video source when available.
- `frontend/src/utils/subtitleAss.js` - Generates an ASS subtitle track from the shared subtitle render model so FFmpeg burn-in can match preview sizing, font family, text color, background color, anchor, margins, and wrapped text instead of relying on approximate force-style overrides.
- `frontend/src/utils/subtitleFontAsset.js` - Fetches a desktop-provided font asset through Electron IPC and writes it into the FFmpeg virtual file system so the ASS burn-in path can render text with a known font.
- `frontend/src/utils/subtitleOverlayAssets.js` - Renders deduplicated transparent PNG subtitle cards from the shared canvas subtitle layout, including configurable subtitle font-size, font family, text color, background color, background opacity, and anchor placement, so the native fast export backend can overlay them offline without browser recording.
- `frontend/src/utils/subtitleRenderModel.js` - Defines the shared subtitle render specification, line-wrapping logic, configurable font-size scaling, 24 subtitle font presets, text color, background color, background opacity, and anchor presets, plus the normalized values and scaled preview styles used by both the in-app frame preview and the exported subtitle burn-in path.
- `frontend/src/utils/subtitleUtils.js` - Converts subtitles to and from SRT, manages authenticated translation jobs, builds and parses persisted translation job ids that include the target language key, sends bearer headers for translation start/status/download ownership checks, updates the stored auth credit balance from successful translation-start responses, downloads completed translation results, probes saved translation job state during restore, and normalizes structured translation API errors into user-facing messages for the subtitle panel.
- `frontend/src/utils/voiceoverUtils.js` - Converts current subtitle JSON into an authenticated SRT upload for Vbee, sends bearer headers for owned voiceover start/status/download calls, updates the stored auth credit balance from successful voiceover-start responses, polls the local voiceover proxy every 0.5 seconds, proxies the returned audio download back through the local server, and returns the generated narration blob plus duration and mime metadata so the desktop renderer can persist it inside the current project and sync the attached in-app voiceover track accurately.
- `frontend/src/utils/timeMapping.js` - Maps timestamps between original video time and kept-scene time.
- `frontend/src/components/ProjectDashboard/ProjectDashboard.jsx` - Orchestrates the desktop dashboard root, loading saved projects, grouping them into series versus standalone videos, saving video metadata through local project persistence, opening series detail view, creating new projects, and coordinating project edit/delete dialogs and developer locator markers.
- `frontend/src/components/ProjectDashboard/ProjectDashboard.css` - Styles the dashboard's top series section, lower standalone-video section, project and series cards, fixed portal option dropdowns, metadata dialogs, and YouTube-like series detail layout with responsive video plus episode list behavior and an editor-player-style series preview stage.
- `frontend/src/components/ProjectDashboard/ProjectDashboardCards.jsx` - Renders reusable dashboard cards for standalone videos, series groups, the new-project action, preview thumbnails, and the icon-only per-video option menu that portals above dashboard and series detail rows to avoid clipping.
- `frontend/src/components/ProjectDashboard/ProjectDashboardDialogs.jsx` - Renders the dashboard dialogs for editing video name or series assignment and for creating a new series by selecting standalone videos.
- `frontend/src/components/ProjectDashboard/SeriesDetailView.jsx` - Renders a selected series as a watch page with an editor-player-style active episode preview and transport controls on the left, sorted episode rows on the right, per-episode edit/delete menus, and inline episode-number editing on double-click.
- `frontend/src/components/ProjectDashboard/projectDashboardModel.js` - Holds pure dashboard helpers for title/date formatting, series id/name normalization, episode sorting, standalone-video filtering, next episode calculation, and applying project metadata updates.
- `frontend/src/components/UploadZone/UploadZone.jsx` - Handles drag-and-drop or file-picker video selection.
- `frontend/src/components/UploadZone/UploadZone.css` - Styles the upload drop zone and selected-file summary.
- `frontend/src/components/VideoPlayer/VideoPlayer.jsx` - Plays the edited timeline inside an editable output-frame preview, consumes the shared left-nav section state from `App.jsx`, delegates the fixed portal sidebar shell, preview stage, bottom transport row, voiceover playback, scene-motion controls, and bulk scene-motion rules to child modules, and applies the shared preview/export controls owned above the player, including premium watermark visibility.
- `frontend/src/components/VideoPlayer/VideoPlayerPreviewStage.jsx` - Owns the player canvas, video, and voiceover media stage, loads frame background images, renders scene-motion preview frames through `frameCanvasRenderer`, and passes current playback time plus premium watermark visibility so the moving `G Studio` watermark stays in sync only for non-premium accounts.
- `frontend/src/components/VideoPlayer/useVideoPlayerVoiceover.js` - Owns only the voiceover preview playback timing, timeline re-sync rules, and clamp-at-audio-end behavior that prevents short voiceovers from looping their opening audio over the remaining video now that mix state lives in `useFrameExport`.
- `frontend/src/components/VideoPlayer/VideoPlayerSidebar.jsx` - Renders the fixed portal-based left-nav shell used by the player, including the shared dialog container, backdrop, title area, and developer locator marker.
- `frontend/src/components/VideoPlayer/VideoPlayerFrameSummaryBar.jsx` - Renders the compact frame-summary row above the preview, exposing only one-line `Frame` and `Background` buttons that show the current value inline and toggle the matching left-nav editing section.
- `frontend/src/components/VideoPlayer/VideoPlayerFrameControls.jsx` - Renders the section-specific left-nav editor for the player, so frame, background, audio, export, subtitle, per-scene motion, and bulk scene-motion entries each show focused controls while delegating larger subsections to dedicated child modules.
- `frontend/src/components/VideoPlayer/VideoPlayerSceneMotionControls.jsx` - Renders per-scene motion controls in the player sidebar, including the zoom-mode select, zoom ratio, face-detection action with center fallback, manual focus target fields, reset, and developer locator markers.
- `frontend/src/components/VideoPlayer/VideoPlayerFrameControls.css` - Styles the compact left-nav frame editor, including its section-focused layout, preset grid, background mode switcher, detail-on-demand panels for color, fade, image backgrounds, shared slider controls, text inputs for export file names, and responsive sidebar behavior.
- `frontend/src/components/VideoPlayer/VideoPlayerExportControls.jsx` - Renders the dedicated `export` section inside the player sidebar, exposing the shared quality preset selector, output file name field, current output folder, and folder-picker action used by `panel.export` configuration.
- `frontend/src/components/VideoPlayer/VideoPlayerSubtitleControls.jsx` - Renders the dedicated subtitle section inside the player sidebar as two compact groups, `Font` and `Text background`, exposing size, font, text color, background color, background opacity, and the 3x3 anchor controls that all write into the shared subtitle settings state used by preview, export, and persistence.
- `frontend/src/components/VideoPlayer/VideoPlayerSubtitleControls.css` - Styles the dedicated subtitle appearance controls, including the color pickers, appearance field layout, and the 3x3 subtitle anchor picker.
- `frontend/src/components/VideoPlayer/VideoPlayerTransportControls.jsx` - Renders the bottom transport row for play/pause, time, seek bar, and quick source-video volume control so the main player component can stay focused on playback state and frame composition.
- `frontend/src/components/VideoPlayer/VideoPlayer.css` - Styles the player shell, the two-button one-line frame-summary bar, the fixed portal-based left-nav overlay with backdrop and left-to-right entrance animation, the independently scrollable frame-editing nav container, the editable output-frame preview surface, playback controls, the shared canvas preview stage, and scene indicator.
- `frontend/src/components/SceneList/SceneList.jsx` - Displays detected scenes, a scene-list quick-config launcher button, sensitivity controls, delete or restore actions, and per-scene config buttons that open the selected scene in the player left-nav motion settings, with developer locator markers across visible scene-list states and cards.
- `frontend/src/components/SceneList/SceneBulkMotionConfig.jsx` - Renders the controlled left-nav quick config rule builder where users add persisted grouped face or duration conditions with AND/OR matching, assign zoom-mode actions, and see how many kept scenes were applied.
- `frontend/src/components/SceneList/SceneList.css` - Styles the scene list, quick-config launcher, AND/OR bulk motion rule builder, scene cards, and detection states.
- `frontend/src/components/Timeline/Timeline.jsx` - Renders the kept-scene timeline, playhead, subtitle markers, and the attached voiceover track when scene cuts exist, falls back to a raw-video timeline with full-duration seeking before scene detection has produced any cuts, and now lets both the subtitle row and voiceover row act as interactive entrypoints that open the matching player config nav.
- `frontend/src/components/Timeline/Timeline.css` - Styles the timeline bar, scene blocks, subtitles, and tooltip.
- `frontend/src/components/SubtitlePanel/SubtitlePanel.jsx` - Shows subtitle editing plus transcription, translation, and Vbee voiceover controls, gates subtitle generation, translation, and voiceover generation behind login, shows the current credit balance plus the cost of each action, includes a grouped display-language selector plus translate action, empty states for untranslated languages, a Vietnamese-only narration action that is disabled for other display languages, and an explicit note that translation always starts from the original subtitle track while the latest generated narration is stored in the current project and anchored at `00:00`, with developer locator markers for subtitle states and cards.
- `frontend/src/components/SubtitlePanel/SubtitlePanel.css` - Styles the subtitle list, edit mode, grouped display-language controls, untranslated-language empty states, and subtitle tool states.
- `frontend/src/components/ExportPanel/ExportPanel.jsx` - Shows export progress, process diagnostics, the active frame summary, a `Config` launcher for the dedicated left-nav export settings, and result actions, while staying visible before scene detection by explaining the full-video fallback export state; it no longer renders saved-session history inside `panel.export`, summarizes the selected export quality plus output target, and switches the result action from browser download to `Show File` when export already wrote the MP4 locally.
- `frontend/src/components/ExportPanel/ExportPanel.css` - Styles the export panel, export-config launcher and summary, diagnostic log view, frame summary, saved-file path state, and result actions.
- `frontend/src/components/Auth/AuthDialog.jsx` - Renders the optional auth popup, login/register mode tabs, demo-login action, credential forms, loading state, error state, and developer locator marker for auth UI, now allowing login by email or username for real admin accounts.
- `frontend/src/components/Auth/AuthHeaderActions.jsx` - Renders the header login button, signed-in credit-balance button that opens the credit-purchase popup, admin-console launcher, and username dropdown that now contains the logout action without blocking unauthenticated editing workflows.
- `frontend/src/components/Auth/AuthDialog.css` - Styles the header auth action, auth popup backdrop, login/register mode tabs, form fields, error state, and responsive auth UI.

## notebooks

## admin-frontend
- `admin-frontend/package.json` - Declares the standalone React admin frontend scripts and dependencies, including Vite, React, ESLint, and lucide icons.
- `admin-frontend/README.md` - Documents admin frontend commands, dev proxy behavior, and Flask serving from the production build output.
- `admin-frontend/vite.config.js` - Configures the admin React app with `/admin/` as the production base path and proxies `/api` to the local Flask server in development.
- `admin-frontend/src/main.jsx` - Boots the standalone React admin app into the Vite root element.
- `admin-frontend/src/App.jsx` - Owns the admin SPA route parsing, including route-driven IAP tab, payment-tools subview, payment-transaction detail URLs, payment-tools bank-hook-history detail URLs, the legacy `/admin/iap/bank-hook-history/:id` alias, and the removed `/admin/iap/pack-function` path now mapped back to packages, along with session verification, login/setup/manage/iap/detail routing, shared layout composition, and the route-level header action slot passed into the shared admin shell.
- `admin-frontend/src/App.css` - Imports the split admin frontend style modules for layout, forms, tables, and detail views.
- `admin-frontend/src/index.css` - Defines the dark desktop-app-matched theme tokens, base document styles, and global reset for the admin frontend.
- `admin-frontend/src/api/adminApi.js` - Wraps the Flask auth and admin APIs, including local session persistence, token refresh, login, logout, bootstrap setup, paginated users, user detail, premium-window updates, IAP package CRUD, copy-hidden IAP API key flows, beneficiary account CRUD, payment transaction and refund-pending reads, editable pack-function CRUD, sale CRUD, bank-hook history list reads with search/date filters, bank-hook transaction detail reads, requests, credit history, and add-credit calls.
- `admin-frontend/src/utils/format.js` - Provides shared number, currency, date-time, datetime-local input, and request-source formatting helpers for admin screens.
- `admin-frontend/src/components/AdminLayout.jsx` - Renders the shared admin React shell with a fixed full-height left navigation for `User manager` and `IAP` only while an admin session is present, plus the topbar brand or account dropdown, page-level header action slot, refresh-page action, and logout flow around the current admin page.
- `admin-frontend/src/components/DeveloperMarker.jsx` - Renders developer-only locator markers for major admin React UI sections with query or localStorage developer-mode overrides.
- `admin-frontend/src/components/LoginPage.jsx` - Renders the admin React login form and submits credentials to the Flask auth API.
- `admin-frontend/src/components/SetupPage.jsx` - Renders the temporary-admin bootstrap form that creates the persisted admin account through the Flask admin API.
- `admin-frontend/src/components/ManagePage.jsx` - Renders the admin user-management table view with header-level refresh action, search, page-size controls, and a paginated user table that shows role, premium plan or window, and locked or active account state plus detail navigation, without the old summary panels.
- `admin-frontend/src/components/IapPage.jsx` - Wraps the dedicated admin React IAP route and passes the parsed route state into the tabbed IAP management shell within the shared admin layout.
- `admin-frontend/src/components/IapManagementTabs.jsx` - Renders the four-tab `admin.react.manage.iap` tab strip after removing the standalone pack-function tab, derives the active tab from the current route, and navigates each tab to its own URL so refresh keeps the current IAP tab and payment-tools detail state.
- `admin-frontend/src/components/IapPackagesPanel.jsx` - Renders the admin React IAP catalog panel with package create or edit dialog, `packType` selection, the desktop-client recommend checkbox, a centered green-check or `-` recommend status cell, truncated package detail text, and a compact three-icon action group per package row for function/edit/delete flows, plus route refresh wiring for package CRUD.
- `admin-frontend/src/components/IapPackageFunctionDialog.jsx` - Renders the per-package pack-function dialog opened from the package actions column, shows Package/Package id/Pack type/Current behavior in a two-column key-value table, edits or creates `credits` and `premiumDurationDays` based on the package type, and allows pack-function deletion.
- `admin-frontend/src/components/IapApiKeyPanel.jsx` - Renders the payment-hook API key management tab for `/api/pay/info`, including the add-new dialog, configured HTTP method, inbound header-name field, configurable header value format using the `<API_KEY>` placeholder, copy-only hidden API key actions in the table, and delete actions.
- `admin-frontend/src/components/IapPaymentOperationsPanel.jsx` - Renders the dedicated IAP payment-tools tab with a route-driven 30/70 split layout, a left-side button list for beneficiaries, refund pending, transactions, and history, and a right-side content area that defaults to transactions while supporting transaction detail replacement plus embedded bank-hook-history detail replacement.
- `admin-frontend/src/components/IapBeneficiaryAccountsDialog.jsx` - Renders the inline beneficiary account management section for the API-key tab, loads Sepay bank ids from `https://qr.sepay.vn/banks.json`, keeps the account list inline in the page body, opens creation in a dedicated popup form, enforces a single current-account checkbox selection, and swaps to a local beneficiary-detail view when an account row is opened.
- `admin-frontend/src/components/IapBeneficiaryAccountDetailPanel.jsx` - Renders the beneficiary account detail replacement view with back or refresh actions and a two-column key-value table for the selected payment receiver account.
- `admin-frontend/src/components/IapPaymentTransactionsDialog.jsx` - Renders the inline IAP payment transaction list for admin review, showing payment ticket code, package, amount, status, timestamps, and failure reason, and supports row clicks that open transaction detail in the payment-tools route.
- `admin-frontend/src/components/IapPaymentTransactionDetailPanel.jsx` - Renders the payment transaction detail replacement view with a back button, refresh action, and a two-column key-value table for the selected admin payment ticket route, including the last desktop client status-check timestamp so admins can tell whether the desktop app is still polling.
- `admin-frontend/src/components/IapRefundPendingDialog.jsx` - Renders the inline refund pending review section, now with a minimal transaction or amount or status table that opens a local refund-detail replacement view for the selected record.
- `admin-frontend/src/components/IapRefundPendingDetailPanel.jsx` - Renders the refund-pending detail replacement view with back or refresh actions and a two-column key-value table for the selected manual-review record.
- `admin-frontend/src/components/IapBankHookHistoryPage.jsx` - Renders the dedicated admin React bank-hook transaction history page with back/refresh header actions, search plus date-range filters, paginated transaction reads, clickable rows showing which API key received each bank payload plus the transaction time, amount, content, and reference fields, and also supports a tighter embedded rendering inside the payment-tools tab while routing row clicks to the payment-tools history detail replacement view.
- `admin-frontend/src/components/IapBankHookHistoryDetailPage.jsx` - Renders the dedicated admin React bank-hook transaction detail page with a top key-value summary table for the selected transaction, separate raw payload JSON inspection, standalone header actions when used on its legacy route, and embedded back or refresh toolbar actions when used as the payment-tools history detail replacement view.
- `admin-frontend/src/components/IapSalesPanel.jsx` - Renders the sale-rule admin tab with modal add-new flow, discount and time-window inputs, first-purchase conditions, sale list, and delete actions.
- `admin-frontend/src/components/UserDetailPage.jsx` - Renders the selected user detail screen with header-level back and refresh actions, inline user identity plus premium-window and lock controls, request history, credit history, and add-credit or premium-window dialogs, without the old summary panels.
- `admin-frontend/src/components/Pagination.jsx` - Renders the reusable paginated footer controls used by admin user, request, and credit-history lists.
- `admin-frontend/src/utils/iapPackages.js` - Centralizes admin frontend IAP package-type constants, labels, and the derived pack-function behavior or timed-premium summary helpers used across the package panel and per-package function dialog.
- `admin-frontend/src/styles/layout.css` - Styles the admin React shell, including the fixed full-height left sidebar, the fixed full-width topbar layered underneath that sidebar corner, shared header action layout, panels, section headings, toolbar actions, developer markers, and responsive layout.
- `admin-frontend/src/styles/forms.css` - Styles admin React forms, account dropdown, buttons, notices, search controls, checkbox fields, and modal dialog surfaces including the IAP package dialog.
- `admin-frontend/src/styles/tables.css` - Styles admin React tables, clickable rows, user cells, status or role pills, inline table actions, empty states, and pagination bars.
- `admin-frontend/src/styles/detail.css` - Styles the admin React user-detail header, two-column detail grid, compact tables, and credit dialog sizing.
- `admin-frontend/src/styles/iap.css` - Styles the tabbed IAP admin shell, the four-tab strip, active tab pointer, tab content panels, the split payment-tools 30/70 layout with equal-height nav/content panels, inline beneficiary/payment/refund sections, the beneficiary form popup trigger state, labeled package function actions plus grouped package edit/delete icon buttons, API-key format helper text, inline code pills, and the bank-hook history and payment-transaction detail layouts.

## server
- `server/app.py` - Flask API entrypoint for the desktop subtitle service, with backend health check plus auth APIs, admin APIs, Redis-backed IAP package APIs, Sepay QR payment APIs, backend-served admin web routes, hourly backend file logging setup, and proxy route composition.
- `server/logging_setup.py` - Configures hourly rotating backend log files under the root `logs/` directory, wiring Flask and Werkzeug logging into UTF-8 file output without duplicating handlers on repeated app initialization.
- `server/auth_routes.py` - Registers optional local auth endpoints with PBKDF2 password hashing, login by email or username, HS256 JWT access tokens, rotating refresh tokens, current-user lookup, logout revocation, admin access guards, and temporary-admin bootstrap integration while enforcing locked-account access denial for locked non-admin users and returning the latest per-user credit balance, premium window state, role state, and lock state.
- `server/auth_identity.py` - Owns shared auth-side username normalization plus public-user shaping helpers, including normalized premium timestamps and lock flags, used by auth routes and admin routes so `auth_routes.py` stays below the 400-line guardrail.
- `server/auth_store.py` - Owns MySQL-backed auth persistence for registered users, usernames, roles, premium timestamps and effective premium state, locked-account state, current credit balances, and the shared auth schema bootstrap, seeds the local demo user credit row, delegates premium-window shaping to `server/auth_user_record.py`, delegates refresh-token persistence to `server/auth_refresh_store.py`, and delegates credit-ledger mutations to `server/auth_credit_store.py` so the main auth store stays below the 400-line guardrail.
- `server/auth_user_record.py` - Centralizes auth-user role, lock, and premium-window normalization plus row-to-user shaping helpers so timed premium logic can stay shared without pushing `server/auth_store.py` over the 400-line guardrail.
- `server/auth_refresh_store.py` - Owns refresh-token persistence helpers split out of `server/auth_store.py`, including token cleanup, lookup, storage, and revocation, while preserving the existing auth-store public API.
- `server/auth_credit_store.py` - Owns the MySQL credit-history ledger, typed credit balance mutations, admin credit grants or adjustments, and paginated per-user credit-history reads used by the admin detail page.
- `server/admin_store.py` - Owns admin-side auth-user queries for checking whether any persisted admin exists, summarizing users, paginating accounts, filtering the paginated user list by display name, username, or email search, loading a single user detail record, and applying admin role or premium-window or lock or balance changes with credit-ledger integration while preventing admin accounts from being locked.
- `server/admin_bootstrap.py` - Creates and logs the temporary in-memory bootstrap admin credentials when no persisted admin exists, logs the explicit skip reason when a real admin already exists, and clears that temporary state once a real admin account is created.
- `server/admin_routes.py` - Registers the admin-only backend API for completing temporary-admin bootstrap, reading paginated or searched user lists, loading per-user detail, requests, and credit history, adding credits from the admin detail page, updating user role or premium window or lock or balance, and listing recent backend requests.
- `server/iap_store.py` - Owns MySQL-backed IAP package persistence and schema creation, validates package ids plus names plus price or currency or pack-type fields, persists the admin-managed `isRecommended` flag alongside active state, preserves legacy package credits, and supports listing, creating, updating, and deleting IAP catalog records.
- `server/iap_api_key_store.py` - Owns MySQL-backed payment-hook API key persistence, including configured HTTP method, inbound header name, and a `<API_KEY>`-based header value format template, and validates live `/api/pay/info` requests against those settings.
- `server/iap_admin_store.py` - Owns MySQL-backed admin-only IAP configuration persistence for pack function mappings and sale rules, including create, update, and delete support for timed premium-duration and combined credit-plus-premium pack-function records, separate from payment-hook API key storage.
- `server/iap_cache.py` - Owns the Redis cache helper for the public client IAP package list, targeting `localhost:6379` with the `audio_editor` key prefix and graceful fallback when cache access is unavailable.
- `server/iap_routes.py` - Registers the public client IAP package read API, the `/api/pay/info` payment-hook endpoint that matches configured API-key method, header name, and header value format settings, logs each validated bank payload into history, passes it into the IAP payment processor, returns a minimal `historyId` plus `success` acknowledgment, and admin-only IAP package, API key, searchable/date-filtered bank-hook history list, bank-hook history detail, editable pack-function, and sale APIs, while serializing package `packType` plus the admin-managed `isRecommended` flag and timed premium-duration pack-function fields and invalidating the Redis-backed public package cache after package mutations.
- `server/iap_beneficiary_store.py` - Owns MySQL-backed IAP beneficiary account persistence for Sepay QR payment receivers, including name, bank id, bank account, CRUD helpers, and single-current-account enforcement.
- `server/iap_payment_store.py` - Owns MySQL-backed Sepay QR payment tickets, refund pending records, 3-minute ticket expiry, bank-hook matching against transaction content/account/amount, pack-function entitlement application, the persisted `last_client_check_at` timestamp used to show when the desktop app last polled a payment ticket, and the server-side cancellation of pending desktop QR payment tickets.
- `server/iap_payment_records.py` - Holds the shared payment ticket/refund row mappers plus the generic payment/refund pagination helpers split out of `server/iap_payment_store.py` so the main payment store stays below the 400-line guardrail.
- `server/iap_payment_routes.py` - Registers authenticated desktop payment ticket create/status/cancel APIs plus an authenticated backend QR-image proxy for desktop checkout, stamps the payment ticket with the latest desktop status-check time whenever the client polls status, exposes admin beneficiary account plus payment transaction list/detail plus refund-pending APIs, and starts the payment expiry worker.
- `server/iap_payment_expiry.py` - Starts a once-per-process daemon worker that expires pending IAP payment tickets every minute when clients stop polling.
- `server/iap_bank_hook_history_store.py` - Owns MySQL-backed persistence, searchable/date-filtered pagination, and single-record lookup for bank-hook transaction history captured from `/api/pay/info`, storing the matched API key snapshot, bank transaction fields, normalized timestamps, and raw payload JSON for the admin history pages.
- `server/admin_web_routes.py` - Serves the standalone React admin frontend build from `admin-frontend/dist` for `/admin`, `/admin/login`, `/admin/setup`, `/admin/manage`, `/admin/iap`, all nested `/admin/iap/...` SPA paths including route-driven IAP tabs and payment transaction detail URLs, plus `/admin/users/:id`, exposes built `/admin/assets/*`, and redirects `/console` to the React management route.
- `server/proxy_routes.py` - Registers the translation and Vbee voiceover proxy endpoints, reuses shared credit-charge helpers, falls back to the MySQL-backed local translation job when the upstream translation worker is unavailable, returns local translation downloads from database content, proxies Vbee audio downloads back through Flask, and delegates transcription route registration to `proxy_transcription_routes.py` so the main proxy file stays under the 400-line guardrail.
- `server/proxy_transcription_routes.py` - Owns the authenticated transcription proxy endpoints, normalizes `WHISPER_API_URL` for the internal Docker `whishper` service, sends subtitle jobs to Whishper's direct internal FastAPI endpoint on port `8000`, charges or refunds subtitle-generation credits, handles sync transcription responses that return `segments` immediately, saves owned request ids, and only polls downstream status for legacy async providers.
- `server/proxy_credit_helpers.py` - Centralizes shared credit charge or refund helpers plus auth-store error responses used across the proxy-backed transcription, translation, and voiceover routes.
- `server/proxy_route_helpers.py` - Owns the shared proxy-route helper functions split out of `server/proxy_routes.py`, including provider-response shaping, request-record persistence helpers, owned-request checks, translation fallback response helpers, and Vbee router URL resolution.
- `server/request_store.py` - Owns MySQL-backed persistence for server-managed request/job state, including user-owned transcription, translation, and voiceover request records plus local translation fallback job output, initializes the request tables, migrates legacy translation-job files into MySQL without creating new request files, lists recent request records for the admin console, and paginates per-user request history for the admin detail page.
- `server/translation_fallback.py` - Runs local subtitle translation jobs via `deep-translator`, stores user-owned job metadata and translated SRT output in MySQL through `server/request_store.py`, writes completed generic request records as `success`, and serves the fallback `start -> status -> download` flow used when the upstream translation service cannot accept work.
- `server/requirements.txt` - Lists Flask, HTTP client, translation, MySQL connector, and Redis client dependencies for the subtitle-service backend.
- `server/Dockerfile` - Builds the backend runtime as a multi-stage image, compiling `admin-frontend/dist` in a Node stage, installing Python backend dependencies plus Gunicorn, and serving the Flask backend on port `5000` with a health check.

## root
- `.gitignore` - Excludes generated workspace artifacts including persisted project folders, runtime backend log files under `logs/`, the local Docker `.env` secret file, and bind-mounted Docker data directories for backend logs, uploads, MySQL, Redis, and the internal Whishper worker stack.
- `.dockerignore` - Keeps Docker build context small by excluding git metadata, Python caches, local logs or projects, and installed frontend dependencies or built assets.
- `.env` - Stores local Docker Compose secrets and runtime defaults for the backend stack, including the Cloudflare tunnel token, MySQL password, database name, and JWT secret.
- `docker-compose.yml` - Defines the containerized backend stack with `web`, `mysql`, `redis`, `phpmyadmin`, `cloudflared`, plus internal `mongo`, `translate`, and `whishper` services for the Whisper-compatible transcription worker; Flask reaches Whishper through the internal Docker API target `http://whishper:8000` instead of a host-published port, while phpMyAdmin remains the only database UI exposed on port `8080`.
- `EXPORT_ARCHITECTURE.md` - Describes the target fast-export architecture: Electron-main native FFmpeg coordination, shared render manifests and overlay assets for parity, hardware probing, and chunk-level parallelism.
- `TASK.md` - Tracks active work, validation state, and follow-up refactors for this repository.
- `MAP.md` - Maps files to responsibilities so future tasks can route to the correct owner quickly.